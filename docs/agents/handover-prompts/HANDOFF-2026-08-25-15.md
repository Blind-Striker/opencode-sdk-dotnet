# Session Handoff - the runtime increments are landed; harvest, push, then the emitter batch

Date: 2026-08-25

Consume this against live Git, the worktree, and GitHub before changing anything. This handoff
supersedes and deletes `HANDOFF-2026-08-24-14.md`. Repository files and current canon beat this
handoff. Decisions live in research log Sessions 40–41 (Q126–Q136); ADR-0018 records the pipeline
composition; the operative plan `docs/superpowers/plans/2026-08-24-runtime-pipeline-plan.md` is
fully executed through Increment 4 and ready for archival judgment.

## Start here

The worktree is clean; the runtime-arc series is pushed through `9da0ae3` — verify the exact
state against live Git, as a few local docs commits may sit ahead of `origin/master`. The
arc-milestone full benchmark run (Q131 cadence: full suite, default job, both runtimes) is
harvested into research Q138; its artifacts live in the local benchmark-artifacts store outside
the repository. Push only on the maintainer's explicit ask; CI validates the whole series once
GitHub Actions billing is restored.

## What Session 41 landed (all local commits, in order)

| Commit | Content |
|---|---|
| `f6b2223` | Increment 1 — `FailureClassification` phase map (caller token first) |
| `910e05d` | Increment 2 — internal policy pipeline + ADR-0018 |
| `0f3cda2` | Q130 benchmark evidence (Inc 1→2) |
| `d58c72e` | Q131 — tiered benchmark cadence sealed into `quality-gates.md` |
| `2d0c1b1` | Increment 2b — generated `StatusVerdict Classify`; drain canon; doc 20 E5 |
| `3b1dfd7` | Q132 evidence |
| `7ce8bc0` | Increment 3 checkpoint — dependency rule in `platform-and-packaging.md`; downlevel `System.Memory`/`System.Buffers`; Q133 |
| `8632408` | Increment 3 — pooled buffering + progress timeout; timeout canon rewritten |
| `c7d52d3` | Q134 evidence (net10 pipeline row flat at 2,112 B per size) |
| `429a08a` | Increment 4 — exception-free encoding; `Microsoft.Bcl.Memory`; parity matrix (R09/R10 closed) |
| `31e2be8` | Q135 evidence |
| `4617562` | Q136 — real pinned-v2-server acceptance run (green; see below) |
| `c6ef5cd` | Q137 — upstream v2 drift map since the pin |
| (tail) | Handoff supersession and queue updates |

Increment 0 archived the R16 experiment: the patch is preserved in the local
benchmark-artifacts store outside the repository, plus local branch `scratch/r16-experiment`
(`03244d6`, never push). Sealed follow-on decisions in Q133: `Microsoft.Bcl.TimeProvider`/clock
seam — neither (M6 trigger); `System.Collections.Immutable` — deferred to the emitter batch;
`System.Net.ServerSentEvents` — deferred to SSE stage-2.

## Acceptance state

The sandbox ran against the pinned v2 server built from `external/opencode` @ `a6a712a3`
(`bun install --frozen-lockfile`, `bun run src/index.ts serve` in `packages/cli`; the server
prints a generated password — export `OPENCODE_PASSWORD`, and run the sandbox with
`--no-launch-profile` or its launchSettings endpoint wins). One-shot, stream (`EventLogSynced`
typed frame + live hold), and events (`SessionCreated` live dispatch) are green; pagination ran
against an empty session (zero items — needs a provider-configured session for a non-empty pass)
and the sandbox exits nonzero on empty enumeration (nit, unfixed). The installed `opencode.exe`
1.18.21 is the v1 line; its health body is correctly refused by the pinned contract — do not use
it as the oracle.

## Exact next

1. CI green on all three OSes (billing gate above), then
2. `docs/ROADMAP.md` refresh — it still queues the executed increments; the plan document is
   fully executed and is a candidate for archival per `docs/engineering/documentation.md`.
3. **Spec refresh — blocked upstream (2026-08-25):** the attempt found upstream's document lost
   its SSE payload schemas to the effect beta.107 regen (Q139 supersedes Q137's stream-channel
   claim; the one-shot map stands). Pin stays `a6a712a3`; reported with a verified restore path
   as [anomalyco/opencode#44911](https://github.com/anomalyco/opencode/issues/44911). Resume the
   `spec/SNAPSHOT.md` procedure when upstream restores the payload link.
4. The **emitter allocation batch** on the current `a6a712a3` base (backlog = doc 20's ranked table;
   `FrozenDictionary` decision rides it), then the **M4 launcher plan** (1–2 pages, approval
   before source).
5. Benchmark records infrastructure, agreed in shape (maintainer, 2026-08-25): a git-ignored
   `.benchmarks/` folder for run artifacts and comparison extracts, driven by a file-based C#
   app (the `tools/opencode-tool.cs` pattern) replacing the session-scratch PowerShell extract
   script; build it when benchmark work next runs, not before. Q138's extracts and the one-off
   HTML report were produced with the scratch script.
6. Leftovers, none blocking: docs items R04/R05/R06/R11 (Session 38/39 register); net472 pooled
   rents above the downlevel pool's 1 MB bucket cap still allocate 1× wire on >1 MB bodies
   (larger-cap `ArrayPool.Create` is a benchmark-gated follow-up); the sandbox `--paginate`
   empty-enumeration exit code.

## Onboarding order

1. `AGENTS.md`, `CONTEXT.md`, `docs/ROADMAP.md`, this handoff.
2. Research log Sessions 40–41 (Q126–Q136); ADR-0018; the executed plan for background.
3. `docs/engineering/` canon (quality-gates has the new benchmark cadence; workflow, styles) and
   `docs/architecture/` (client-runtime's rewritten timeout paragraph; platform-and-packaging's
   new Dependencies section).
4. Runtime source: `Pipeline.cs`, the policy files (`RequestDecorationPolicy`,
   `ResponseBufferingPolicy`, `TransportPolicy`), `PipelineMessage`, `ResponseMaterializer`,
   `FailureClassification`, `StatusVerdict`, `ResponseEncodingPolicy`, `FrameDispatch`,
   `IEventStreamFramer`.

## Completion gates

Unchanged from `docs/engineering/quality-gates.md` (slopwatch, Release build, format
whitespace/style, tests; generator changes add the tool smoke and `generate --verify`).
Benchmark cadence is tiered per Q131: targeted `--job short` per increment, full default-job
suite per arc. Push only on explicit ask.

## Ready-to-paste priming prompt

```text
Start a session for opencode-sdk-dotnet from
docs/agents/handover-prompts/HANDOFF-2026-08-25-15.md. Consume it against live Git, the clean
worktree, and GitHub before changing anything.

The runtime-pipeline arc (Increments 0-4) is fully landed as an unpushed local series with
per-increment evidence in research log Q130-Q136 and a green real-server acceptance run. First
harvest the arc-milestone benchmark artifacts from the local benchmark-artifacts store, then
surface the push decision, then continue with the ROADMAP refresh and the emitter allocation
batch per the handoff's queue. Direct green commits stay authorized in the development loop;
never push without an explicit ask; the agreed queue order is push, spec refresh, emitter batch; canonical-document edits only with explicit maintainer
approval.
```
