# Session Handoff — the location + PTY family arc is live, the pin is at 803ead32

Date: 2026-08-27 (second handoff this date; it fully replaces the morning one in place)

Consume this against live Git, the worktree, and GitHub before changing anything. Repository
files and current canon beat this handoff. This session's evidence lives in research log
**Q151**; the arc's decisions live in ADR-0021 and the amended program design
(`docs/superpowers/specs/2026-08-26-continuous-protocol-coverage-program-design.md` §3.3/§5).

## Start here

- Everything is pushed as of this handoff (the push includes this file). **Check the three-OS
  hosted run for that push before building on top** — every prior push is green. `#50` stays
  open only for branch protection.
- The worktree is clean. The full local gate was green at the last commit: **2,767 tests**,
  `generate --verify` current, `refresh-spec --verify` reproducing the committed receipt.
- **The accepted snapshot is `803ead32e733e32f94bfeb29248def8b9781a3be`** (v2, 2026-08-27):
  a document-identical refresh from `954cdc7b` — 133 operations, 336 components,
  `contentSchema` restored at 2 by the SSE Restore patch. Identity: `spec/SNAPSHOT.md` +
  `spec/receipt.json`; patch: `spec/patches/001-*`.
- **Profile: 81 selected / 52 pending.**

## What this session landed (aggregated)

1. **The second receipt-governed refresh** (`954cdc7b` → `803ead32`): byte-identical
   normalized document, so only the pin identity, receipt, and submodule moved. The SSE
   patch still applies (PR #45182 unmerged); `rawSha ≠ baselineSha` persists (doc 21 T7).
2. **The typed-location + hand-written PTY family arc** (plan-first, subagent-driven, every
   task independently reviewed; final whole-branch review + one fix wave, clean):
   - `OpenCodeRequestOptions.Location` — per-call location merged member-by-member over
     ambient in `RequestDecorationPolicy` (per-call wins, null inherits, no clearing;
     directory percent-encoded, workspace raw).
   - Generator **internal-raw emission mode** (`"emission": "internalRaw"` group row) and
     the **declared-headers runtime channel**: document-declared header parameters emit as
     internal method parameters feeding `PipelineMessage.DeclaredHeaders`; the wire-shape
     wall admits header parameters for internal-raw groups only, and merged families must
     agree on emission. The channel is generic — `persistentPty.connectToken` declares the
     same header and inherits it.
   - The pty family flipped: hand-written `PtysClient`/`PtyClient` over generated
     `PtysRawClient`/`PtyRawClient`; public `ListPtysAsync` and `CreateConnectTokenAsync`;
     the `x-opencode-ticket: "1"` sentinel lives ONLY in the hand-written door
     (ADR-0013/0021). The PublicApi swap was pure addition.
   - A **transportOwned fingerprint** row pins `v2.pty.connect`'s canonicalized subtree so
     spec drift on the hand-built WS operation fails `generate` loudly.
   - **`PtySession`** — the WebSocket working object: `PtyClient.ConnectAsync(PtyConnectOptions)`
     (Cursor: null = full replay, -1 = live-only, n ≥ 0 = resume; per-call Location merges
     over ambient onto the connect query), `ReadAsync()` yielding
     `PtyOutputFrame`/`PtyCursorFrame`, `WriteAsync`, graceful disposal hardened against
     in-flight writes. Basic auth rides the upgrade ticket-less — the server's auth
     exemption keys on ticket presence, so tickets exist for browser handoff only.
   - **Live proof** (Q151, sandbox `PtySessionWalkthrough`): ticket-less upgrade 101 (curl
     control: 401 without credentials), token door 403/200, replay + exactly one cursor
     frame, cursor-resume replayed the exact delta and zero at tip, `end=ServerClose` on
     remove. Discovery: **a terminal's Enter is CR** — `WriteAsync("cmd\n")` renders but
     never submits; send `"cmd\r"`. Now in canon (client-runtime PTY section) and on
     `WriteAsync`'s doc.
3. **The polish batch**: all 15 final-review FIX-LATER items closed (403 typed test,
   LocationMerge≡policy equivalence matrix, validator `continue`, doc/message fixes,
   pre-Win8 mapping, test renames), plus `PtySession.SubmitAsync(string)` parked in
   ROADMAP until its first real consumer (expected: this repo's MCP server work).

## The next work — envelope completion (C2)

ROADMAP: "the queue now opens at envelope completion." Design §6 is the sealed shape:

- `EnvelopePlan` carries a `TypeReferencePlan` instead of only a type-name string; an
  envelope payload is accepted when its ingested `SchemaNode` binds to a supported plan —
  named models, lists, dictionaries, promoted inline object models, represented nullable
  payloads. DTO/envelope emitters use the existing `TypeSyntaxEmitter`.
- 18 of 31 refusals fall to this (doc 21 §4 table). Unsupported nodes still fail closed;
  **family-specific array/dictionary/inline exceptions are forbidden**. Inline models need
  deterministic operation-scoped naming or a reasoned naming row. A successful nullable
  payload is distinguished from an error path by response state, never by treating `null`
  as an unset backing field (the non-nullable `{"data":null}` refusal stands, Q148).
- Read first: design doc §6, `docs/architecture/protocol-and-generation.md`,
  `EnvelopeFacetBinder`/`TypePlanBinder`/`EnvelopeEmitter`/`EnvelopeDtoEmitter`, research
  doc 21 §4.

Queue after: operation inventory + assurance ledger; M4 launcher/fixture + the
deterministic simulated-session workflow (C# WS controller in `tests/Shared`);
persistentPty's HTTP batch (its connectToken inherits the declared-header channel) and —
now unblocked, §10's wait satisfied — the persistentPty WS door reusing the PtySession
machinery. Riders: doc 18 B3/B4, the deferred default-job benchmark, prerelease packaging
(ADR-0006/#51).

## Standing facts and gotchas

- **Per-session refresh cadence is active** (ADR-0020): open with
  `refresh-spec --ref origin/v2`, review the receipt, apply on maintainer approval. A
  document-identical candidate still moves the pin only if the maintainer says so.
- **PR #45182** (our SSE restore) is open upstream, `needs:issue`; when it merges, prepare
  refuses the patch and an empty-patch refresh retires it.
- Parked upstream reports unchanged (ROADMAP owns the list): T3 operationIds (draft at
  `.scratchpad/upstream-issue-drafts/`), T2 `security: []`, T6 lost descriptions, T7 stale
  document, the undeclared ticket value.
- Sandbox from CLI needs `--no-launch-profile`. Server for smokes:
  `OPENCODE_SERVER_PASSWORD=<pw> bun src/index.ts serve --hostname 127.0.0.1 --port <p>`
  from `external/opencode/packages/cli` (`bun install --frozen-lockfile --ignore-scripts`
  once at the checkout). The sandbox's `--paginate` mode still exits nonzero on an empty
  enumeration (known gap).
- **PTY input**: Enter is CR (`\r`). The walkthrough constant documents it; so does canon.
- Naming traps carried: the location query member is `workspace`, the
  `session.create`/`import` body member is `workspaceID`; doc 18 refactor items are
  `B1/B2/B3/B4` while mechanism batches were `B-1`, `B-2` — unrelated tracks.
- Cosmetic leftover: `ISessionMessageAssistantContentEncoded` keeps its suffix by the
  sibling-guard's design.

## Onboarding order

1. `AGENTS.md`, `CONTEXT.md`, `docs/ROADMAP.md`, this handoff.
2. For the arc's mechanisms: ADR-0021, `docs/architecture/client-runtime.md` (Location and
   PTY sections), research log Q151.
3. For envelope completion: the design doc §6, `docs/architecture/protocol-and-generation.md`,
   research doc 21 §4, then the binder/emitter files named above.
4. `spec/SNAPSHOT.md` + `spec/receipt.json` for the snapshot identity.

## Completion gates

Unchanged (`docs/engineering/quality-gates.md`): slopwatch, Release build, format
whitespace/style, tests; generator changes add the tool smoke and `generate --verify`;
snapshot changes add `refresh-spec --verify`. Repo policy: no AI trailers; direct green
commits stay authorized in the development loop; **never push without an explicit ask**;
canonical-document edits only with explicit maintainer approval.

## Ready-to-paste priming prompt

```text
Start a session for opencode-sdk-dotnet from
docs/agents/handover-prompts/HANDOFF-2026-08-27.md (the second, post-arc revision).
Consume it against live Git, the worktree, and GitHub before changing anything.

State: the typed-location + hand-written PTY family arc is live (ADR-0021, research
Q151), the accepted snapshot is 803ead32 (81 selected / 52 pending), and everything is
pushed - check the three-OS hosted run for the latest push first.

Next work: envelope completion (C2, design doc section 6) - EnvelopePlan carries a
TypeReferencePlan so list/dictionary/promoted-inline/nullable payloads bind; 18 of 31
refusals fall; family-specific exceptions are forbidden. It is plan-first: read design
section 6, protocol-and-generation.md, and research doc 21 section 4, then write the
plan for my review before implementing.

Per-session refresh cadence is active: you may open with refresh-spec --ref origin/v2
and show me the receipt. Repo policy: no AI trailers; never push without an explicit
ask; canonical-document edits only with explicit maintainer approval.
```
