# Session Handoff — the coverage program is canon, the synchronizer is live, the pin is at 803ead32

Date: 2026-08-27

Consume this against live Git, the worktree, and GitHub before changing anything. This is the
first handoff after the 2026-08-26 one was consumed and deleted. Repository files and current
canon beat this handoff. This session's evidence lives in research log **Q148–Q150**; the sealed
program lives in ADR-0020/0021/0022 and
`docs/superpowers/specs/2026-08-26-continuous-protocol-coverage-program-design.md`.

## Start here

- **Master has moved past this handoff's `76103b6`.** The typed per-call location + hand-written
  PTY family arc this handoff opened has since landed and been retired at `7df4be0` (15 commits
  past `76103b6`, not pushed to `origin/master`) — see "The typed location + hand-written PTY
  family arc — LANDED since this handoff" below. Check the hosted runs before building on top;
  every push through `76103b6` is green. `#50` stays open only for branch protection.
- The worktree is clean. The full local gate was green at `76103b6`: 2,333 tests,
  `generate --verify` current, `refresh-spec --verify` reproducing the committed receipt. The
  arc's own gate, at its retirement commit `7df4be0`, was green; the post-arc polish batch on
  top of it (FIX-LATER items plus two doc additions) is green with 2,767 tests.
- **The accepted snapshot is `803ead32e733e32f94bfeb29248def8b9781a3be`** (v2, 2026-08-27):
  133 operations, 336 components, `contentSchema` restored at 2 by the SSE Restore patch. It
  reached this commit through a document-identical refresh off `954cdc7b`, so the surface is
  unchanged and only the identity moved. Identity: `spec/SNAPSHOT.md` + `spec/receipt.json`;
  patch: `spec/patches/001-*`.
- **Profile: 81 selected / 52 pending** — `pty.list` and `pty.connect.token` joined it with the
  hand-written PTY family (ADR-0021).

## What this session landed (aggregated)

Three layers, in order:

1. **The program sealed as canon** (research Q148; the amended design doc). A full design-tree
   grilling plus four read-only upstream fact-findings (serve/stdio contract, the simulation
   backend, persistentPty's marking, the server's location resolver) produced the decision set:
   ADR-0020 (receipt-governed snapshot production, Restore-only patches, per-session refresh
   cadence once tooling exists — **now active**), ADR-0021 (normal PTY public surface
   hand-written over generated internals; split-partials rejected), ADR-0022 (deterministic
   evidence gates releases). In-place revisions: ADR-0003/0005 (relays), ADR-0007 (options carry
   `LocationSelector`; future NoThrow-less stream options type), ADR-0008 (SSE-completeness is a
   snapshot property; PTY exception relay), ADR-0013 (production/consumption boundary;
   operation-identity curation rows). CONTEXT.md gained the accepted-snapshot vocabulary
   ("spec pin" retired); ROADMAP re-scoped M4/M5/M6 and closed #37 (ambient + typed per-call
   location, member-by-member merge, matching the server's own per-member resolver).
   ExperimentalDeferred was dropped entirely — persistentPty is ordinary target surface.
   Doc 21 was re-verified at tip `6170221e` (Q149): everything held.

2. **The synchronizer** (Q150; plan executed and retired). `opencode-tool refresh-spec` with
   three modes — `--ref <commit-ish>` prepares a candidate (resolves the ref once, identity
   transform on an empty patch list, else the exact pinned upstream generator over ordered
   hash-pinned Restore patches; writes receipt + document to `.scratchpad/refresh/<sha>/`),
   `--apply <receipt>` installs the accepted snapshot after human review (refuses TOCTOU drift;
   never stages/commits), `--verify` reproduces the committed identity. The SSE patch was cut
   from PR #45182's source-only subset; its repair predicate self-retires when upstream merges.
   Ingestion pre-work rode ahead: header parameters ingest (selected operations refuse at the
   binder wall until a runtime channel exists), `contentEncoding` strings project as the
   fail-closed `EncodedStringNode`, and reason-bearing `operationIdentities` curation rows map
   upstream identity defects at ingestion with stale-row retirement.

3. **The first accepted refresh** (`a6a712a3` → `954cdc7b`, Q150). Question family gone
   (2 shipped ops removed — the first removal-bearing PublicApi review, accepted);
   `pty.connect.token` deselected on that refresh (its new `x-opencode-ticket` header parameter
   announced itself at the new wall exactly as Q146 predicted) and **selected again in the arc
   below**, now that ADR-0021's hand-written family owns the door and applies the header through
   the internal channel; eight `persistentPty.*` identity rows map the leaked
   `server.experimental.` group id;
   pin-era `…1` curation rows retired; the restored event tree's `_N` duplicates collapsed via
   structurally validated aliases — **the `ProviderState_N → Form.Metadata` structural
   coincidence was refused by hand** (doc 21 O6's blind spot, exercised for real: sixteen
   provider-state records alias within their own family only). **`ProjectionArtifactNamePolicy`**
   (maintainer-sealed, deliberately reusable) strips Effect's encode-side `*Encoded` suffix from
   derived names unless the unsuffixed component exists — the same rule class as the `v2.`
   strip; goes quietly dead if upstream fixes their projection. Live proof: the committed
   sandbox's full standing walkthrough against a server built from the new pin — 24 operations
   as declared, `session.interrupt`'s new typed 200 observed on the wire.

## The typed location + hand-written PTY family arc — LANDED since this handoff

The maintainer answered the open question this handoff recorded: the PTY WebSocket lives **inside**
the arc. The arc then executed in full (evidence: research log **Q151**; status: `docs/ROADMAP.md`).
What shipped:

- **Typed per-call location:** `OpenCodeRequestOptions.Location` (`LocationSelector?`), merged
  member by member over ambient (per-call wins, null inherits, no per-call clearing), injected
  uniformly in `RequestDecorationPolicy`. Naming trap that still bites tests: the query member is
  `workspace`, the `session.create`/`import` body member is `workspaceID`.
- **PTY family (ADR-0021):** the generator's curation-declared internal-raw emission mode and the
  internal, document-bounded header channel; hand-written `PtysClient`/`PtyClient` over the
  generated internal raw clients; the token door applying `x-opencode-ticket: "1"` internally and
  never as a caller's argument; a `transportOwned` fingerprint guarding `v2.pty.connect`.
- **`PtySession`:** `PtyClient.ConnectAsync(PtyConnectOptions)` opens it; `ReadAsync` yields
  `PtyOutputFrame`/`PtyCursorFrame`, `WriteAsync` sends input, disposal closes. Cursor semantics:
  omitted replays everything, `-1` attaches live-only, `n >= 0` resumes.
- **Live proof** in the committed sandbox's PTY leg: the ticket-less upgrade carrying only the
  Basic credential answered `101`, one cursor frame closed the replay, a cursor resume replayed
  only what followed, and removing the PTY ended the read as a normal close.

Queue now: envelope completion (C2 — 18 of 31 refusals: list/dictionary/inline payloads via
`TypeReferencePlan`), operation inventory + assurance ledger, persistentPty's HTTP batch (it
inherits the header channel this arc built), M4 launcher/fixture + deterministic
simulated-session workflow (C# WS controller in `tests/Shared`). Riders: doc 18
B3/B4, the deferred default-job benchmark, prerelease packaging (ADR-0006/#51).

## Standing facts and gotchas

- **Per-session refresh cadence is active** (ADR-0020): open working sessions with
  `refresh-spec --ref origin/v2`, review the receipt, apply on maintainer approval. Every
  prepare also surfaces upstream's stale committed document mechanically
  (`rawSha ≠ baselineSha` in the receipt = doc 21 T7).
- **PR #45182** (our SSE restore) is open upstream, `needs:issue` label; when it merges,
  prepare refuses the patch and an empty-patch refresh retires it.
- **Parked upstream reports** (file only when the maintainer chooses — ROADMAP owns the list):
  T3 operationIds (draft ready at `.scratchpad/upstream-issue-drafts/`), T2 `security: []`,
  T6 lost descriptions, T7 stale document, the undeclared ticket value.
- Sandbox from CLI needs `--no-launch-profile` or the checked-in profile hijacks
  endpoint/mode. Server for smokes: `OPENCODE_SERVER_PASSWORD=<pw> bun src/index.ts serve
  --hostname 127.0.0.1 --port <p>` from `external/opencode/packages/cli` (run
  `bun install --frozen-lockfile --ignore-scripts` at the checkout first).
- Cosmetic leftover: `ISessionMessageAssistantContentEncoded` keeps its suffix by the
  sibling-guard's design (`SessionMessageAssistantContent` exists); one naming row away if it
  ever bothers the maintainer.
- Roadmap known gap carried: the sandbox's `--paginate` mode exits nonzero on an empty
  enumeration.
- PTY input gotcha (Q151): a terminal's Enter key is **CR**. Writing `"…\n"` into a PTY leaves the
  line rendered but unsubmitted — observed live against PowerShell — so the sandbox leg sends
  `"echo hello\r"`.
- Naming hazard (carried): doc 18 refactor items are `B1/B2/B3/B4`; mechanism batches were
  `B-1`, `B-2`. Unrelated tracks.

## Onboarding order

1. `AGENTS.md`, `CONTEXT.md`, `docs/ROADMAP.md`, this handoff.
2. ADR-0020/0021/0022; research log **Q148–Q151**; the program design doc (§5, §10, §13).
3. For the location and PTY surface the arc landed:
   `docs/architecture/client-runtime.md`, `RequestDecorationPolicy.cs`, `LocationSelector.cs`,
   `src/OpenCode.Sdk/Ptys/`, ADR-0007/0019/0021; Q148's location-resolver facts (server resolves
   per member: query > header > cwd for directory, query > header > unset for workspace).
4. `spec/SNAPSHOT.md` + `spec/receipt.json` for the snapshot identity.

## Completion gates

Unchanged (`docs/engineering/quality-gates.md`): slopwatch, Release build, format
whitespace/style, tests; generator changes add the tool smoke and `generate --verify`;
snapshot changes add `refresh-spec --verify`. Repo policy: no AI trailers; direct green commits
stay authorized in the development loop; **never push without an explicit ask**;
canonical-document edits only with explicit maintainer approval.

## Ready-to-paste priming prompt

```text
Start a session for opencode-sdk-dotnet from
docs/agents/handover-prompts/HANDOFF-2026-08-27.md. Consume it against live Git, the
worktree, and GitHub before changing anything.

State: the coverage program is canon (ADR-0020/0021/0022, research Q148-Q151), the
refresh-spec synchronizer is live, the accepted snapshot is 803ead32 (81 selected /
52 pending), and the typed per-call location + hand-written PTY family arc has landed
with live proof. Check the hosted runs before building on top.

Next work: envelope completion (C2), then the operation inventory and assurance ledger,
persistentPty's HTTP batch, and M4's launcher/fixture arc. Each is plan-first: a 1-2
page plan and my approval before source. Per-session refresh cadence is active: you may
open with refresh-spec --ref origin/v2 and show me the receipt.

Repo policy: no AI trailers; never push without an explicit ask; canonical-document
edits only with explicit maintainer approval.
```
