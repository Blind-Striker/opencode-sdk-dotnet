# Session Handoff - Arc 3b global event breadth closed and hosted

Date: 2026-08-19

Consume this against live Git and GitHub. Arc 3b is complete, reviewed, pushed, and hosted green.
No later M3 source increment has begun.

## Live Baseline

| Item | State |
|---|---|
| Source head | `8d2a79d` (`feat(sdk): add global event subscription`) |
| Branch | `master`, synchronized with `origin/master` before this docs-only handoff commit |
| Worktree | Expected clean after this handoff commit |
| Tests | 1,320 local modern-TFM executions, 0 failed, 0 skipped |
| Generation profile | 15 selected, 105 pending |
| Hosted CI | Run `32269060251` green on Linux, Windows, and macOS at `8d2a79d` |
| Completed gate | Arc 3b `v2.event.subscribe` breadth and live demonstration |
| Closed issue | #44 reason-bearing `Events.SubscribeAsync` naming |
| Closed issue | #49 representative fixtures plus mechanical union completeness |
| Carried issue | #53 remains open only for deferred M5 walls |
| ADRs | 0001-0016 |
| Research | Through Session 34 / Q116 |

Direct green commits to `master` remain authorized for the development loop. Work one green source
increment at a time: red evidence, smallest implementation, complete diff/PublicApi/manifest review,
focused tests, one fresh-context read-only review, all applicable gates, commit, push, hosted
Linux/Windows/macOS, owning-issue updates, and an aggregated handoff before the next session.

The main agent exclusively owns edits, generation, tests, measurements, commits, pushes, and GitHub
updates. Subagents are read-only reviewers/researchers. Do not use stacked PRs, implementation
worktrees, or parallel writers. `docs/superpowers/` remains transient reference material.

## Reading Order

1. `AGENTS.md`, `docs/architecture/client-runtime.md`, and
   `docs/architecture/protocol-and-generation.md`.
2. ADR-0003, ADR-0007 through ADR-0011, and ADR-0013 through ADR-0016.
3. `docs/engineering/coding-style.md`, `docs/engineering/testing-style.md`, and
   `docs/engineering/quality-gates.md`.
4. Research log Q115-Q116, then `docs/ROADMAP.md`.
5. Live closed issues #44/#49 and open #53, then this handoff.

Repository files and current canon beat this handoff. The pinned `spec/openapi.json` remains the
sole protocol-semantic input. Upstream source remains diagnostic evidence only (ADR-0013).

## Completed Checkpoint - Arc 3b (`8d2a79d`)

### Generated operation and live-event closure

- `client.Events.SubscribeAsync(CancellationToken)` is generated for the pinned parameterless GET
  `/api/event`. It exposes no request, filter, cursor, replay, resume, reconnect, or per-call options.
- Generic reason-bearing `operationNames` and `schemaNames` curation choose reviewed .NET names.
  Missing, pending, duplicate, blank-reason, invalid-identifier, and final owner/member collisions
  fail closed; emitters remain operation- and endpoint-independent.
- The pinned 87-branch `V2Event` closure emits as `IEvent`, with no public `V2` or spec-gen numeric
  suffix leakage. Converter maps, registry, source generation, compilation, PublicApi, manifest,
  route, adapter, client, Extensions DI, and runtime contract evidence all landed together.
- The inline Effect cause graph shared by both stream operations collapses through one structurally
  verified schema alias; Arc 3a typed failure behavior is unchanged.

### Plural membership and structural unions

- Shared session leaves implement both `IEvent` and `ISessionEventDurable`; a schema-valid
  `SessionCreated` fixture deserializes through both sibling interfaces to the same generated type.
- The binder refuses sibling union chains requiring the same marker name with different kinds.
- Representative runtime fixtures remain small. There is no invented 40/87 payload corpus;
  exact marked tag maps and structural token-to-arm maps are compared mechanically with bound plans,
  and the single emitted source-generated registry is checked exactly.
- ADR-0011 now shows marked versus structural examples. ADR-0016 owns token-distinct structural
  carriers: one sealed carrier plus `Kind`, guarded typed accessors/factories, pinned-order dispatch,
  a raw unknown arm for unclaimed non-null value tokens, and ambiguity/malformed-known walls.
- Same-primitive refinements collapse without emitting dead branch models. Special-number string
  spellings require an earlier text arm; no unsupported converter path remains latent.

### Review and gates

- Fresh-context review found eleven actionable items: special-number precondition, public spec-gen
  suffixes, missing structural map evidence, stale roadmap, stale sandbox README, over-broad name
  normalization, excess registry entries, resolved-never accounting, mixed test intent, dropped
  operation descriptions, and a duplicated namespace literal. All were fixed before commit.
- Slopwatch found zero issues. Release build passed with zero warnings/errors. Whitespace and style
  verification passed. All 1,320 modern-TFM test executions passed with none failed or skipped.
- Both tool entry smokes and generation verify passed at 15 selected / 105 pending.
- Hosted run `32269060251` passed Linux, Windows, and macOS.

### Live global-bus demonstration

- Exact package: `@opencode-ai/cli@0.0.0-next-17403`, server PID `597763`, port `41999` on Linux.
- The committed `--events` Generic Host worker identified that server and consumed the generated
  global event stream. It observed `EventServerConnected/server.connected`, then a separately
  triggered breadth invocation created session `ses_fe573d9ecffeIOfpw1CScSgYtI` and produced the
  shared typed `SessionCreated/session.created` frame.
- The background `nohup` launch inherited ignored SIGINT, so the evidence honestly uses SIGTERM:
  Generic Host logged `Application is shutting down...`, exited normally, and disposed its SDK
  client while the separately launched server remained healthy. Stopping the server separately made
  health refuse the connection.
- This was one Linux demonstration. The volatile bus has no replay/resume guarantee and the run did
  not induce overflow, a reserved failure frame, malformed payload, unknown variant, or network cut;
  deterministic contract tests own those paths. Research Q116 records commands and limitations.

## Next Gate - Align Before Source Work

No paginator, net472 transport, launcher, telemetry, retry, hook, #53 M5 wall, or unrelated
performance source work began. The next session must consume live roadmap/issues and align with the
maintainer on the next bounded M3 increment before touching source. The transient M3 plan lists
paginator, the net472 GA cluster, and the deliberate performance pass, but it does not override
current canon or maintainer sequencing.

## Completion Gates

The base gate remains:

```bash
dotnet tool run slopwatch analyze --exclude ".scratchpad/**,external/**" --fail-on warning
dotnet build --configuration Release
dotnet format whitespace --verify-no-changes --no-restore
dotnet format style --verify-no-changes --no-restore --severity warn
dotnet test --configuration Release --no-build
```

Generator/curation/output changes also run:

```bash
dotnet run --file tools/opencode-tool.cs -- --help
./tools/opencode-tool.cs --help
dotnet run --file tools/opencode-tool.cs -- generate --verify
```

Before committing, inspect status, every changed/new/deleted file, the complete diff, PublicApi,
manifest, and recent log. Push directly to `master`, wait for Linux/Windows/macOS, update owning
issues, and write the next aggregated handoff.

## Ready-To-Paste Priming Prompt

```text
Continue work on /home/deniz/src/opencode-sdk-dotnet from
docs/agents/handover-prompts/HANDOFF-2026-08-19-5.md.

Consume it against live Git and GitHub before touching files. Expected source head is 8d2a79d and
hosted run 32269060251 is green on Linux, Windows, and macOS. Arc 3b v2.event.subscribe breadth,
#44, and #49 are complete; no later M3 source work has begun.

First align with the maintainer on the next bounded M3 increment using current canon and roadmap.
Do not silently start paginator, net472 transport, launcher, telemetry, retry, hook, #53 M5 walls,
or unrelated performance work.
```

Delete this handoff only when the next bounded increment ships and a newly aggregated handoff
supersedes it.
