# Session Handoff — Arc 3a is reviewed; model and generator closure are next

Date: 2026-08-17

Consume this against live Git. The reviewed source head is `045113f` on `master`; this handoff
lands as the docs-only commit immediately after it. Delete the handoff when Arc 3b and the
review-derived M3 queue below are closed.

## State

| Item | State |
|---|---|
| Source head | `045113f` (`fix(sdk): reject malformed UTF-8 in event streams`) |
| Worktree | Expected clean; verify before work |
| Tests | 1204 local modern-TFM executions, 0 failed/skipped |
| Profile | 14 operations selected, 106 pending |
| Hosted CI | Budget restored; `b19e32d` rerun green on Windows/macOS/Linux; verify newer runs live |
| ADRs | 0001–0012 |
| Research | Through Session 28 / Q105 |
| Review issues | #39–#53 plus riders on #23/#24/#27/#30 |

Every commit in the development loop runs the six local gates under `set -o pipefail` and prints
`ALL-GREEN` only after all pass. Direct pushes to `master` are explicitly authorized for this
loop; permanent branch-protection policy remains tracked by #50.

## What landed

- **Independent review and verification:** 18 fresh-context passes across runtime, generator,
  generated output, both test projects, shared infrastructure, concurrency, performance, and
  TFM/AOT behavior. Findings were cross-fed, deduplicated, and independently reproduced or
  traced before issue filing. The durable result is issues and docs, not raw agent transcripts.
- **Factual correction:** Appendix B claims 1–11 held. Claim 12's “no runtime behavior change”
  was too broad; claim 13's zero-sequence explanation was false. Research doc 02 records the
  default server's sequence/payload-persistence behavior.
- **Policy seals:** fixed literals are read-validated constants (ADR-0004/#45); known objects
  skip additive unmapped fields while hybrids fail closed (ADR-0012/#46); lossy operation naming
  and parameter typing use reason-bearing generic curation (#40/#44); runtime stream fixtures
  stay representative while mechanical gates prove breadth (#49).
- **Review closure commit `b7125c9` (#39):** lazy enumeration rechecks disposal, buffered frames
  honor cancellation before dispatch, and untouched-token stream cancellation maps to
  `OpenCodeTransportException`. A prefix-then-block test proves first-frame delivery before EOF.
- **Review closure commit `045113f` (SSE half of #42):** the frame reader rejects invalid and
  incomplete UTF-8 with the decoder failure retained. `MemoryDiagnoser` stayed exactly
  321.54 KB for 64 large frames and 177.69 KB for 1024 small frames. The one-shot half of #42
  remains on #23's Arc 6 adapter handoff.
- **Issue composition:** new #39–#53; #23 gained timeout/media/UTF-8/ownership correctness riders,
  #24 writer safety/recovery riders, #27 naming/curation riders, and #30 location diagnostics.

## Onboarding Order

1. `AGENTS.md`, then every ADR (0001–0012).
2. `docs/engineering/coding-style.md` and `testing-style.md`.
3. `docs/ROADMAP.md`.
4. `docs/superpowers/plans/2026-08-15-m3-plan.md`.
5. Research doc 02's v2 SSE section and doc 15 §5.
6. Research log Sessions 24–28 (Q92–Q105).
7. Read the GitHub issue for the increment being executed; its trigger and done boundary are
   part of the task.

## Exact Queue

Work one green increment at a time, red test first, six gates, commit, push, then close or update
the owning issue.

### A. Finish the immediate review closure

1. **#41 — special numbers.** Preserve `SpecialNumberNode` through binding/emission so normal
   numbers and exactly `"NaN"`, `"Infinity"`, `"-Infinity"` round-trip under source-generated
   metadata; keep arbitrary numeric strings and wire null refused.
2. **#48 — collection element/value nullability.** Reject null list elements and dictionary
   values only where the represented item/value schema is non-null; preserve nullable and
   unrestricted controls.
3. **#45 — fixed literals.** Emit every boolean/numeric/string literal as a constant/get-only
   public property and validate it on read through one shape-driven rule. No endpoint, schema,
   or property-name branches. This intentionally changes the public API baseline.
4. **#46 — additive field tolerance.** Make known-object unknown-field skipping explicit and
   tested; pure dictionaries remain typed, while named objects plus typed additional properties
   fail binding until lossless representation exists.
5. **#27 — current Inbox aliases.** Collapse the exact `Session.Inbox.UserPayload1` and
   `Session.Inbox.SyntheticPayload1` duplicates through validated `schemaAliases`; leave #27's
   remaining first-appearance naming/curation walls anchored at M5.
6. **#40 — numeric resume cursor.** `SessionLogRequest.After` becomes non-negative `long?`
   through generic reason-bearing parameter curation; zero is valid, negatives refuse before
   send, and SSE `id:` remains unrelated.
7. **#47 — complete StreamPlan.** Validate `id`, `event`, `encoding`, and failure-name collision;
   add bind → emit → compile coverage plus exact method/token/error-map/docs tests. Body-bearing
   streams refuse in binding until a runtime overload exists.
8. **#49 — evidence honesty.** Fix invalid `message-*` fixture IDs, assert converter/registry
   completeness mechanically, and prove plural union membership through emission, compilation,
   source-generated registration, and deserialization. Do not fabricate 40/87 payload corpora.

### B. Close Arc 3a's original remaining three

1. Type `x-effect-stream.causeSchema`; #53 owns the typed-applicator ingestion wall reached on
   the way in. The engine already refuses the failure frame; replace text-only cause handling
   with the typed contract.
2. Add the sandbox streaming example through the existing Generic Host DI showcase.
3. Add the end-to-end stream benchmark in `ClientOperationBenchmarks` style. Keep the existing
   reader baselines green and report `MemoryDiagnoser` numbers.

### C. Arc 3b — `v2.event.subscribe`

- Add generic reason-bearing operation-name curation and expose `Events.SubscribeAsync` (#44).
- Confirm both media-type and completed stream-profile walls report against the operation.
- Admit the 87-branch live union and prove the 39 shared durable leaves implement both sibling
  interfaces through mechanical tests.
- Demonstrate representative real live-bus frames; do not claim synthetic exhaustive runtime
  coverage.

### D. Standing arcs

Paginator (Arc 4), owned net472 transport plus #32 (Arc 5/#43), then the #23/#29/#33 performance
pass last (Arc 6). The ambient-location decision #37, #51/#52, and the remaining #27/#53 walls
close by M5. Broaden #30 on the next `EnvelopeEmitter` touch; #24 leftovers close by M6.

## Issue Map

| Area | Issues |
|---|---|
| Completed review fixes | #39 closed; #42 SSE arm landed, one-shot arm open |
| Immediate model/runtime | #40, #41, #45, #46, #48 |
| Stream generation/evidence | #44, #47, #49 |
| Owned transport / route | #43, #32 |
| Perf/converter pass | #23, #29, #33 |
| Tooling and full-surface walls | #24, #27, #51, #52, #53 |
| Diagnostics / location | #30, #37 |
| Repository assurance | #50 |

All confirmed generated-output symptoms point back to their binder/emitter/curation owner. Never
hand-edit generated files.

## Verification Contract

The six pre-commit gates are:

```bash
set -o pipefail && \
dotnet tool run slopwatch analyze --exclude ".scratchpad/**,external/**" --fail-on warning && \
dotnet build --configuration Release && \
dotnet format --verify-no-changes --no-restore && \
./tools/opencode-tool.cs --help && \
dotnet run --file tools/opencode-tool.cs -- generate --verify && \
dotnet test --configuration Release --no-build && \
printf 'ALL-GREEN\n'
```

Before each commit inspect `git status`, `git diff`, and recent log; stage only the increment.
Performance-touching work additionally carries before/after `MemoryDiagnoser` numbers. The six
interim allocation guards remain 25.92 KB, 26.67 KB, 2.08 KB, 17.37 KB, 321.54 KB, and
177.69 KB.

## Stop Conditions

- No endpoint/operation/schema-name branches in ingestion, binding, emission, or runtime
  machinery. Endpoint-specific API intent lives only in generic, reason-bearing, fail-closed
  curation data.
- Stop for a public API decision not already sealed above, a widened fail-closed wall, or any
  proposal to extend unchecked hand-authored fixtures.
- Union membership, error model, and streaming signature remain sealed by ADR-0011/0007/0008.
- No SDK hot-path optimization outside Arc 6; correctness changes still remeasure affected
  interim baselines.
- On-disk files beat stale indexes. Do not modify `external/`.

## First Task In The Next Session

Start with #41. Produce the real red using the selected `Session.Message.Shell.exit` contract,
then carry special-number semantics through the generic plan/emitter path. Verify finite numbers,
all three named values, arbitrary numeric-string refusal, optional wire-null refusal, AOT/source
generation, all six gates, and the unchanged interim baselines before committing.
