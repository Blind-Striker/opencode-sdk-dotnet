# M1 Arc B Handoff — Tasks 3b–4

Date: 2026-08-13

Consume this document against live Git and delete it when Arc B ships. It records unfinished
session state, not evergreen architecture.

## Onboarding Reading Order

1. `AGENTS.md` — locked decisions (ADR-0005 targets the v2 protocol surface only).
2. ADRs 0003/0004/0005/0007/0008/0009 — the binding design spine.
3. `docs/superpowers/plans/2026-08-13-m1-arc-b-callable-client.md` — the canonical execution
   plan. Tasks 0–2 are fully checked; Task 3 is in flight (route naming landed); everything
   unchecked is this handoff's work.
4. `docs/research/15-opencode-v2-platform.md` — the canonical v2 platform picture.
5. `docs/engineering/coding-style.md` and `docs/engineering/testing-style.md` — before writing
   any code.

## Git And Workspace State

| Item | State |
|---|---|
| Workspace | `/home/deniz/src/opencode-sdk-dotnet` |
| Branch | `feature/m1-callable-client` |
| HEAD | `aa7e351 feat(generator): plan route container and member names` |
| Ahead of `origin/master` | 13 commits, no remote branch, no pushes |
| Gates at HEAD | all six green (441 tests; net8/9/10 SDK legs, tools) |

The maintainer authorized implementation commits inside this development loop; pushing still
requires an explicit request. Run gate chains with `set -o pipefail` — a `| tail` pipe
otherwise swallows a red gate's exit code.

## Landed This Session (beyond the plan checkboxes)

- **Error maps derived from the pin:** health = 400 `InvalidRequestError`, 401
  `UnauthorizedError`; message adds 404 as inline `anyOf(SessionNotFoundError,
  MessageNotFoundError)`. All `_tag`-marked Effect errors; pinned binder tests assert the maps.
- **Mechanical envelope payload names (maintainer-sealed):** payload = operation subject
  (`Health`, `Message`); `envelopePayloadNames` is override-only for response-spine collisions
  (reserved: `Error`, `IsError`, `RawBody`, `Status`, and the response type name); a collision
  fails generation until an override lands. The required-per-operation validation is retired.
- **`OperationPlanBinder`** (`tools/.../Binding/`): consumed-only `ClientPlan` (Root ×1 +
  Collection + Handle roles), `OperationPlan`, `OperationParameterPlan`, `EnvelopePlan`,
  `ErrorMapPlan`; fail-closed walls (GET + JSON + required-string path params + one 200, no
  body/query/SSE/websocket/wildcard, non-2xx must be ≥400); `BindingErrorCategory.Operation`;
  name/response/client/route-member collision checks; template-ordered parameters; the
  synthetic same-shape gadget test proves zero operation/group/client-name branches.
- **Route naming on the plan:** `RouteContainerName` = clientName ?? Pascal(group);
  `RouteMemberName` = verb+subject with no group fold (`Health.Get`, `Sessions.GetMessage`).
  Maintainer approved the `OpenCodeRoutes` shape: nested static classes, parameterless ops as
  `const`, parameterized ops as `<Member>Template` const + escaped `<Member>(...)` builder.
- **Hand-written core (Task 2 complete):** spine (`ErrorBehavior`, options, `OpenCodeResponse`,
  exception spine), `IEnvironmentProvider`/`SystemEnvironmentProvider`,
  `EndpointPolicy.Normalize` (absolute HTTP(S), no query/fragment/userinfo, prefix preserved,
  returns base string without trailing slash), `UserAgentPolicy` (maintainer-sealed: strip
  `+metadata`; missing/unparsable → version token omitted, never assembly-version fallback,
  never a construction failure), `Pipeline` (per-request Basic `opencode` decoration, explicit
  password before one-time `OPENCODE_SERVER_PASSWORD` read at construction, single
  `SendAsync(ResponseHeadersRead)`, buffered body → synchronous `ResponseAdapter<TResponse>`,
  throw-vs-NoThrow after adaptation, `OpenCodeTransportException` wrap, OCE passthrough,
  owned-client disposal, `ObjectDisposedException` after dispose).
- **Multi-TFM traps solved once:** implicit usings lack `System.Net.Http` below net6 →
  conditional `<Using>` for net472/ns2.0 in the SDK csproj; `InternalsVisibleTo` +
  source-only Polyfill → `PolyUseEmbeddedAttribute=true` (research doc 14 updated);
  `DynamicProxyGenAssembly2` friend for NSubstitute over internal seams; S1694 arbitration on
  `ResponseAdapter` (abstract base will carry shared mapping helpers; DIM unavailable on
  net472/ns2.0).

## Remaining Work

### Task 3b — emitters (next)

Sequenced so gates stay green per commit:
1. **Commit E:** `RoutesEmitter`, `EnvelopeEmitter`, `ResponseAdapterEmitter`,
   `ClientEmitter`+`OperationMethodEmitter` (members returned, no partials), synthetic
   `EmitterPlanFixture` client plans, micro-snapshots, adapter-base shared helpers
   (`ReadBarePayload`/`ReadDataPayload`/`ReadTolerantError` on `ResponseAdapter<T>` —
   hand-written, tag-set param `IReadOnlyCollection<string>?`, null = undeclared status;
   FrozenSet is net8+, use `string[]`). Do NOT wire `SourceEmitter` yet — generate must not
   emit new files before the writer admits their roots.
2. **Commit F (Task 3c):** writer ownership extension (root files + `Internal/ResponseAdapters/`),
   unmanifested-overwrite refusal, provenance-header requirement, case-insensitive collisions.
3. **Commit G:** wire `SourceEmitter`, CA1034 arbitration for generated `OpenCodeRoutes.cs`,
   CS1591 gate, regenerate (committed surface appears), Linux CI `generate --verify` step.

Emitted shapes settled: envelopes = sealed records over `OpenCodeResponse`, backing-field
guarded payload getter, explicit public parameterless ctor (any explicit ctor kills the
implicit one), internal `[SetsRequiredMembers]` error ctor (`Status/IsError/Error/RawBody`),
`protected override bool PrintMembers` appending the payload only when present. Adapters =
`Internal/ResponseAdapters/`, sealed, private ctor + `Instance`, status-switch: 200 → payload
read through `OpenCodeJsonContext.Default.<Payload>`, else error ctor with per-status
`string[]` tag sets. Clients = unsealed, virtual members, protected mock ctor, guarded
internal `Pipeline` accessor throwing instructive `InvalidOperationException`, root owns
`Pipeline.Create` + `SystemEnvironmentProvider` + CA1063 Dispose pattern, handle holds
immutable applied parameter, method bodies = guard + one `Pipeline.ExecuteAsync` delegation
via `OpenCodeRoutes`. Aggregate compile probe must add the hand-written SDK sources (copy
`src/OpenCode.Sdk/**/*.cs` into test fixtures, exclude paths that collide with freshly emitted
sources) — the probe references only BCL assemblies and would otherwise miss the spine.

### Task 4 — contracts and M1 closure

Unchanged from the plan: recording-handler contract matrix, error-skew tests
(unknown tag / undeclared status / known tag at wrong status → `UnknownOpenCodeError`;
malformed non-2xx → null typed error + exact raw body), guarded payload/`ToString`, route
escaping, endpoint joining, auth resolution, User-Agent, cancellation, lifetime, mock seam;
`PublicApiGenerator` 11.5.4 + `Verify.TUnit` baseline review; pack fails only on
`.generation-incomplete`; real `opencode2 serve` demo from `.scratchpad/`; move ROADMAP to M2;
delete this handoff.

## Real-Server Demonstration

`opencode2` v0.0.0-next-17403 (`~/.nvm/versions/node/v24.18.0/bin/opencode2`). Verified:
`serve` prints endpoint + generated password; unauthenticated `/api/health` → 401; Basic
`opencode:<password>` → 200 `{healthy, version, pid}`; sessions come back in `{data}`.
Record the pin (`a6a712a`) vs installed-build skew as demo evidence.

## Final Gates

```text
dotnet run --file tools/opencode-tool.cs -- generate
dotnet run --file tools/opencode-tool.cs -- generate --verify
dotnet build --configuration Release
dotnet test --configuration Release --no-build
dotnet format --verify-no-changes --no-restore
dotnet tool run slopwatch analyze --exclude ".scratchpad/**,external/**" --fail-on warning
dotnet pack src/OpenCode.Sdk/OpenCode.Sdk.csproj --configuration Release
```

The first six must succeed; pack must fail only on the `.generation-incomplete` marker.

## Stop Conditions

Unchanged: Binder needing Microsoft.OpenApi/raw JSON; an emitter needing an operation ID,
wire group, or concrete client-name branch; selected shape beyond M1 capability; structural
union in the closure; ownership unable to distinguish hand-written files; temporary
public/transport API; a locked TFM failing to compile; partial generation packing; or
retry/telemetry/DI/launcher/SSE/breadth creeping in. Public-API taste decisions go to the
maintainer.
