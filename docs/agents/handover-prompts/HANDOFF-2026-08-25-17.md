# Session Handoff - emitter batch executed; breadth is next, closing benchmark deferred

Date: 2026-08-25

Consume this against live Git, the worktree, and GitHub before changing anything. This handoff
supersedes and deletes `HANDOFF-2026-08-25-16.md`. Repository files and current canon beat this
handoff. This session's decisions and evidence live in research Q140–Q143 and `docs/ROADMAP.md`;
the runtime arc's history is Q126–Q139 and ADR-0018.

## Start here

The repository is **public**. The worktree is clean; a series of local commits sits ahead of
`origin/master` — verify against live Git; push only on the maintainer's explicit ask. Two
blockers remain external, tracked, and not worked:

- **CI is billing-blocked** — every GitHub Actions job dies at startup on the spending-limit
  annotation; nothing since `9da0ae3` has a three-OS verdict, which now includes this session's
  whole series. Issue #50 owns the follow-up. Local gates carried every commit.
- **The spec refresh is blocked upstream** — the pin stays `a6a712a3`; Q139 owns the evidence,
  [anomalyco/opencode#44911](https://github.com/anomalyco/opencode/issues/44911) carries the
  report plus a verified restore recipe, and #56 is the local tracker.

## What Session 43 landed (2026-08-25)

- **Records tooling:** `opencode-tool compare-benchmarks <before> <after> [--output <csv>]`
  joins two `.benchmarks/` run folders' full JSON exports on (FullName, runtime leg) with exact
  allocation columns and an indicative median ratio; fail-closed on missing/duplicate/disjoint
  inputs; verified by reproducing Q138's 72-case arc-milestone comparison.
- **The emitter allocation batch, executed** (Q140–Q143; doc 20's ranked emitter rows closed):
  - **#12** shared static empty request per optional body type; measured in isolation at
    −56 B (net10.0) / −57 B (net472) on the new permanent `CreateSessionOmittedBodyAsync` row
    with both control rows flat.
  - **#8** all 15 generated tag tables freeze (`ToFrozenDictionary`, ordinal on string markers);
    `System.Collections.Immutable` 10.0.11 conditioned to net472/netstandard2.0 only
    (maintainer-sealed; named among the bridge examples in `platform-and-packaging.md`).
    Allocation-flat as expected; the timing verdict rides the deferred closing run.
  - **#3** `TryFindKnown` replaces `ReadString`: net9.0+ dispatches known tags from a stack copy
    through `GetAlternateLookup` — no marker string. Surgical evidence: −49,152 B on 1,024-frame
    stream rows, −168,040 B on large-message materialization, 0 B on every non-dispatch row;
    net472 unchanged (Polyfill's downlevel alternate lookup is an O(n) scan — known boundary).
  - **#10** route/query composition measured behind the permanent `RouteCompositionBenchmarks`
    rung: 184/824 B against 12.7 KB–314 KB materialization — honest negative, no rework.
  - Doc 20 **D2/D3 (#7/#9)** verified already landed inside the runtime arc.
- All batch increments ran the full local gate chain (2,186 tests, PublicApi and manifest
  membership unchanged). Comparison extracts live beside the runs in `.benchmarks/`
  (`empty-request-*`, `frozen-*`, `alternate-*`, `route-*`).

## Exact next

1. **Batch-closing default-job benchmark** (deferred by the maintainer — wants an idle machine):
   full suite, default job, both runtimes, from a clean mirror, compared against
   `arc-milestone-default` with `compare-benchmarks`. It carries the frozen-table timing verdict
   and the batch's cumulative totals; record the outcome as the next research Q.
2. **M5 breadth batches pulled ahead of M4**, grown against the current pin and filtered by the
   Q137/Q139 drift map — never select the upstream-removed families (question flow,
   `projectCopy.*`, `health.stop`, `project.directories`). Before the first batch, take the
   **generator binding locality decision** (doc 18 §8's drift-check and cost model; ROADMAP open
   question). The **M4 launcher** rides alongside as a small independent arc (1–2 page plan,
   approval before source).
3. **Prerelease packaging track**: decision-first amendment of the partial-operation packing wall
   in `Directory.Build.targets` (prerelease packs allowed, stable packs stay blocked; rides
   ADR-0006/#51), then the ADR-0006 pipeline — per-merge GitHub Packages CD, manual NuGet.org
   lane. CI legs validate once billing restores.

## Onboarding order

1. `AGENTS.md`, `CONTEXT.md`, `docs/ROADMAP.md`, this handoff.
2. Research Q137–Q143 (drift map, upstream regression, this batch); ADR-0018; ADR-0006 for the
   packaging track; doc 18 for the binding-locality decision.
3. `docs/engineering/` canon (quality-gates owns the benchmark cadence) and `docs/architecture/`.
4. For breadth batches: `docs/architecture/protocol-and-generation.md`, `tools/curation.json`,
   `tools/generation-profile.txt`, and the emitters under
   `tools/OpenCode.Sdk.Tools/Generator/Emission/`.

## Completion gates

Unchanged from `docs/engineering/quality-gates.md` (slopwatch, Release build, format
whitespace/style, tests; generator changes add the tool smoke and `generate --verify`).
Repo policy: no AI trailers in commit messages or PR bodies. Direct green commits stay
authorized in the development loop; never push without an explicit ask; canonical-document
edits only with explicit maintainer approval.

## Ready-to-paste priming prompt

```text
Start a session for opencode-sdk-dotnet from
docs/agents/handover-prompts/HANDOFF-2026-08-25-17.md. Consume it against live Git, the
worktree, and GitHub before changing anything.

The repository is public. The emitter allocation batch is executed (research Q140-Q143); CI is
billing-blocked (#50) and the spec refresh is blocked upstream (#56 / anomalyco/opencode#44911)
- both external, tracked, and not to be worked. Work the queue: (1) the batch-closing
default-job benchmark against arc-milestone-default when the maintainer clears an idle window;
(2) M5 breadth batches ahead of M4, filtered by the Q137/Q139 drift map, with the doc 18
binding-locality decision taken before the first batch and the M4 launcher as a small side arc
(plan first, approval before source); (3) the prerelease packaging track, decision-first on the
partial-operation pack wall. Repo policy: no AI trailers in commits or PR bodies. Direct green
commits stay authorized in the development loop; never push without an explicit ask;
canonical-document edits only with explicit maintainer approval.
```
