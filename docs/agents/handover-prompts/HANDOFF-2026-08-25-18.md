# Session Handoff - the A-series is complete; the mechanism batches open behind B1

Date: 2026-08-25

Consume this against live Git, the worktree, and GitHub before changing anything. This handoff
supersedes and deletes `HANDOFF-2026-08-25-17.md`. Repository files and current canon beat this
handoff. This session's evidence lives in research Q144–Q145, ADR-0019, and `docs/ROADMAP.md`.

## Start here

The repository is **public**. The worktree is clean and `master` is pushed — verify against
live Git. **CI is restored**: billing refilled mid-session, `9da0ae3` went green on rerun, and
every push since has a hosted three-OS verdict (verify the tip's run). Two external items:

- **#50** stays open only for branch protection and required checks — a maintainer decision,
  not blocked work.
- **The spec refresh stays blocked upstream** — the pin stays `a6a712a3`; Q139 owns the
  evidence, [anomalyco/opencode#44911](https://github.com/anomalyco/opencode/issues/44911)
  carries the report, #56 is the local tracker.

## What Session 44 landed (2026-08-25)

- **The Q144 wall-probe** mapped the full pinned surface: 99 workable pending operations, 52
  routine, 47 partitioned across the bodyless-POST, inline-promotion, envelope-shape, PUT, and
  query walls plus three singleton decisions. The doc 18 gate was decided on it: **B2's
  `ReservedNamePolicy` landed** (`1292726`, with reflection coverage tests), **B1's facet
  binders land before the first mechanism batch**, B3/B4 behind B1.
- **The A-series is complete** — all 52 routine operations admitted in four family batches
  (`faef68b`, `31b3d11`, `6bb5813`, `f573bfb`); the profile stands at **67 selected / 53
  pending**, 2,269 local tests, every PublicApi review additive-only. Q145 owns the details.
- **ADR-0019 accepted**: handle clients only for working objects, judged against the complete
  pinned surface; Agents/Credentials/Permissions/Providers are flat, the handle families stand,
  and the ADR is anchored to the Azure SDK .NET guidelines. **Group curation rows now require a
  reason** (validator wall + red test).
- **Live evidence**: the committed sandbox's session-actions walkthrough ran against the pinned
  server (fork's request union accepted on the wire, NoThrow carrying a typed 404), and the
  permission ask → get → reply lifecycle was closed by curl with an `OPENCODE_CONFIG_CONTENT`
  probe agent (Q145; the deny path is an agentless session's deny-all fallback, not a defect).
- Structural catches feeding the parked folder-layout review: the `Models` family name was
  refused by the writer's shadow wall (the family is `LanguageModels`), and the stock
  `[Dd]ebug/` gitignore pattern swallowed the Debug family until a narrow negation (`ccd2a9f`).

## Exact next

1. **B1 — the facet-binder refactor** (sealed to precede the mechanism batches; doc 18 §7 B1
   owns the sketch: facet binders over a shared `OperationFacetContext`, spine-shape
   recognizers into a `SpineShapePolicy`). Behavior-preserving; `generate --verify` must stay
   byte-identical.
2. **B-1 mechanism batch — bodyless POST + PUT** (15 operations: twelve bodyless POSTs
   including `mcp.connect`/`disconnect` and the session action/flow ops, three PUTs including
   `mcp.add`). Then, each design-first: the inline-promotion batch (config/integration/form
   families), the envelope-shape extensions, and the query walls. Q144 owns the exact
   partition.
3. **The batch-closing default-job benchmark** stays deferred until the maintainer clears an
   idle machine window: full suite, default job, both runtimes, clean mirror, against
   `arc-milestone-default` via `compare-benchmarks`; it still carries the frozen-table timing
   verdict. Record as the next research Q.
4. **The prerelease packaging track** (decision-first on the partial-operation pack wall;
   ADR-0006/#51) — CI legs can now validate on the restored matrix.
5. Parked for the M5 freeze-time surface review (ROADMAP Open Questions): parent-mediated id
   access for flat families, the handle-id property (`dotnet-subclient-properties`), and the
   maintainer's SDK folder-layout review.

## Onboarding order

1. `AGENTS.md`, `CONTEXT.md`, `docs/ROADMAP.md`, this handoff.
2. ADR-0019; research Q144–Q145 (probe partition, A-series, surface decisions); doc 18 §7 for
   B1's sketch; Q137/Q139 for the drift map.
3. `docs/engineering/` canon and `docs/architecture/protocol-and-generation.md`;
   `tools/curation.json` (every group row now reasons its placement).
4. For the mechanism batches: the binder under `tools/OpenCode.Sdk.Tools/Generator/Binding/`
   and the emitters under `Generator/Emission/`.

## Completion gates

Unchanged from `docs/engineering/quality-gates.md` (slopwatch, Release build, format
whitespace/style, tests; generator changes add the tool smoke and `generate --verify`).
Repo policy: no AI trailers in commit messages or PR bodies. Direct green commits stay
authorized in the development loop; never push without an explicit ask; canonical-document
edits only with explicit maintainer approval.

## Ready-to-paste priming prompt

```text
Start a session for opencode-sdk-dotnet from
docs/agents/handover-prompts/HANDOFF-2026-08-25-18.md. Consume it against live Git, the
worktree, and GitHub before changing anything.

The repository is public and CI is restored (three-OS verdicts on every push; #50 stays open
only for branch protection). The A-series is complete (research Q144-Q145; profile 67/53) and
ADR-0019 owns handle placement. Work the queue: (1) B1 - the facet-binder refactor from doc 18
S7, behavior-preserving, before any mechanism batch; (2) the B-1 mechanism batch - bodyless
POST + PUT (15 operations); then inline promotion, envelope extensions, and query walls, each
design-first; (3) the deferred batch-closing default-job benchmark when the maintainer clears
an idle window; (4) the prerelease packaging track, decision-first. The spec refresh stays
blocked upstream (#56). Repo policy: no AI trailers in commits or PR bodies. Direct green
commits stay authorized in the development loop; never push without an explicit ask;
canonical-document edits only with explicit maintainer approval.
```
