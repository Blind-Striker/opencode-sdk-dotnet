# Session Handoff - design and research complete; execute the runtime increments

Date: 2026-08-24

Consume this against live Git, the worktree, and GitHub before changing anything. This aggregated
handoff supersedes and deletes `HANDOFF-2026-08-24-13.md` and preserves the full Session 40 arc so
the next session onboards without archaeology. Repository files and current canon beat this
handoff. Decisions live in research log Session 40 (Q126–Q129); contracts, increments, gates, and
test migration live in `docs/superpowers/plans/2026-08-24-runtime-pipeline-plan.md` (the operative
document); research evidence lives in docs 17–20. This file owns only operational continuity.

## Start here - do not clean the worktree yet

The checkout still contains the uncommitted R16 experiment from Session 39. It is measured
evidence whose fate is now decided (mechanism accepted, code re-derived later): Increment 0
archives it as a patch and cleans the worktree. Until then do not reset, stash, commit, or build
on it.

On entry:

```powershell
git status --short --branch
git log --oneline --decorate -12
git fetch origin
gh run view 32411438829
```

Do not pull automatically: local `master` is eleven commits ahead of `origin/master` (after this
handoff commit) and nothing is pushed. Reconcile explicitly if origin moved.

## Live baseline

| Item | State |
|---|---|
| Remote | `origin/master` = `0e1735e`; CI run `32411438829` green on Linux/Windows/macOS at that tip |
| Local unpushed commits | `3f68ddf` response/union repair · `713f09a` net472 cancellation repair · `d89f812` Q123 evidence · `02dbb70` R18 SSE span scan · `1222664` HANDOFF-12 (since deleted) · `05cd5d7` design seal (Session 40, plan + HANDOFF-13) · `1e003af` Track B evidence (doc 18) · `0833041` allocation API survey (doc 17) · `8219343` feature catalog + idiom audit (docs 19/20) · `b70f219` research fold-in to the plan · plus this handoff commit |
| Source gate | Last source-affecting commit is `02dbb70`, fully gated in Session 39 (Slopwatch 0; Release build clean; whitespace/style clean; 1,832 tests green across the Windows matrix). Every commit after it is Markdown-only |
| Dirty worktree | Exactly the R16 set — modified: `src/OpenCode.Sdk/Internal/{EncodedResponseBody,Pipeline,ResponseBodyReader,ResponseEncodingPolicy}.cs`, `tests/OpenCode.Sdk.Performance.Tests/Benchmarks/ResponseEncodingPolicyBenchmarks.cs`, `tests/OpenCode.Sdk.Tests/ResponseEncodingPolicyTests.cs`; untracked: `PooledResponseBodyBuffer.cs`, `PooledResponseBodyStream.cs`, `PooledResponseBodyStreamTests.cs`, `ResponseBodyReaderTests.cs`, `Support/TrackingByteArrayPool.cs` |
| Generation profile | 15 selected / 105 pending; PublicApi and manifest unchanged since `02dbb70` |
| Push policy | Direct green commits authorized inside the agreed development loop; never push without an explicit ask; no AI-attribution trailers |
| Open items | #53 wall inventory stays M5; docs leftovers R04/R05/R06/R11 (below); M4 has no plan or source |

## What Session 40 did (2026-08-24, aggregated)

1. **Onboarded from HANDOFF-12** and verified the pause state against live Git/CI.
2. **Three read-only architecture scans**: runtime depth scan (committed `Pipeline` = 520 lines,
   ~13 policies in two orchestration methods, five missing seams, four failure-classification
   cascades, the internal ctor as the Behavior core's only test seam), generator tool scan
   (healthy four-stage skeleton; the mass in Binding — `OperationPlanBinder` 1,319 lines /
   24 responsibilities), and Azure.Core + AWS SDK runtime pipelines read at source, including a
   charset follow-up proving neither replicates `HttpContent` charset negotiation. Candidates
   were presented as a temp HTML review (outside the repo, disposable) and worked through a
   grilling session. Evidence: Q126, doc 18.
3. **Design sealed by the maintainer** (Q127/Q128): Azure-style **internal policy pipeline** —
   `PipelineMessage` / abstract `PipelinePolicy` (slice-passing, async-only, `ValueTask` hops) /
   day-one roster `RequestDecorationPolicy → ResponseBufferingPolicy → TransportPolicy`; the
   composed class keeps the `Pipeline` name and generated-facing entry points. Post-pipeline
   `ResponseMaterializer` (closes R12). **A3-Full**: generated `StatusVerdict Classify(int)` is
   the single status authority (3xx stays transport's protocol-invariant refusal;
   `SuccessStatusCode`/`ReadsSuccessBody` die). **A5** `FailureClassification.Map(exception,
   token, phase)`. **Framer**: named seam `IEventStreamFramer` + stateless
   `ServerSentEventFramer`; `FrameDispatch` moves beside `IStreamAdapter`. **Timeout**:
   progress semantics (100 s internal `NetworkTimeout`), Azure machinery (linked CTS +
   `CancelAfter` re-arm + dispose-to-interrupt), owned `HttpClient.Timeout` → infinite, live
   SSE success bodies exempt, error bodies always protected. **Encoding**: full
   `HttpContent.ReadAsStringAsync` parity KEPT on both planes (ADR-0014 untouched) but rebuilt
   low-allocation/exception-free with a differential parity matrix (closes R09/R10). **R16**:
   mechanism accepted, dirty code will be re-derived inside `ResponseBufferingPolicy`.
   Standing principles sealed: knowledge-source provenance tags on policy modules
   (pin-derived / BCL-derived / upstream-observed), TFM rule (algorithm divergence → per-TFM
   adapter; API shape → `#if`), and "a seam gets a name, never a delegate parameter".
4. **Track B recorded** (doc 18): the generator-binding locality evidence and the M5-gate
   reassessment method (§8: drift check, cost model, per-candidate rules). Decision point is
   before the first M5 breadth batch; B2 (reserved-name owner) may ride any earlier
   generator-touching increment opportunistically.
5. **Research complete** (maintainer's standing decision — no walking back): doc 17
   (increment-gated allocation API survey; verified against restored package artifacts), doc 19
   (breadth catalog, ~45 entries, incl. the §0 IL-probe answering `params ReadOnlySpan`
   downlevel codegen by experiment; `u8` literals zero-alloc on all five TFMs), doc 20 (runtime
   idiom audit; verdict: upper tier, classic anti-patterns essentially absent, the misses map
   onto the planned increments). Adoption items are folded into the plan at `b70f219`.

## Exact next - execute the plan, increment by increment

The plan document owns the details; the order is:

1. **Increment 0** — archive the complete R16 dirty diff plus untracked files as
   `C:\bench-artifacts\r16-experiment-2026-08-24.patch`, then restore a clean worktree.
2. **Increment 1** — `FailureClassification` extraction (smallest green step).
3. **Increment 2** — pipeline skeleton, behavior-preserving, zero generated diff; includes the
   doc 20 D2/D3 fold-ins and the framer/dispatch moves; benchmark against the Increment 1
   baseline.
4. **Increment 2b** — A3-Full verdicts (generator increment; generator gates; the drained-204
   canon wording touch; E5 fold-in).
5. **Increment 3** — entry checkpoint first (four package decisions: `Microsoft.Bcl.Memory`,
   `Microsoft.Bcl.TimeProvider` vs internal clock seam, `System.Collections.Immutable`,
   `System.Net.ServerSentEvents`; seal the explicit-vs-transitive dependency rule into
   `platform-and-packaging.md` with per-edit maintainer OK; explicit downlevel `System.Memory`/
   `System.Buffers` declarations) — then pooled buffering + progress timeout, red-first, with
   the `client-runtime.md` timeout canon edit inside the same increment.
6. **Increment 4** — encoding hardening with the differential parity matrix.
7. After the increments: the **emitter allocation batch** (widened typed-switch item; backlog =
   doc 20's ranked table), then the **M4 launcher plan** (1–2 pages, approval before source).

Every increment runs the full completion gate; perf increments add same-environment
before/after benchmarks per `quality-gates.md` (clean copy outside the repo, Dry first,
allocation primary, net472 numbers only from the Windows leg). The internal-policy-pipeline
decision is an ADR candidate to seal alongside Increment 2's commit — maintainer decides then.

## Onboarding order for the next session

1. `AGENTS.md`, `CONTEXT.md`, `docs/ROADMAP.md`, this handoff.
2. `docs/superpowers/plans/2026-08-24-runtime-pipeline-plan.md` — the operative document.
3. `docs/engineering/` canon (coding-style, testing-style, quality-gates, documentation,
   workflow) and `docs/architecture/client-runtime.md` + `protocol-and-generation.md`.
4. ADR-0001, 0007, 0009, 0010, 0014, 0015, 0017.
5. Research log Session 40 (Q126–Q129) and docs 17/18/19/20; Sessions 38–39 (Q121–Q125) for
   the performance-evidence history behind the design.
6. Committed runtime source: `Pipeline.cs`, `ResponseBodyReader.cs`, `ResponseEncodingPolicy.cs`,
   `EncodedResponseBody.cs` (via `git show HEAD:` while the worktree is dirty),
   `ServerSentEventReader.cs`, `ResponseAdapter.cs`, `IStreamAdapter.cs`,
   `OpenCodeErrorReader.cs`, `Internal/Pagination/`.
7. Tests named by the plan's migration map: `PipelineTests`, `PipelineStreamTests`,
   `PipelineResponseOwnershipTests`, `OwnedTransportTests`, `ServerSentEventReaderTests`,
   `ResponseEncodingPolicyTests`.
8. Only then the dirty R16 diff — as evidence for Increment 3's re-derivation, kept mentally
   separate from the committed design. After Increment 0 it lives only in the archived patch.

## Preserved review leftovers (lossless carry; none block the increments)

Docs items from the Session 38/39 register, resolved in a documentation pass when convenient:
R04 (ADR-0014 still calls the immutable-collection comparison open although Q120 closed it),
R05 (ROADMAP's append-only history relocation), R06 (decoding-rule single-owner cleanup across
canon), R11 (ADR-0009 `Date:` move and duplicated mechanism statement), plus SSE wording nits.

## Evidence locations

Permanent evidence: research Q121–Q129 and docs 17–20. Temporary benchmark artifacts:
`C:\bench-artifacts\q123-*`, `r18-*`, `r16-after-*`; clean copies `C:\bench`, `C:\bench-r18`,
`C:\bench-r16`; Session 38 originals on dev-01 `~/opencode-session38-artifacts/`. The R16 patch
joins them at Increment 0. Never reference these paths from product files.

## Completion gates

```powershell
dotnet tool run slopwatch analyze --exclude ".scratchpad/**,external/**" --fail-on warning
dotnet build --configuration Release
dotnet format whitespace --verify-no-changes --no-restore
dotnet format style --verify-no-changes --no-restore --severity warn
dotnet test --configuration Release --no-build
```

Generator/curation/output changes (Increment 2b) also run the tool-entry smoke and
`dotnet run --file tools/opencode-tool.cs -- generate --verify`. Inspect status, every changed
file, the complete diff, PublicApi, and the manifest before every commit. Push only on explicit
ask.

## Ready-to-paste priming prompt

```text
Start an execution session for opencode-sdk-dotnet from
docs/agents/handover-prompts/HANDOFF-2026-08-24-14.md. Consume it against live Git, the dirty
worktree, and GitHub before changing anything.

The runtime-pipeline design and its research are complete and maintainer-sealed (research log
Session 40 Q126-Q129; plan: docs/superpowers/plans/2026-08-24-runtime-pipeline-plan.md; evidence:
research docs 17-20). local master is ahead of origin with nothing pushed; the worktree carries
the uncommitted R16 experiment whose fate is decided.

The session's task is implementation, in the plan's order: Increment 0 (archive the R16 patch to
C:\bench-artifacts\r16-experiment-2026-08-24.patch, clean the worktree), then Increment 1
(FailureClassification), then onward. Every increment lands independently green through the full
quality gate; benchmark evidence per quality-gates.md where perf-relevant. Direct green commits
are authorized in the development loop; never push without an explicit ask; canonical-document
edits only at the points the plan schedules, each with explicit maintainer approval.
```
