# Session Handoff — both arcs are code-complete on master; the closing legs and the push are queued

Date: 2026-08-28 (replaces HANDOFF-2026-08-27 in place)

Consume this against live Git, the worktree list, and the two SDD ledgers before changing
anything. Repository files and current canon beat this handoff. Evidence: research log Q152
(the third refresh), Q153 (the M4 arc); the C2 arc's Q154 is NOT yet written — it is queued
work below.

## Start here

- **Master is at `82d6f89`, ~33 commits ahead of `origin/master` (ed41096), UNPUSHED** — pushing
  is an explicit maintainer decision queued below. The accepted snapshot is **`d2ee536c`**
  (Q152). **Profile: 98 selected / 36 pending** of 134 operations; plus the hand-written
  `PtySession` WS door.
- The full local gate is green at `82d6f89` **except 2 known residual SW004 warnings**
  (`DrivePortGate.cs`, `SimulatedSessionWorkflowTests.cs`) awaiting the maintainer's
  adjudication (below). 3,282 tests pass on all TFMs; `generate --verify` current;
  `refresh-spec --verify` reproduces the receipt.
- **The SDD ledgers are the authoritative recovery maps** — read them before dispatching
  anything: `.superpowers/sdd/2026-08-28-envelope-completion/progress.md` (C2) and
  `.superpowers/sdd/2026-08-28-m4-launcher-and-fixture/progress.md` (M4). They carry every
  ruling, every deferred minor, and every fix-round record. Do not delete these workspaces —
  the arcs are not closed.
- The M4 worktree (`.claude/worktrees/agent-a4ed77880ebed9460`) is merged into master and can
  be removed after the C2 arc closes (its task reports are already copied into the main
  checkout's SDD workspace).

## What landed today (aggregated)

1. **The third receipt-governed refresh** (`803ead32` → `d2ee536c`, Q152): upstream force-pushed
   v2; +1 op (`persistentPty.handoff`), `Session.Metadata`, `session.import` 404; two curation
   walls caught and fed (a ninth T3 identity row; a `Running_1` stabilize alias).
2. **C2 — envelope completion (plan: `docs/superpowers/plans/2026-08-28-envelope-completion.md`),
   Tasks 1–7 complete with independent reviews**: `EnvelopePlan` carries a `TypeReferencePlan`;
   container payloads (list/dictionary) bind through `TypePlanBinder`; bare containers register
   with pinned `TypeInfoPropertyName` accessors; inline payloads promote under operation-scoped
   names (`{stem}Data`, uniform — the grandfathering was maintainer-overruled and reversed:
   `GenerateTextPostData`/`SessionGeneratePostData` renames landed); Marked-union payload roots
   get the `I` prefix; represented-nullable payloads use the `IsError` response-state guard
   (`required T?` DTO wall pinned both directions); DataLocationList inline items promote;
   location-envelope data refs resolve to array components (nullable fall-through pinned).
   **18 operations selected** across server/workspace/worktree (new families)/vcs (status +
   branches)/location/session×6/debug/integration/model/permission/project. The
   `WorktreeClient`→`ProjectWorktreesClient` rename landed (handle names its key).
3. **M4 — launcher arc (plan: `docs/superpowers/plans/2026-08-28-m4-launcher-and-fixture.md`),
   all 6 tasks + integration + final whole-branch review (Ready to merge)**: `OpenCodeServer.
   StartAsync` (standalone door, ADR-0001) with bounded-everything teardown; real-process
   lifecycle acceptance ×4 TFMs; CI gained submodule checkout + bun 1.3.14 + lockfile-keyed
   caches (UNPROVEN on hosted runners until the push); `PinnedOpenCodeServerFixture` +
   `CliWrapServerAdapter`; `DriveController` (JSON-RPC simulation client); the ADR-0022
   day-one-blocking simulated-session workflow test — **its config-seeded-provider checkpoint
   passed live** (`llm.request` with the seeded model id, now asserted in the gate); the
   `--standalone` sandbox demo ran live. Canon door taxonomy + CONTEXT.md vocabulary landed.
   ROADMAP closure + Q153 recorded (`082ac8f`).
4. **The slopwatch incident (important history)**: slopwatch is **worktree-blind** (silently
   scans nothing in a git worktree), so the M4 arc's slopwatch legs were vacuous; 31 real
   findings surfaced on master. A first fix wave that mass-suppressed was **REJECTED by the
   maintainer, killed, and reverted uncommitted**. The proper wave landed (`82d6f89`): SW003
   matches the catch BODY, not the type — 24 sites converted to outcome-bearing `Try…` shapes
   (PtySession precedent), 3 delays synchronized, `DriveController.DisposeAsync` now awaits its
   receive loop. Zero suppressions. Its scoped re-review by the M4 final reviewer was IN FLIGHT
   at handoff time — check the ledger tail for the verdict before building on those files.
5. **Compat audit**: zero backward-compat-preservation instances beyond the one reversed;
   Q131's "pre-GA owes no compatibility" is standing canon. **No pushes, no suppressions, no
   pragmas landed today.**

## The queue (next session, in order)

1. **Adjudicate the 2 residual SW004s (maintainer decision)** — both are bounded polls with
   provably no signal to wait on (OS file lock: no wait-for-lock API; drive protocol: exactly
   one notification). Options: suppress-with-reason (the `TricklingContent.cs:76` precedent —
   "the delay IS the mechanism" class) / leave the gate red / redesign. The maintainer's
   standing overrule: NO suppression without their explicit approval.
2. **C2 7d — the live sandbox leg**: extend the committed sandbox walkthrough with 3–5
   representative new ops (`Vcs.GetBranchesAsync` ref-to-array + Location sibling,
   `Sessions.GetActiveAsync` dictionary payload, one promoted-inline op, one session-scoped
   read) and run live against a pinned-built server (bun from `external/opencode/packages/cli`,
   `OPENCODE_SERVER_PASSWORD`, sandbox needs `--no-launch-profile`). The killed dispatch's full
   text is in the session record; the C2 ledger has the scope.
3. **C2 T8 — arc closure**: canon paragraph in `protocol-and-generation.md` (draft below,
   maintainer approves wording), research log **Q154**, ROADMAP status paragraph, the C2 final
   whole-branch review (most capable model; point it at the ledger's deferred-minors list for
   triage — includes: LanguageModelsClient.ListModelsAsync has NO contract tests;
   IntegrationClient factory-guard untested; the vcs fallback re-walk; 7a's vcs.status
   query/protocol-failure test gaps; the formatter own-commit convention), one fix wave,
   plan retirement.
4. **The maintainer package: canon wordings + PUSH** — present together: (a) the C2 canon
   paragraph draft below; (b) M4's two canonical riders (a one-level heading lift for
   "Connection modes" out from under §Launcher in `client-runtime.md`; a CA1502
   top-level-statement note for `quality-gates.md`); (c) the push itself → first three-OS
   hosted run with the new bun legs — budget a hosted-CI fix round (FileShare.None on Unix,
   bun on runners are the named risks).
5. **Riders (approved, sequenced)**: the no-wall sweep batch (14 ops) + the bindability
   telltale in `.generation-incomplete`; the benchmark coverage batch (PtySession read-path
   ladder, location-merge case, dictionary-envelope rung, compare-benchmarks CSV completeness
   fix) then the full default-job run vs `arc-milestone-default` from a clean copy.
6. **Release track (decision-first)**: packing-wall amendment (prerelease packs allowed, stable
   blocked), pre-1.0 versioning/VersionPrefix/RELEASE_NOTES decisions, GitHub Packages CD +
   manual NuGet.org lane (ADR-0006).
7. **Service-parity arc intake** (queued named work): `OpenCodeService.Discover/Ensure/Stop`;
   fixture retention/disposal consolidation; `OpenCodeServer` post-dispose guards; three cheap
   contract tests (composer backslash-before-quote, negative grace timeout, non-object
   readiness root); `DriveController.Dispatch` malformed-notification hardening; the
   `global.tmp` TMPDIR note.

## Canon draft for T8 (maintainer approves wording before it lands)

> An envelope payload is accepted when its ingested schema binds to a supported type plan:
> named models, lists, dictionaries, inline objects promoted under deterministic
> operation-scoped names (reasoned naming rows override), and represented-nullable payloads —
> distinguished from the error path by response state, never by treating CLR null as an unset
> backing field. Location-envelope data may reference an array component; the cursor-list item
> stays nominal (ADR-0017). Unsupported nodes fail closed, and no family-specific shape
> exception exists.

## Standing facts and gotchas

- **Slopwatch is worktree-blind** — worktree gates must run slopwatch from a normal checkout or
  declare the gap. SW003 matches the catch **body**, not the catch type.
- **Agent worktrees fork from `origin/master`, not local HEAD** — with today's work unpushed,
  any new worktree dispatch must merge local master first or it builds on the stale pre-refresh
  tree (this bit the M4 T1 PublicApi baseline).
- **Bun's workspace/JSX discovery is CWD-keyed**: the pinned server must run with cwd at
  `external/opencode/packages/cli` (fixture, README recipe, and Q153 all record it).
- **Implementer agents must run long commands in the FOREGROUND** with generous timeouts —
  several yielded passively on backgrounded `generate`/test runs and needed nudges.
- LSP diagnostics were stale/noisy all session — the gate (build+tests) is the referee, never
  the editor squiggles.
- No backward compatibility until release (Q131, reaffirmed by maintainer): renames are free;
  reasons in curation must state true facts (a false collision citation was a review Important).
- Naming traps: location query member `workspace` vs body `workspaceID`; the emitted vcs method
  is `GetBranchesAsync` (HTTP-verb fallback), not ListBranchesAsync.
- Sandbox from CLI needs `--no-launch-profile`; server smokes:
  `OPENCODE_SERVER_PASSWORD=<pw> bun src/index.ts serve --hostname 127.0.0.1 --port <p>` from
  `external/opencode/packages/cli`.
- `session.idle` is deprecated upstream with no publisher while `SessionIdle` remains in the
  event union — upstream-report candidate (Q153); parked with the T2/T3/T6/T7 reports.

## Completion gates

Unchanged (`docs/engineering/quality-gates.md`): slopwatch, Release build, format
whitespace/style, tests; generator changes add the tool smoke and `generate --verify`; snapshot
changes add `refresh-spec --verify`. Repo policy: no AI trailers; direct green commits stay
authorized in the development loop; **never push without an explicit ask**; canonical-document
edits only with explicit maintainer approval.

## Ready-to-paste priming prompt

```text
Start a session for opencode-sdk-dotnet from
docs/agents/handover-prompts/HANDOFF-2026-08-28.md. Consume it against live Git and the two
SDD ledgers (.superpowers/sdd/*/progress.md) before changing anything.

State: master at 82d6f89 (~33 commits, UNPUSHED), pin d2ee536c, profile 98/36. Both arcs are
code-complete with per-task reviews; C2 lacks its closing legs (7d live sandbox, T8 canon +
Q154 + final whole-branch review); M4 lacks only the three-OS hosted proof. Two residual SW004
warnings await my adjudication (no suppression without my explicit approval).

First: give me the maintainer package — the SW004 adjudication options, the C2 canon draft,
M4's two canonical rider wordings, and the push decision. Then run 7d and T8 per the ledgers
(subagent-driven, foreground commands only, task reviews as today). Repo policy: no AI
trailers; never push without my explicit ask.
```
