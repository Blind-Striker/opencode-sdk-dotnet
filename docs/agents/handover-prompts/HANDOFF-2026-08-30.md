# Session Handoff — persistent PTY landed and proven; next: hygiene, then the last twelve

Date: 2026-08-30 (replaces HANDOFF-2026-08-29.md in place)

Consume this against live Git, `src/OpenCode.Sdk/.generation-incomplete`, and `docs/ROADMAP.md`
before changing anything. Repository files and current canon beat this handoff.

## Start here

- **`master` is at `56c43ca`:** `2801dd4` (the merged arc), `5834854` (this closing documentation),
  `4324f3e` (the net472 scheduling fix, verified green in run `33288967119`), and `56c43ca` (its
  record). Check `git status` for what is pushed — pushing is the maintainer's call. The branch
  `feature/persistent-pty-family` was merged fast-forward (`203595c..2801dd4`, 17 commits) and
  deleted. The accepted snapshot is **`b1e3a7b2`** after this session's opening document-identical
  refresh (`106629aa` → `b1e3a7b2`: 8 upstream commits, none touching `packages/protocol` or the
  watched PTY-door inputs; the SSE Restore patch still applies, PR #45182 unmerged; full gate green
  against the server built from the new pin).
- **Profile: 122 selected / 12 pending / 2 transport-owned** of 136 operations, plus the two
  hand-written PTY WebSocket doors (`PtySession`, `PersistentPtySession`).
- **The full local gate is green at `4324f3e` and slopwatch reports ZERO findings.** 4,016 tests
  pass on all TFMs, the Release build has zero warnings, `dotnet format` is clean, and
  `generate --verify` is current.
- **The persistent PTY round trip is proven.** It executed on hosted Linux, hosted macOS, and a
  WSL2 server from this workstation — see the proof findings below. Nothing in this family is
  unexecuted any more.
- **The hosted Windows net472 red of run `33276305571` is diagnosed and fixed.** Linux ✅ (3,162
  tests), macOS ✅, Windows ❌ there: attempt 1 failed
  `PipelineTests.ExecuteAsync_Should_Fail_A_Body_That_Stalls_Past_The_Progress_Window`, the re-run
  failed
  `PinnedOpenCodeServerFixtureExternalModeTests.Fixture_Should_Attach_To_An_External_Endpoint_When_One_Is_Supplied`
  instead — both net472 only, both green locally. Neither budget was the problem: the run's
  per-test timings show in-process tests stalling 100–1,600× while three or four real server
  processes started and died in parallel (research log Q157). The fix serializes every
  server-process test under `ParallelConstraintKeys.ServerProcess` and runs the timing-bounded
  tests keyless `[NotInParallel]`, alone at the end. Q157 records the hosted run that verified it.
- **`.generation-incomplete` is still the map of what is left.** Every pending operation carries a
  `[bindable]` or `[refused: <verbatim wall message>]` mark produced by the real binding pipeline.
  Read it first — it is more current than any prose, including this file.

## What landed since the previous handoff

The persistent PTY family arc, eight tasks with an independent review each, one final whole-branch
review, and a five-commit fix wave (plan:
`docs/superpowers/plans/2026-08-29-persistent-pty-family.md`; briefs, reports, and the ruling
ledger: `.superpowers/sdd/2026-08-29-persistent-pty-family/`; the durable account: research log
Q156 and `docs/ROADMAP.md` §Status). Everything before this arc is recorded there and is not
repeated here.

1. **base64 binds** (`c5e703c`) — a `contentEncoding: base64` string materializes as
   `ReadOnlyMemory<byte>` through a `BinaryTypeReferencePlan`; any other encoding refuses by name,
   and a binary arm inside a structural union refuses too. This is what `snapshot`'s `checkpoint`
   needed.
2. **The HTTP family** (`726a564`) — curated `internalRaw` group keyed on `ptyID`, ten operations
   selected, hand-written `PersistentPtysClient`/`PersistentPtyClient` over the generated raw
   clients, the shared `Internal/PtyTicketHeader` sentinel, DI registration, arm-complete contract
   tests.
3. **The shared core** (`c49b49e`) — `Internal/TerminalSocketCore<TFrame>` behind
   `ITerminalFrameDecoder<TFrame>`, `ITerminalClosePolicy`, and `ITerminalUpgradeFailurePolicy`;
   `PtySession` re-hosted on it with its public surface unchanged.
4. **The session and its door** (`ccb5376`, fix `2514a60`) — `PersistentPtySession`, its options,
   the closed frame hierarchy with the unknown-type carrier, the framed input encoder, the close
   and upgrade-failure policies, and the connect URI builder. `ConnectAsync` returns only after the
   server's `attached` frame and exposes it.
5. **Transport-owned** (`efac958`) — `v2.persistentPty.connect` fingerprint-pinned; the marker moved
   to 122 / 12 / 2.
6. **Test reach** (`5997172`, fix `68a03c3`) — the exact-pin fixture's external-endpoint mode
   (`OPENCODE_SDK_TESTS_ENDPOINT`/`OPENCODE_SDK_TESTS_PASSWORD`, with a blank-password guard that
   fails by name), `PersistentPtyDaemonGate` (`OPENCODE_SDK_TESTS_PTY_DAEMON` override; Windows
   defaults to daemon-absent), and the WSL2 recipe in `tests/OpenCode.Sdk.Sandbox/README.md`.
7. **The live proof** (`9cdc53d`, fix `db7de72`) — two-arm `PersistentPtyLiveTests`, the sandbox's
   `PersistentPtyWalkthrough` leg, and the shared `TerminalExcerpt` helper.
8. **Canon, research log Q156, and the roadmap** (`150c263`, fix `59bc2f9`), then the final fix wave
   (`cf062b8`, `eb1d15a`, `8c87c30`, `d064ca8`, `2801dd4`): the decoder refuses a JSON null in a
   promised frame member, the MA0144 arbitrations were deleted in favour of
   `OperatingSystem.IsWindows()`, three canon sentences this arc falsified were corrected, the live
   test returned to the plan's location-free session shape, and the read-ladder allocation evidence
   landed.
9. **This documentation commit** — the hosted and WSL2 proof in Q156, the corrected README recipe,
   the roadmap update, and this handoff.

## Proof and measurement findings the next session should not re-derive

**The round trip executed three times, all from TUnit report artifacts / TRX** (TUnit captures
console output, so the arm line never appears in a job log — read the artifact):

```text
ppty-live: arm=round-trip id=pty_persistent_3640412883362027 status=Running role=Controller inputProtocol=1 echo=sdk-live resize=100x30 read=sdk-live remove=204 listed-after=absent
ppty-live: arm=round-trip id=pty_persistent_3236185671631908 status=Running role=Controller inputProtocol=1 echo=sdk-live resize=100x30 read=sdk-live remove=204 listed-after=absent
ppty-live: arm=round-trip id=pty_persistent_1152050572023375 status=Running role=Controller inputProtocol=1 echo=sdk-live resize=100x30 read=sdk-live remove=204 listed-after=absent
```

hosted Linux, hosted macOS, and the WSL2 run respectively. Windows carried the other arm, as
designed, and every `PersistentPty*` class passed there:

```text
ppty-live: arm=daemon-absent create=503 service=opencode-pty list=0 read=<null> handoff=<null> shutdown=204
```

- **The hosted daemon-binary risk is closed.** `bun install --frozen-lockfile --ignore-scripts` on
  the hosted Linux and macOS runners does leave a usable `opencode-pty` binary, and the daemon
  starts under CI. Do not re-litigate this before a pin refresh changes it.
- **The WSL2 recipe works and is now correct in the README.** WSL Ubuntu 22.04, HTTPS clone at
  `~/repos/opencode-sdk-dotnet`, submodule shallow at `106629aa`, bun **1.3.14** (the pin's own
  `packageManager`, not the workstation's 1.4.0) at `~/.bun`, isolated XDG roots under
  `/tmp/ocsdk`, server on port 4097, Windows-side filtered `dotnet test` → 4 of 4 TFMs passed. Its
  gotchas are in Standing facts below.
- **The `PtySession` read ladder was re-measured across the shared-core extraction and the added
  interface dispatch costs 0 B.** `DecodeFrames` is byte-identical on both runtimes;
  `ReadFramesAsync` allocates 0–16 B *less* per read. Before `.benchmarks/b2-short`, after
  `.benchmarks/persistent-pty-after-short`, joined in
  `.benchmarks/persistent-pty-short-comparison.csv` (Q156). The 16,776 B per-`ReadAsync()` receive
  buffer still stands as the optimization target.
- **Timing on this workstation is not comparable across runs.** A control run of *identical*
  compiled code four hours apart returned byte-identical allocation on 96 of 96 cases while 87 of
  them moved more than 10% on time. Allocation is the reliable axis; never quote a cross-run timing
  delta from here.
- **Allocation, baseline → the envelope arc:** .NET 10.0 **−1,847,984 B**; net472 **+56,615 B**
  (47 regressed), of which `MessageList` is 50,242 B. **Unattributed** — a bisect across the spec
  refresh and the envelope arc is the named follow-up. Allocation amplification is scale-invariant
  at ~5.8× and belongs entirely to deserialization: `ExecuteWithoutAdapterAsync` allocates a flat
  2,128 B across a 490× payload range.

## The next session's goals, in order

### 1. The hygiene batch — one dispatch, one review

Every deferred minor from the arc, plus the candidates the integration found. They are all
non-blocking and none has been triaged out; the point of batching them is that they are cheap
together and expensive one at a time. Verify each against the file before changing it — line
numbers move.

**Already resolved before this handoff:** the two net472 timing failures were scheduling, not
budgets — see Q157 and `tests/Shared/ParallelConstraintKeys.cs`; run `33288967119` is green on all
three legs. Nothing to harden here; a new test that starts a server process takes that key, and a
new test with a wall-clock bound takes keyless `[NotInParallel]`. One residual is queued in the
roadmap's Known Gaps: a single server-process start still stalls whichever in-process tests are
running for about ten seconds on the hosted net472 host — harmless now, a measurement item.

**The batch is landed** (branch `feature/coverage-to-full`, 2026-08-30; the durable account is
research log Q158, the item-by-item table is
`.superpowers/sdd/2026-08-30-coverage-to-full/task-9-report.md`). The twenty-eight items this
section used to list, this arc's own additions, and the per-task review minors from Tasks 1–8 were
dispatched once and reviewed once. Two of them were not cosmetic: the persistent-PTY decoder
mapped an unrecognized `role` — a JSON null included — onto `Controller`, which reported an
observer as a controller, and `ResizeAsync` committed the viewport before the control frame had
left. Both are fixed and tested. The family-neutral rename landed too: `IPtyWebSocket` →
`ITerminalWebSocket`, `ClientPtyWebSocket` → `ClientTerminalWebSocket`, the scripted and canned
doubles with them, and `ptyId` → `terminalId` on `ITerminalUpgradeFailurePolicy.Map`. All
internal; the PublicApi snapshot is unchanged.

**What the batch left for the maintainer.** Three canon sentences are proposed, not applied, in the
task report: `client-runtime.md`'s ragged frames-bullet wrap, its "three named seams" bullet (the
core consumes two of them; the upgrade seam is consumed at connect), and a sentence covering the
decoder's new role refusal. Two follow-ups are queued in the roadmap's Known Gaps with their
reasons: `envelopePayloadNames` rows still cannot carry a reason, and the dot-segment refusal now
has two homes (the shared `RouteValuePolicy.EscapeSegment` and the generator's emitted copy).

One note on scope kept from before: `2514a60` was typed `test(sdk)` while also changing the
decoder's production wording; that is a cosmetic label on a landed commit, recorded rather than
fixed.

### 2. Coverage to full — the last twelve

Grouped by the mechanism that unblocks them, not by family. `persistentPty.snapshot` is **now
selected**, so no persistentPty row remains on this list.

- **Curation-only (3):** `vcs.base` is `[bindable]` outright (the `vcs.branches` envelope shape);
  `config.get` needs a `schemaNames` row (its C# name collides with `Config.Model.CostEncoded`);
  `fs.list` needs an `envelopePayloadNames` row (the group does not pluralize naively).
- **Query-parameter mechanism (3):** `fs.find` and `vcs.diff` both refuse with "query parameter
  must be optional"; `session.stats` refuses on an unsupported query schema shape. One design touch
  likely clears all three.
- **Error-style mechanism (3):** `worktree.create` / `.refresh` / `.remove` all refuse with
  "selected errors must use the Effect `_tag` style in M1" — an ADR-0007 decision, not a curation
  row.
- **Envelope mechanism (1):** `shell.output` — "location envelope 'data' must reference a named
  component schema, or be an array of one". **Closely adjacent to what C2 already built** for
  `vcs.branches` (ref-to-array resolution); read that arm before designing.
- **Path mechanism (1):** `fs.read` — "wildcard paths are not supported in M1". An upstream-report
  candidate; the wall stands meanwhile.
- **Structural-union wall (1):** `experimental.migration.v1.status` — the known union-overlap wall.
  The maintainer chose option (a) on 2026-08-28: **the wall stands.** Do not reopen it for
  convenience.

### 3. The tools touch

All four landed on `feature/coverage-to-full` on 2026-08-30 (Tasks 5–8 of
`docs/superpowers/plans/2026-08-30-coverage-to-full.md`); the bullets below record what each was:

- **Proposal 1 — landed (`936dfbf`, fix `6726900`).** The mechanical `_N` stabilize-duplicate
  collapse with its seven guardrails (`DeepEquals` or refuse naming both, fixpoint, never chains,
  manifest telltale, explicit rows kept for non-`_N` cases, loud worst case); 24 rows retired,
  25 folds in the manifest.
- **Proposal 2 — landed (`a14a82a`).** `spec/source-watch.json` pins both file sets (20 entries) by
  path, sha256, and a content anchor; the receipt carries `watchedSources`; a review trigger only,
  never a generation input (ADR-0013). Apply refuses a receipt whose anchor failed — the
  maintainer has not sealed that step.
- **The `ToolJsonContext` `NewLine` pin — landed (`3122bdb`);** the manifest and receipt are LF-only.
- **The single-key envelope facet — landed (`d6d3ff8`).** `SpecEnvelopeShape.SingleKey` flattens an
  inline one-required-key body: `response.Handoff` is `PersistentPtyHandoff?` and `server.get`
  reads `response.Urls`.

### 4. Release preparation

Untouched and maintainer-gated. Note the coupling: the packing wall keys on generation
completeness, so goal 2 and this one meet.

- **Packing-wall wording** — now satisfiable at full admission (`transportOwned` leaves pending, so
  122/12/2 can reach 134/0/2). Decide the prerelease-versus-stable wording **before** the coverage
  work closes, or it blocks its own release.
- **ADR-0021's `Date:`** stays 2026-08-26 while the record carries a "Scope (revised 2026-08-29)"
  section. `documentation.md` says a material decision revision moves the date; the plan said keep.
  The maintainer decides.
- **Whether the no-suppression rule enters `quality-gates.md`.** It currently lives only in this
  handoff's Standing facts — an operational doc — which is exactly why it went missing once already.
- **Pre-1.0 versioning:** `VersionPrefix`, `RELEASE_NOTES.md` discipline, GitHub Packages CD plus a
  manual NuGet.org lane (ADR-0006).

### 5. Benchmarks

Add a `PersistentPtySession` read-ladder rung beside the `PtySession` one, then run the full default
job at the milestone boundary — not before, and not on a contended machine. The canonical invocation
is in `quality-gates.md`.

## Also queued (recorded in ROADMAP, do not re-derive)

- **The pre-1.0 surface review** — the `{handoff}` accessor above; ADR-0019's parent-mediated id
  access for flat families; the Azure `dotnet-subclient-properties` follow-up; the SDK folder layout
  review before the M5 freeze.
- **Tools hygiene (C2 leftovers)** — the wrapper-shape predicate duplicated across
  `SchemaNameResolver` and `EnvelopeFacetBinder` is the one with a real silent-drift hazard (a
  one-sided widening falls back to a pointer-derived name with **no wall to catch it**); plus two
  XML-doc one-liners, two unit rows, and the `AssertMissingDataFailsAsync` rename.
- **vcs.status contract top-up** — empty-list, location-query, protocol-failure tests.
- **`PtySession` `ArrayPool` rent** (the 16 KiB per-call receive buffer) and the **net472 allocation
  bisect** across the spec refresh and the envelope arc.
- **Benchmark metric-id pin** — the diagnoser's descriptor ids and `BenchmarkRunReader`'s constants
  are duplicated strings in *different assemblies*; an emit-side rename fails **soft** to blank wire
  columns with every test still green. A shared constant or contract test closes it.
- Two curation/doc minors: the `form` group's reason says "no per-id operations, ever" where
  siblings state present-tense fact; the `MedianNanoseconds` CSV column breaks the abbreviation
  convention.
- Four parked upstream reports plus the three this arc added (research log Q156): the `lines` query
  on `persistentPty.read` is a bare string while the handler decodes 1..65535; close code 4404 is
  overloaded across "no such terminal" and "no daemon"; `v2.persistentPty.connect` declares 404 and
  503 arms its handler cannot produce.

## Standing facts and gotchas

- **The `opencode-pty` daemon does not exist on Windows.** `@opencode-ai/pty` ships darwin/linux
  platform packages only at the pin, the server spawns it as a detached child over a Unix domain
  socket, and no container can host it alone. Any local persistentPty result on this workstation is
  the daemon-absent arm — correct, asserted, and only half the story.
- **The WSL2 recipe lives in `tests/OpenCode.Sdk.Sandbox/README.md`** and is corrected. Its four
  gotchas: drive WSL from a script file (`MSYS_NO_PATHCONV=1 wsl.exe -- bash /mnt/c/<path>.sh`),
  because Git Bash mangles quoting and converts `/tmp` paths; stop the server by pattern
  (`pkill -f "index.ts serve --port 4097"`), because a backgrounded `setsid nohup … &` leaves the
  pid file empty; **never share the Windows checkout's `node_modules` with WSL** — one tree holds
  the platform package of whichever OS installed last, so the installs clobber each other's daemon
  binary; and the sandbox needs `--no-launch-profile` (its `launchSettings.json` prefills
  `OPENCODE_SANDBOX_ENDPOINT` at port 4096) while its earlier session legs answer 500 on a
  provider-less isolated server, so it is not a substitute for the live test.
- **The maintainer's Linux-side assets persist:** the WSL clone at `~/repos/opencode-sdk-dotnet`
  and bun 1.3.14 at `~/.bun`. Reuse them rather than re-cloning. **The WSL clone's submodule is
  still at `106629aa`** and its superproject at `2801dd4`, so a bare `git submodule update` there would
  re-check-out `106629aa`: after the refresh commit is pushed, `git pull` in the WSL clone first, then
  `git submodule update` (the shallow submodule fetches the missing commit itself).
- **TUnit captures console output.** Any evidence a test prints lives in the TRX or the TUnit report
  artifact, never in the CI step log. Fetch the artifact.
- **Slopwatch walks the filesystem and does NOT consult `.gitignore`.** `node_modules/` is
  gitignored and was still scanned on CI. The canonical invocation
  (`--exclude ".scratchpad/**,external/**"`, `quality-gates.md`) is what protects the gate — CI and
  local must use the same line, or drift appears only on the hosted runner.
- **No suppression without an explicit maintainer approval AND a good, verifiable reason.**
  Slopwatch is at zero and stays there; a documented impossibility argument is the only accepted
  shape (`abdfb28` is the precedent).
- **`external/` is a submodule, deliberately checked out on CI** (`submodules: true`) so `bun
  install` can build the pinned server. It is not ignored, and the runner creates its
  `node_modules` itself.
- **Never edit the performance project while a benchmark run is live.** BDN regenerates and
  rebuilds a project referencing it per job/runtime *during* the run; a mid-edit tree either breaks
  a later build or silently changes what is measured. Run from a clean copy, or leave the tree
  alone; keep the machine otherwise quiet.
- **The canonical benchmark invocation is in `quality-gates.md`.** `--runtimes net10.0 net472` is
  load-bearing: without it BDN labels every case `DefaultJob`, and the comparison joins on the
  runtime label, so a whole run becomes unjoinable against the archive. (This cost a 1h48m run; the
  tool now fails loudly instead of printing an all-one-sided comparison.)
- **Editor/LSP diagnostics in this repo are stale and noisy** — routinely flagging generated members
  that exist. The gate is the referee, never the squiggles.
- **No backward compatibility until release** (Q131): renames are free; curation reasons must state
  facts that are true *today*, and several have gone stale as later work landed — check before
  citing one.
- Naming traps: the location query member is `workspace` (the body elsewhere uses `workspaceID`);
  the emitted vcs method is `GetBranchesAsync`, not `ListBranchesAsync`.
- `session.idle` is deprecated upstream with no publisher while `SessionIdle` stays in the event
  union — an upstream-report candidate parked with the others.

## Completion gates

Unchanged (`docs/engineering/quality-gates.md`): slopwatch, Release build, format whitespace/style,
tests; generator changes add the tool smoke and `generate --verify`; snapshot changes add
`refresh-spec --verify`. **Slopwatch is at zero — keep it there.** Repo policy: no AI trailers;
direct green commits stay authorized in the development loop; **never push without an explicit
ask**; canonical-document edits only with explicit maintainer approval.

## Ready-to-paste priming prompt

```text
Start a session for opencode-sdk-dotnet from
docs/agents/handover-prompts/HANDOFF-2026-08-30.md. Consume it against live Git,
src/OpenCode.Sdk/.generation-incomplete (the pending map), and docs/ROADMAP.md before changing
anything.

State: the persistent PTY family arc is merged into master at 2801dd4, followed by the closing
documentation commit and the net472 scheduling fix (green in run 33288967119); the branch is
deleted. Pin b1e3a7b2 (document-identical refresh from 106629aa), profile 122 selected / 12 pending /
2 transport-owned of 136, slopwatch ZERO, 4,016 tests green locally. The live round-trip arm is
PROVEN — hosted Linux, hosted macOS, and a WSL2 run all carried arm=round-trip. The hosted Windows
net472 red of run 33276305571 was parallel server-process scheduling, not timing budgets; it is
fixed, and Q157 records the run that verified it.

Goals this session, in order: (1) the hygiene batch as ONE dispatch with ONE review — the 28
arc minors listed in the handoff with file:line plus the sandbox items; (2) the last twelve pending operations,
grouped by mechanism: the
curation-only trio (vcs.base, config.get, fs.list), the query-parameter three, the error-style
three (ADR-0007), shell.output, fs.read, and migration.v1.status behind its standing wall;
(3) the tools touch riding the first mechanism that reaches tools/ — Proposal 1, Proposal 2, the
ToolJsonContext NewLine pin, and the {handoff} envelope facet; (4) release prep, starting with the
packing-wall wording, which is now satisfiable; (5) a PersistentPtySession benchmark rung.

Work subagent-driven with per-task reviews, foreground commands only. Repo policy: no AI trailers;
never push without my explicit ask; canonical edits need my approval.
```
