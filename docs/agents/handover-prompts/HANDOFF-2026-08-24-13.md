# Session Handoff - runtime pipeline design sealed; execute the increment plan

Date: 2026-08-24

Consume this against live Git, the worktree, and GitHub before changing anything. This handoff
supersedes and deletes `HANDOFF-2026-08-21-12.md`; the pause it described is over. Repository files
and current canon beat this handoff. The sealed design and its rationale live in research log
Session 40 (Q126–Q129) and `docs/superpowers/plans/2026-08-24-runtime-pipeline-plan.md`; this file
owns only operational continuity.

## Start here

```powershell
git status --short --branch
git log --oneline --decorate -10
git fetch origin
```

Do not pull automatically: local `master` is ahead of `origin/master` and nothing is pushed. Until
Increment 0 runs, the worktree still contains the uncommitted R16 experiment (six modified, five
untracked files listed in Q125/HANDOFF-12 history); do not build on it — Increment 0 archives it as
a patch under `C:\bench-artifacts\` and cleans the worktree.

## State

- The Session 40 design session sealed the runtime rebuild: internal policy pipeline
  (`PipelineMessage`/`PipelinePolicy`/three-policy roster), `ResponseMaterializer`, A3-Full
  generated status verdicts, `FailureClassification`, `IEventStreamFramer` + `FrameDispatch`,
  progress-timeout semantics, R16's mechanism re-derived inside `ResponseBufferingPolicy`, and
  full-parity low-allocation encoding. The plan document owns contracts, increments, gates, the
  test migration map, and the canon-edit schedule.
- Research doc 17 (modern .NET allocation API sweep) was dispatched 2026-08-24 and may still be in
  flight; it gates Increment 3 internals, Increment 4, and SSE stage-2. If
  `docs/research/17-modern-dotnet-allocation-apis.md` is absent, that research must complete and be
  reviewed before those increments start; Increments 0–2b do not wait for it.
- Increment order (each independently green through the full gate): 0 archive R16 →
  1 `FailureClassification` → 2 pipeline skeleton (behavior-preserving, zero generated diff) →
  2b A3-Full (generator increment) → 3 pooled buffering + progress timeout (behavior change,
  red-first, canon edit inside) → 4 encoding hardening. Benchmarks per the quality-gates rules,
  clean copy outside the repository, allocation primary.
- Typed-switch union dispatch stays paused until after the increments; the M4 launcher plan starts
  after them; issue #53's wall inventory remains M5.

## Preserved review leftovers (from the Session 38/39 register; still open)

Docs items R04/R05/R06/R11 resolve losslessly in a documentation pass: ADR-0014's already-closed
immutable-collection question, ROADMAP's append-only history relocation, the decoding-rule
single-owner cleanup across canon, ADR-0009's `Date:` move and duplicated mechanism statement, and
the SSE wording nits. None block the increments.

## Policies

Direct green commits remain authorized inside the agreed development loop; never push without an
explicit ask; no AI-attribution trailers; canonical-document edits only with explicit maintainer
approval, scheduled by the plan's canon-edit list. Benchmark evidence locations and the completion
gate are unchanged from `docs/engineering/quality-gates.md`; permanent evidence lives in research
Q121–Q129, temporary artifacts under `C:\bench-artifacts\` and the clean copies under `C:\bench*`.
