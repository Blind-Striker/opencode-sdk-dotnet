# Session Handoff - M3 Arc 6 measured performance pass opens next

Date: 2026-08-20

Consume this against live Git and GitHub. This is an aggregated M3 handoff: Arc 3a, Arc 3b, Arc 4,
and Arc 5 are complete, reviewed, pushed, and hosted green. The exact next bounded increment is Arc
6's measured performance pass. No Arc 6 source or measurement work began during Arc 5.

## Live Baseline

| Item | State |
|---|---|
| Source head | `b261014` (`fix(sdk): harden owned transport`) |
| Branch | `master`, synchronized with `origin/master` before this docs-only handoff commit |
| Worktree | Expected clean after this handoff commit |
| Tests | 1,374 local executions, 0 failed, 0 skipped |
| Generation profile | 15 selected, 105 pending |
| Hosted CI | Run `32350952168` green on Linux, Windows, and macOS at `b261014` |
| M3 completed | Construction runway, location/request design, Arc 3a SSE/session log, Arc 3b global bus, Arc 4 paginator, Arc 5 owned transport/net472 GA gate |
| Exact next | Arc 6 measured performance pass (#23/#29/#33 plus generated collection comparison) |
| Carried issue | #53 remains open only for deferred M5 walls |
| ADRs | 0001-0017 |
| Research | Through Session 36 / Q119 |

Direct green commits to `master` remain authorized for the development loop. Work one green source
increment at a time: baseline measurements, smallest implementation, after measurements, complete
diff/PublicApi/manifest review, focused tests, one fresh-context read-only review, all applicable
gates, commit, push, hosted Linux/Windows/macOS, owning-issue updates, and an aggregated handoff.

The main agent exclusively owns edits, generation, tests, measurements, commits, pushes, and GitHub
updates. Subagents are read-only reviewers/researchers. Do not use stacked PRs, implementation
worktrees, or parallel writers. `docs/superpowers/` remains transient reference material.

## Reading Order

1. `AGENTS.md`, `docs/engineering/quality-gates.md`, and the Arc 6 section of the transient M3 plan.
2. Live issues #23, #29, and #33; ADR-0007, ADR-0009, ADR-0014, and the current response/union
   adapter paths they constrain.
3. Research Q108's generated collection comparison, Q113's stream baseline, and the permanent
   BenchmarkDotNet suite under `tests/OpenCode.Sdk.Performance.Tests`.
4. `docs/architecture/protocol-and-generation.md`, `docs/architecture/client-runtime.md`,
   `docs/engineering/coding-style.md`, and `docs/engineering/testing-style.md`.
5. `docs/ROADMAP.md`, current PublicApi baselines, and generated manifest/output.

Repository files and current canon beat this handoff. Arc 6 is performance work: no optimization
lands from intuition, and contract costs such as error-path `RawBody` retention stay visible.

## Completed Arc 5

- Owned modern and downlevel handlers disable automatic redirects; surfaced 3xx responses fail as
  protocol errors before body reads on one-shot and streaming paths, including under `NoThrow`.
- Modern `SocketsHttpHandler` retains its observable 120-second pooled lifetime. Downlevel
  endpoint-and-proxy `ServicePoint` connection/lease policy is applied at construction and before
  every owned send, surviving idle scavenging and ambient proxy changes without changing global
  defaults. The selected `ServicePoint` remains process-shared by BCL design.
- A real loopback handler path proves redirect refusal, proxy-backed policy, and two same-authority
  net472 streams coexisting with an ordinary request. Separate response/content trackers cover
  success, API error, protocol failure, and caller cancellation.
- Generated path/query values refuse lone surrogates and inputs above 32,766 UTF-16 code units before
  escaping on every TFM. Exact-limit values, valid pairs, empty cursors, and dot/blank path guards
  remain accepted/refused as previously sealed.
- Fresh-context review caught and closed a netstandard2.0 runtime-member trap, `ServicePoint`
  reapplication, vacuous concurrency assertions, compile-probe TFM drift, and test-fixture risks.
  PublicApi and manifest membership are unchanged.
- Local gates passed with 1,374 executions; source `b261014` passed hosted run `32350952168` on all
  three operating systems. Issues #43 and #32 are closed. Research Q119 owns the durable detail.

## Exact Next Increment - Arc 6

Arc 6 is the final M3 increment and remains one measured pass because the mechanisms share response
materialization and generated API trade-offs:

1. Record fresh baselines against the current `b261014` tree and compare under the same SDK/runtime.
2. Implement #23's single-pass known-union dispatch and success-path response adapters only where
   measurement justifies them. Unknown carriers remain lossless; error-path `RawBody` remains.
3. Close #29's remaining measured response/cursor/DOM copies inside that same adapter rewrite.
4. Execute sealed #33 inside the converter rewrite: hand-constructed carriers refuse tag/payload
   disagreement; `Write` remains payload-only verbatim replay.
5. Run the pre-freeze generated collection comparison from Q108: direct `IReadOnlyList` /
   `IReadOnlyDictionary` versus immutable alternatives, with source-generation, Native AOT,
   netstandard2.0/net472, ergonomics, allocation, and throughput evidence. Do not select a shape by
   convention.
6. Measure the downlevel `StringBuilder.Append(StringBuilder, int, int)` polyfill path called out in
   the plan rather than assuming net10 frame-reader numbers transfer.

### Arc 6 boundaries

- No optimization lands without before/after BenchmarkDotNet evidence under `MemoryDiagnoser`.
- Do not weaken error typing, `RawBody`, `NoThrow`, cancellation, charset/BOM decoding, unknown-union
  preservation, or response ownership to improve a benchmark.
- Do not reopen Arc 5 transport/route policy without new contradictory evidence.
- Do not start launcher, M5 breadth/#53 walls, telemetry, retry, hooks, spec refresh, or MCP work.

## Completion Gates

The base gate remains:

```bash
dotnet tool run slopwatch analyze --exclude ".scratchpad/**,external/**" --fail-on warning
dotnet build --configuration Release
dotnet format whitespace --verify-no-changes --no-restore
dotnet format style --verify-no-changes --no-restore --severity warn
dotnet test --configuration Release --no-build
```

Generator/curation/output changes also run:

```bash
dotnet run --file tools/opencode-tool.cs -- --help
./tools/opencode-tool.cs --help
dotnet run --file tools/opencode-tool.cs -- generate --verify
```

Before committing, inspect status, every changed/new/deleted file, the complete diff, PublicApi,
manifest, benchmark artifacts, and recent log. Push directly to `master`, wait for
Linux/Windows/macOS, update owning issues, and write the next aggregated handoff.

## Ready-To-Paste Priming Prompt

```text
Continue work on /home/deniz/src/opencode-sdk-dotnet from
docs/agents/handover-prompts/HANDOFF-2026-08-20-8.md.

Consume it against live Git and GitHub before touching files. Expected source head is b261014,
and hosted run 32350952168 is green on Linux, Windows, and macOS. Arc 3a through Arc 5 are complete;
no Arc 6 source or measurement work has begun.

The exact next increment is M3 Arc 6's measured performance pass from #23/#29/#33 plus the generated
collection comparison. Establish fresh same-environment baselines before changing code, land only
measured improvements, preserve error RawBody/unknown unions/response ownership, and do not start
launcher, M5, telemetry, retry, hooks, spec refresh, or MCP work.
```

Delete this handoff only when Arc 6 ships and a newly aggregated handoff supersedes it.
