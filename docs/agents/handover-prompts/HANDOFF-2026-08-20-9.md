# Session Handoff - M3 complete; M4 launcher planning opens next

Date: 2026-08-20

Consume this against live Git and GitHub. This is the aggregated M3 closure handoff: Arc 3a through
Arc 6 are complete, reviewed, pushed, and hosted green. No M4 planning, research, or source work began
during Arc 6.

## Live Baseline

| Item | State |
|---|---|
| Source head | `fa6124d` (`perf(sdk): reduce materialization costs`) |
| Branch | `master`, synchronized with `origin/master` before this docs-only handoff commit |
| Worktree | Expected clean after this handoff commit |
| Tests | 1,413 local modern-TFM executions, 0 failed, 0 skipped |
| Generation profile | 15 selected, 105 pending |
| Hosted CI | Run `32374393085` green on Linux, Windows, and macOS at `fa6124d` |
| M3 completed | Construction runway, location/request design, SSE/session log, global bus, paginator, owned transport/net472 GA, measured performance pass |
| Exact next | Create the short M4 launcher plan, then execute M4 only after alignment |
| Carried issue | #53 remains open only for deferred M5 walls |
| ADRs | 0001-0017 |
| Research | Through Session 37 / Q120 |

Direct green commits to `master` remain authorized for the development loop. Work one green source
increment at a time: align on the short milestone plan, smallest implementation, focused tests, one
fresh-context read-only review, all applicable gates, commit, push, hosted Linux/Windows/macOS,
owning-issue updates, and an aggregated handoff.

The main agent exclusively owns edits, tests, process demonstrations, commits, pushes, and GitHub
updates. Subagents are read-only reviewers/researchers. Do not use stacked PRs, implementation
worktrees, or parallel writers. `docs/superpowers/` remains transient reference material.

## Reading Order

1. `AGENTS.md`, `docs/ROADMAP.md`, and `docs/engineering/quality-gates.md`.
2. ADR-0001, the launcher section of `docs/architecture/client-runtime.md`, and
   `docs/architecture/platform-and-packaging.md`.
3. Research doc 06 section 3, research doc 15's launcher/platform evidence, and the relevant launcher
   passages in the public-API and testing architecture transient specs.
4. `docs/engineering/coding-style.md`, `docs/engineering/testing-style.md`, and current three-OS CI.
5. Live GitHub launcher issues/queue before deciding the exact M4 increment.

Repository files and current canon beat this handoff. The next session must align on M4's 1-2 page
plan before implementation; do not infer launcher behavior from the older transient API sketch.

## Completed Arc 6

- Source `fa6124d` passed hosted run `32374393085` on Linux, Windows, and macOS. The Windows leg ran
  the net472 timeout/cancellation and downlevel long-SSE-line evidence. Issues #23, #29, and #33 are
  closed as completed.
- Known tagged-union arms scan a copied reader and deserialize the original reader once. Unknown
  arms alone retain their `JsonElement`; duplicate top-level markers preserve last-value behavior.
  Hand-constructed unknown carriers refuse whitespace, non-object, missing, fixed-marker, or
  discriminator disagreement while `Write` remains payload-only replay.
- Declared one-shot successes use generated valid-UTF-8 byte adapters. Charset/BOM and malformed
  UTF-8 replacement behavior remain compatible; API errors remain decoded strings with `RawBody`
  under throw and `NoThrow`. One-shot timeout covers send through finite body consumption, caller
  cancellation stays distinct, abandoned reads are disposed and observed, and no-content successes
  skip body reads without weakening response ownership.
- Same-environment BenchmarkDotNet baseline -> final allocations: `GetMessageAsync` 21.91 -> 19.15
  KB, `ListMessagesAsync` 22.60 -> 19.81 KB, deep union 13.35 -> 12.72 KB, large session-log 717.82
  -> 713.32 KB, and small session-log 507.20 -> 435.20 KB. Parser controls remained 321.54/177.69
  KB. Health moved 2.08 -> 2.05 KB; no health throughput improvement is claimed. Q120 owns exact
  means, noise, environment, and limits.
- The generated collection comparison retained shallow `IReadOnlyList` / `IReadOnlyDictionary`.
  Immutable alternatives increased construction and/or deserialization cost and required a new
  downlevel package; all candidate shapes passed source-generation, Native AOT, and downlevel compile
  probes before the decision.
- Polyfill's downlevel whole-line append was replaced by a dedicated buffer. Source-equivalent
  complete-response measurements moved small lines from 144.38 to 16.41 KB and large lines from
  304.82 to 25.35 KB; Windows net472 contract evidence proves the buffer never overwrites unread
  decoder output.
- PublicApi and generated-manifest membership are unchanged. Fresh-context adversarial review found
  and closed the decoder-buffer alias, unmanaged body task, downlevel Polyfill wrapper, and public
  constructor exception leak. Final follow-up found no substantive issue.

## Exact Next Increment - M4 Entry

M4 is the launcher milestone. Start by producing and aligning on its short plan; no launcher source
change should precede that decision. The plan must reconcile ADR-0001 with current evidence rather
than copying the old transient sketch. It owns at least:

1. The smallest public `OpenCodeServer` / options surface and readiness contract.
2. Executable discovery, arguments, endpoint/port ownership, credentials, and stdout/stderr draining.
3. Cancellation, normal disposal, failed startup, crash observation, and descendant cleanup.
4. Fake-binary deterministic tests plus real `opencode2 serve` acceptance on Linux, Windows, and
   macOS; platform-specific behavior is exercised on its platform.
5. The net472 stdout and process-tree-kill items already assigned to M4.

### M4 boundaries

- Keep the launcher in `OpenCode.Sdk` and hand-written over `System.Diagnostics.Process` unless new
  evidence meets ADR-0001's reversal trigger.
- Do not reopen M3 streams, pagination, transport, route, union, response, or collection decisions
  without contradictory evidence.
- Do not start M5 breadth/#53 walls, telemetry, retry, hooks, spec refresh, release work, or MCP work.
- A compile is not launcher lifecycle evidence. The process boundary and three operating systems are
  the acceptance gate.

## Completion Gates

The base gate remains:

```bash
dotnet tool run slopwatch analyze --exclude ".scratchpad/**,external/**" --fail-on warning
dotnet build --configuration Release
dotnet format whitespace --verify-no-changes --no-restore
dotnet format style --verify-no-changes --no-restore --severity warn
dotnet test --configuration Release --no-build
```

Generator/curation/output changes also run:

```bash
dotnet run --file tools/opencode-tool.cs -- --help
./tools/opencode-tool.cs --help
dotnet run --file tools/opencode-tool.cs -- generate --verify
```

Before committing, inspect status, every changed/new/deleted file, the complete diff, PublicApi,
manifest, artifacts, and recent log. Push directly to `master`, wait for Linux/Windows/macOS, update
owning issues, and write the next aggregated handoff.

## Ready-To-Paste Priming Prompt

```text
Continue work on /home/deniz/src/opencode-sdk-dotnet from
docs/agents/handover-prompts/HANDOFF-2026-08-20-9.md.

Consume it against live Git and GitHub before touching files. Expected source head is fa6124d,
and hosted run 32374393085 is green on Linux, Windows, and macOS. M3 is complete; no M4 planning,
research, or source work has begun.

The exact next increment is M4 entry: inspect live launcher issues/evidence, create and align on the
short M4 plan, and only then implement the first bounded launcher slice. Do not reopen M3 or start
M5, telemetry, retry, hooks, spec refresh, release, or MCP work.
```

Delete this handoff only when a newly aggregated handoff supersedes it.
