# Handover — Slice 1 execution session (issue #2)

Date: 2026-08-10

Priming prompt for the implementation session. Consume against live git; delete when the
slice 1 PR merges.

---

Task: execute the Slice 1 (issue #2 — Parser + SpecIR) implementation plan.

This session is an IMPLEMENTATION session only:

- Use the `deniz-process:subagent-driven-development` skill (the sealed execution model —
  slice map, decision 7). Fresh subagent per task + two-stage review.
- The plan is executed task by task; no scope beyond the plan, no new design decisions,
  no reopening sealed documents.
- Contradictions with a spec or the plan: `docs/agents/deviation-protocol.md` —
  subagents never self-resolve level 2+; the session stops and involves the maintainer.

ONBOARDING:

1. `AGENTS.md`
   - Locked Decisions
   - Hard Rules
   - Working Agreements
   - Engineering Conventions
2. `docs/agents/deviation-protocol.md`
3. `docs/superpowers/plans/2026-08-10-slice-01-parser-specir.md`
   — the canonical execution artifact: Global Constraints + "SpecIR at a glance" + the
   11 tasks. Task steps are deliberately self-contained; a dispatched subagent receives
   its task text plus the Global Constraints and "SpecIR at a glance" sections.
4. `docs/superpowers/plans/2026-08-10-implementation-slice-map.md`
   — only "Done definition — every slice".
5. Reference (only if a task text is not enough): generator spec §4.1/§11
   (`docs/superpowers/specs/2026-08-09-generator-architecture.md`) — §4.1 received four
   dialect corrections in the planning session; the plan matches the corrected text.

LIVE STATE:

- Slice 0 is merged (issue #1 closed); the slice 1 plan is sealed and issue #2 is
  `ready-for-agent` with the plan linked.
- Claim issue #2 with `gh issue edit 2 --add-assignee @me`.
- Worktree + branch: `feature/slice-01-parser-specir` (created at execution start via
  `deniz-process:using-git-worktrees`; no implementation on master).
- Per-task commits are free on the slice branch (the AGENTS.md development-loop
  exception); merging to master needs PR approval.
- `generate` stays a fail-loud stub for the whole slice; CLI behavior, ToolApp/DI, and
  `.github/workflows/ci.yml` do not change.
- New pins: TestableIO trio 22.2.0 + TestableIO.System.IO.Abstractions.Analyzers
  2022.0.0 (task 1; re-check newest stable before pinning).

EVERY TASK:

- TDD: RED (focused test failing with the expected error) → GREEN → the full gate
  (`dotnet build --configuration Release` → `dotnet test --configuration Release
  --no-build` → `dotnet format --verify-no-changes --no-restore` →
  `dotnet tool run slopwatch analyze --exclude ".scratchpad/**,external/**" --fail-on
  warning`) → Conventional Commit (subject written in the plan task).
- Count assertions against the pinned spec are banned; the plan's landmark shape
  assertions are the law.
- If the pinned TUnit/CommandAppTester/TestableIO API differs in detail, adapt in place
  (level 0); the adaptation notes inside the plan tasks apply.

CLOSURE:

- Task 11: ROADMAP one-liner + research-log entry (only if implementation produced
  findings) + push + PR (`Closes #2`, all three CI legs green).
- The PR body carries the plan's honest note: the CLI did not change; the slice's
  working software is the parser library plus its test suite.
- Merge needs maintainer approval; the merge closes issue #2 and this handover file is
  deleted.
