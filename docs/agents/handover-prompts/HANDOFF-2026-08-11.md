# Handover — Slice 1 execution: Ingestion + SpecIR

Date: 2026-08-11

Priming prompt for the Slice 1 execution session. Consume against live git; delete when the
slice PR merges. Transient and agent-facing — unlike canonical docs, this file may point at
the maintainer's other repositories and machine-local paths.

## Task

Execute `docs/superpowers/plans/2026-08-11-slice-01-ingestion-specir.md` task-by-task via
deniz-process:subagent-driven-development on a fresh worktree branch
`feature/slice-01-ingestion-specir` (deniz-process:using-git-worktrees). One PR to master,
`Closes #2`. The plan is the canonical execution artifact; this handover adds context the
plan deliberately keeps machine-independent.

## Onboarding order

1. `AGENTS.md`; `docs/agents/deviation-protocol.md`
2. `docs/engineering/coding-style.md` + `docs/engineering/testing-style.md` — **binding on
   every task**; the executing agents' output is reviewed against them
3. Generator spec §3.1, §4, §4.1, §4.2, §9, §11
   (`docs/superpowers/specs/2026-08-09-generator-architecture.md`)
4. The plan itself, in full
5. Research log sessions 12–14 (`docs/research/00-research-log.md`) — why the previous
   parser died, what the wall grill fixed, and the overengineering grill's hosting/test
   authorship corrections

## Live state

| Item | Value |
|---|---|
| Main workspace | `E:\repos\my-projects\opencode-sdk-dotnet`, branch `master` |
| Slice issue | #2 (`ready-for-agent`) |
| Evidence worktree (READ-ONLY) | `C:\Users\deniz\AppData\Local\Temp\opencode\opencode-sdk-dotnet-slice-01-parser-specir` |
| Evidence branch | `feature/slice-01-parser-specir` — the retired parser; never modify, never delete |

## Style model repositories (the maintainer's own — "build it in this style")

The engineering docs distill these; read the originals when a rule needs depth or a concrete
model. The repos are models for *shape*, not sources to copy — size everything to this
repo's need.

- **Extraction & testing guidelines (origin of `docs/engineering/`):**
  - <https://github.com/janset2d/sdl2-cs-bindings/blob/spike/binding-autogen-sdl2-gfx/docs/knowledge-base/extraction-guidelines.md>
  - <https://github.com/janset2d/sdl2-cs-bindings/blob/spike/binding-autogen-sdl2-gfx/docs/knowledge-base/testing-guidelines.md>
- **Scenario test infrastructure model** (first-class fakes over TestableIO — the mechanics
  Task 2 adapts, not its class-per-situation file count):
  <https://github.com/homeruntech/dotnet-backend-monorepo-tool/tree/master/tests/Homerun.Dotnet.MonoRepoTools.Tests/>
  — study `Scenarios/Base/ScenarioBase.cs` (+`ScenarioContext`), `Utils/ProjectBuilder.cs`
  (fluent, domain-aware, `MockFileSystem`-backed, embedded resources via `ResourceLoader`).
  Small one-off variations stay inline through `SpecScenario.Define`; named scenario
  classes require reuse, non-trivial setup, or durable cross-slice identity.
- **Hosting/DI composition model** (Task 1 takes the complete topology, not a subset):
  `E:\repos\my-projects\env-variable-tools` — `src/PathSmith/Program.cs` (one
  `ServiceCollection` composition root → `DependencyInjectionRegistrar` → `CommandApp`),
  `Services/Abstractions/` (interfaces) vs `Services/` (implementations), `Models/`,
  `GlobalSettings`, `GlobalOptionsInterceptor` (`ICommandInterceptor` for cross-cutting
  CLI concerns), filesystem/console seams, logging, core services, and application
  services. Preserve that full hosting and registration shape; render logging through
  Microsoft.Extensions.Logging (`ILogger<T>` + Spectre/optional-file providers), not the
  model repo's literal custom `ILogger`. Tests replace seams after `ToolApp.CreateServices`
  instead of copying PathSmith's test registration list.

## Reference implementations — per-task map (adapt, never transplant)

The redesign probes live in the evidence worktree's gitignored scratchpad:
`<evidence-worktree>\.scratchpad\OpenApiProbe\` — run from the worktree root with
`dotnet run --project .scratchpad/OpenApiProbe/OpenApiProbe.csproj -- <mode>`; preserved
outputs at `.scratchpad/probe-outputs-2026-08-11.md`. **Caveats:** the probes sit behind a
`root = true` editorconfig (the analyzer wall never ran on them), are single-file
blacklist-shaped scratch code, and violate the coding style on purpose — take the
*mechanics* (proven API calls, predicates, traversal shapes), never the shape. Production
is whitelist-shaped collaborators.

| Plan task | Reference |
|---|---|
| 3 — reader gate | `OperationalProbe.cs` (`LoadAsync` + `LeaveStreamOpen` usage); `HostLossBisect.cs` (the boolean-schema NRE and `3.2.0` injections — the red-test shapes) |
| 4 — schema wall/projection | `ValidationProbe.cs` `WalkSchema` (traversal + refusal mechanics — **invert blacklist→whitelist**); `InventoryProbe.cs` (`IsUnrestricted` predicate, DOM walk, pointer building) |
| 5–6 — object/union family | `ProjectionProbe.cs` (`IsSpecialNumber`, `ClassifyErrorStyle`, `IsLiteral`, `ErrorBranches` — typed `OpenApiSchemaReference.Reference.Id` access, dedup); `OperationalProbe.cs` `TryParseFragments` (the exact `OpenApiModelFactory.Parse<OpenApiSchema>` call for `prefixItems`) |
| 7 — operation surface | `ProjectionProbe.cs` (`StripParameters`, `ClassifyEnvelope`, SSE + `x-effect-stream` extraction via `JsonNodeExtension`); `GrillProbes.cs`/`HostLossBisect.cs` (per-host silent-typing evidence → wall red tests) |
| 8 — hashes + raw walk | `ProjectionProbe.cs` `Fingerprint`/`Sha256`; `InventoryProbe.cs` raw `JsonNode` walk (the `RawSiblingScanner`/`RawContentHasher` traversal shape) |
| 9 — tripwires/guards | `GrillProbes.cs` `RunMembers` (the DOM member+defaults inventory the baseline is built from) |
| 10 — landmark smoke | `ProjectionProbe.cs` landmark checks + the preserved outputs file |

The retired parser (committed on the evidence branch, `tools/OpenCode.Sdk.Tools/Generator/Parsing/`)
adapts well for: `SpecMediaType` (near-verbatim), `SpecParseErrorCollector` (→
`IngestionErrorCollector`), graph-key/promotion mechanics, and the old plan's exhaustive
test-case inventories (the superseded plan file exists on that branch). Its architecture is
the counter-example — research log session 12 records why.

## Warnings

- Never modify, clean, or delete the evidence worktree or its branch; disposition is the
  maintainer's call after this slice merges.
- The evidence worktree contains a maintainer-owned dirty `GenerateCommand.cs` — untouchable.
- Do not recreate the pre-grill class-per-five-line-scenario pattern. The plan's inline
  scenario/promotion rule is binding; all test cases remain, only their setup housing changed.
- Do not reduce Task 1 to `IFileSystem` registration. The complete ToolApp hosting battery,
  MEL providers, global settings, interceptor, and shared test override path are slice scope.
- Deviation protocol applies; subagents never self-resolve level 2+.
- Delete this handover in the slice's docs-pass task once the PR merges.
