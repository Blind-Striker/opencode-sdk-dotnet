# Session Handoff - Arc 6 repairs and Q123 complete; measured performance follow-ups next

Date: 2026-08-21

Consume this against live Git and GitHub. This aggregated handoff supersedes and deletes
`HANDOFF-2026-08-20-10.md`. Repository files and current canon beat this handoff; research log
Session 38 (Q121/Q122) owns the benchmark-suite and review evidence, Session 39 Q123 owns the first
real net472 comparison, and this file owns only the remaining operational sequence.

## Live baseline

| Item | State |
|---|---|
| Remote baseline | `origin/master` = `0e1735e` (`test(perf): decompose benchmarks into component ladders with exact-byte and wire-size columns`) |
| Remote hosted CI | Run `32411438829` passed Linux, Windows, and macOS at `0e1735e`; earlier source run `32374393085` passed all three OSes at `fa6124d` |
| Local source commits | `3f68ddf` (`fix(sdk): harden response and union materialization`), then `713f09a` (`fix(sdk): cancel downlevel stream reads`) |
| This handoff commit | Research Q123, current ROADMAP state, and this replacement handoff; Markdown only |
| Branch | `master`; direct green commits remain authorized in this development loop |
| Push state | The repair commits and this documentation are local only; never push without an explicit ask |
| Final source gate | Slopwatch 0; Release build 0 warnings/errors; whitespace/style clean; 1,832 passed, 0 failed/skipped across Windows net472/net8/net9/net10 plus tool and Extensions tests |
| Generation | 15 selected / 105 pending; generated output, PublicApi, and manifest unchanged |
| Open GitHub issue | #53 remains deferred to M5; no issue was created for the bounded review repair queue |
| M4 | No launcher plan or source work has begun |

## Maintainer decisions carried

1. The accepted Arc 6 defects and test gaps had to close red-test-first before M4. They are now
   closed in the two local source commits above.
2. Public-surface compatibility was not a constraint for the repairs. Generated output still changes
   only through the generator, never by hand.
3. The performance follow-ups remain before M4 because the permanent ladders now make their costs
   directly measurable. Each behavior-neutral performance change is its own measured `perf(...)`
   increment.
4. Conventional Commits, no AI-attribution trailers, gates before every commit, and never push without
   an explicit ask.

## Completed this Windows session

### Increment 1 - `3f68ddf`

- R01: `UnionDiscriminatorReader` now uses `TrySkip`; consumer-side
  `DeserializeAsync`/`DeserializeAsyncEnumerable` tests cover known, unknown, nested fixed-marker,
  multi-item envelope, event, and trailing-padding partial-reader cases.
- R02: modern UTF-7 `NotSupportedException` maps onto `OpenCodeTransportException` on success and error
  planes; net472 retains decoded UTF-7 behavior.
- R17: stream-open error bodies consume the remaining `HttpClient.Timeout` budget.
- R10 option (a) and narrow R12: one sealed `ResponseBodyReader` owns the shared timed byte read and
  `ResponseEncodingPolicy` decodes success and error bodies on every TFM. Quoted charset behavior is
  uniform; the broader speculative Pipeline decomposition was not taken.
- R09/R13/R14/R20 tests now cover the encoding differential, span-null adapter overload, nested carrier
  constructor guard, and caller-token forwarding to continuation pages.
- Canon records partial-reader safety, one decoding policy, and stream-open error budgeting.
- Independent review found no defect. All five mandatory gates passed before commit; 1,825 total test
  executions passed at that point.

### Windows-only increment - `713f09a`

- R19 reproduced first on the real net472 `HttpClientHandler`: after one loopback SSE frame, cancelling
  an idle second `MoveNextAsync` remained pending past one second.
- The downlevel stream lifetime now registers cancellation without capturing `SynchronizationContext`
  and disposes the live `HttpResponseMessage`; disposal-induced I/O failures map back to
  `OperationCanceledException` through the caller token at stream acquisition and enumeration.
- The green loopback test observes both bounded cancellation and a real client TCP disconnect before
  test teardown can close the connection.
- R15 adds a real-handler stalled-body timeout test; net472 also observes the connection close.
- R02 net472 tests prove UTF-7 decoding on success and `NoThrow` error planes. The quoted UTF-8 success
  and error tests from increment 1 run on net472 too.
- Review found and closed one important issue (`CancellationToken.Register` synchronization-context
  capture) and three test/documentation findings. All five gates passed; 1,832 total executions passed.

### Q123 - first real net472 benchmark leg

- Source head `713f09a` was mirrored with `robocopy` to `C:\bench` outside the repository, excluding
  `.git`, scratch/upstream directories, build output, benchmark artifacts, and test results.
- BenchmarkDotNet 0.15.8 Dry and default jobs both completed 46 selected net472/net10 cases across
  `HealthBenchmarks`, `MessageGetBenchmarks`, and `ServerSentEventReaderBenchmarks`. Default took 47:03.
- Environment: Windows 11 `10.0.26200.9168`, Ryzen 9 5900X (12C/24T), SDK 10.0.303, .NET 10.0.11,
  concurrent workstation GC. The net472 binary ran on installed Framework 4.8.1 (`4.8.9337.0`).
- Sustained net472 SSE parsing is 2.76-2.88x slower with 1.06-1.12x allocation versus net10 in the same
  invocation. The large response-read row remains 2.00x wire on net10 and 2.01x on net472. Q123 in the
  research log owns every exact mean, byte count, ratio, warning, and limit.
- The run clears the Windows/net472 execution gate. It is not a cross-machine comparison and does not
  claim that the installed CLR is Framework 4.7.2.

## Closed and open review register

Closed: R01, R02, R09, R10(a), narrow R12, R13, R14, R15, R17, R19, and R20.

Still open by deliberate sequence:

- R18 performance: SSE reader per-character walk. The verified stage-1 prototype scans decoded spans
  with `IndexOfAny('\r', '\n')` and appends slices while preserving the strict decoder, BOM/CR split
  state, multiline semantics, frame limit, and public contracts. Session 38 measured the mechanism
  6.6x faster on the large parser arm and 2.4x on the small parser arm on Linux; Q123 supplies the real
  net472/net10 baseline.
- R16 performance: `ReadAsByteArrayAsync` buffers and copies the complete success body. The permanent
  large-message pipeline rows are still 2.00x/2.01x wire. Replace this with an SDK-owned pooled read
  while preserving timeout, cancellation, pending-task observation, content ownership, charset/BOM,
  malformed UTF-8 replacement, and all TFMs.
- R32 performance: generated marked-union converters retain a `Dictionary<string, Type>` lookup,
  `GetTypeInfo(Type)`, cast, and marker-string allocation. Emit typed string switches through the
  generator and update generated snapshots/output only through that path.
- Documentation: R04 ADR-0014 stale immutable-collection question, R05 ROADMAP history relocation,
  R06 duplicated Arc 6 decoding rule, R11 ADR-0009 date/duplication, plus the verified passthrough nits.

## Exact next increments

1. **R18 stage 1, contract-neutral SSE span scan.** Preserve every reader behavior; add/adjust tests only
   where a boundary is not already pinned. Dry first, then default-job
   `ServerSentEventReaderBenchmarks` and `SessionLogStreamBenchmarks` from a freshly mirrored clean copy.
   Compare against same-machine pre-change evidence, run the full gate, review, and commit one
   `perf(sdk): ...` increment.
2. **R16 pooled response-body read.** Design the cross-TFM owned buffer before editing; keep the current
   `ResponseBodyReader` timeout/cancellation/disposal contract. Gate on ownership tests and
   `MessageGetBenchmarks.ExecuteWithoutAdapterAsync` approaching 1.0x wire, with no materialization
   regression. Review, full gate, own `perf(sdk): ...` commit.
3. **Emitter typed-switch dispatch.** Change `tools/`, regenerate, inspect the complete generated diff,
   update emitter snapshots, and run tool-entry plus generation verification. Gate on
   `UnionDeserializationBenchmarks` interface/concrete and `log.synced` rows. Own measured `perf(...)`
   commit.
4. **Docs-only closure.** Resolve R04/R05/R06/R11 losslessly under documentation ownership rules and
   correct the SSE truncation/strict-decoder wording nits. Do not discard unique ROADMAP history before
   proving its durable owner.
5. Then create and align on the short M4 launcher plan. No launcher source before plan approval.

## Benchmark operation

- Run BenchmarkDotNet only from a clean copy outside the repository. `.scratchpad` or other duplicate
  project trees break project discovery.
- Windows mirror pattern:

```powershell
robocopy <repo> C:\bench /MIR /XD .git .scratchpad external bin obj BenchmarkDotNet.Artifacts TestResults
dotnet build tests/OpenCode.Sdk.Performance.Tests -c Release
```

- Dry before every default job. Use `dotnet run -f net10.0 -- --runtimes net472 net10.0 ...` when the
  downlevel comparison matters. Record exact allocated bytes and wire bytes; compare timing only within
  one environment.
- The Q123 clean copy and external artifacts are operational evidence only. Remirror after source changes;
  never reference temporary output from permanent project files.

## Session 38 originals

If a mechanism must be rechecked, the prior review output, agent transcripts, BDN full JSON, and probes
remain on dev-01 at `~/opencode-session38-artifacts/` (`ssh deniz@dev-01.sigil.denizirgin.com`). They are
temporary evidence, not repository dependencies. Q121/Q122 and current code own durable outcomes.

## M4 boundaries carried

Keep the launcher in `OpenCode.Sdk`, hand-written over `System.Diagnostics.Process` (ADR-0001). Do not
reopen M3 streams, pagination, transport, route, union, response, or collection decisions without
contradictory evidence. No M5 breadth/#53 walls, telemetry, retry, hooks, spec refresh, release, or MCP
work. A compile is not launcher lifecycle evidence; acceptance is a real process on Linux, Windows, and
macOS.

## Completion gates

```powershell
dotnet tool run slopwatch analyze --exclude ".scratchpad/**,external/**" --fail-on warning
dotnet build --configuration Release
dotnet format whitespace --verify-no-changes --no-restore
dotnet format style --verify-no-changes --no-restore --severity warn
dotnet test --configuration Release --no-build
```

Generator/curation/output changes also run `dotnet run --file tools/opencode-tool.cs -- --help`, the Unix
direct invocation where eligible, and `dotnet run --file tools/opencode-tool.cs -- generate --verify`.
Before every commit inspect status, every changed file, complete diff, PublicApi, manifest, and recent log.
Push only on explicit ask; after a push, watch Linux/Windows/macOS and update this handoff against live state.

## Ready-to-paste priming prompt

```text
Continue work on opencode-sdk-dotnet from
docs/agents/handover-prompts/HANDOFF-2026-08-21-11.md. Consume it against live Git/GitHub first.

Arc 6 correctness repairs are committed locally at 3f68ddf and 713f09a; origin/master remains 0e1735e
with hosted run 32411438829 green on Linux, Windows, and macOS. Q123's clean-copy Windows benchmark leg
completed both net472 and net10.0 and is recorded in research Session 39. Nothing has been pushed.

Exact next: R18 stage-1 SSE span scan with same-machine before/after benchmarks, then R16 pooled response
read, then generator-owned typed-switch union dispatch, then docs-only review cleanup, then the short M4
launcher plan. One measured green increment at a time, all gates before every commit, no AI trailers,
never push without an explicit ask. Generated output changes only through the generator; run BDN only
from a freshly mirrored clean copy outside the repo.
```
