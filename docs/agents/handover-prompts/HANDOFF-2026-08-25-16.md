# Session Handoff - public repo, reordered queue: emitter batch, then breadth ahead of the launcher

Date: 2026-08-25

Consume this against live Git, the worktree, and GitHub before changing anything. This handoff
supersedes and deletes `HANDOFF-2026-08-25-15.md`. Repository files and current canon beat this
handoff. Today's decisions live in research Q139 and `docs/ROADMAP.md`; the runtime arc's history
is Q126–Q138 and ADR-0018.

## Start here

The repository is **public** (made public 2026-08-25). The worktree is clean; a couple of local
docs commits may sit ahead of `origin/master` — verify against live Git; push only on the
maintainer's explicit ask. Two blockers are external and tracked, not worked:

- **CI is billing-blocked** — every GitHub Actions job dies at startup on the spending-limit
  annotation; the series since `9da0ae3` has no three-OS verdict. Issue #50 owns the follow-up
  (rerun latest master, verify three OSes, then the branch-protection decision). Secret scanning
  and push protection are already enabled.
- **The spec refresh is blocked upstream** — upstream's regenerated `openapi.json` lost the SSE
  payload schemas to the effect beta.107 bump; the pin stays `a6a712a3`. Q139 owns the evidence,
  [anomalyco/opencode#44911](https://github.com/anomalyco/opencode/issues/44911) carries the
  report plus a verified restore recipe, and #56 is the local tracker with unblock criteria. If
  upstream responds, the restore-PR offer stands; the mechanism is fully documented in Q139 and
  the issue (compile the payload unions with `Schema.toJsonSchemaDocument`, merge, re-attach
  `contentSchema` on the `*Encoded` envelopes).

## What Session 42 landed (2026-08-25)

- ROADMAP trued to the completed runtime arc; the executed runtime-pipeline plan stays in place
  and retires with the `superpowers/` sweep at M6 (maintainer-sealed).
- Q139: the upstream regression's cause chain (effect beta.102 #6424 / beta.103 #6781), the
  recoverability proof, and the supersession of Q137's stream-channel claim.
- Public-readiness cleanup: history verified trailer-free and **repo policy set — no AI
  attribution trailers or session URLs in commits or PR bodies**; README rewritten for public
  visitors (example verified against the PublicApi baseline); handoff scrubbed of machine paths;
  everything pushed; PR #14 body scrubbed.
- Issue cleanup: retired slice-map zombies #3–#12 closed (landed substance → completed;
  never-in-this-form → not planned); the 9 open issues are all milestone-anchored; #50 carries a
  current status comment.
- Queue reordered at the M3 boundary for the public repo (ROADMAP owns it); `.benchmarks/`
  created, git-ignored, and seeded with the full local artifact history (inc1–inc4 baselines,
  arc-milestone pairs, r16 patch, CSVs).

## Exact next

1. **Emitter allocation batch** on the current `a6a712a3` base. At its entry, build the agreed
   records tooling first: a file-based C# comparison app (the `tools/opencode-tool.cs` pattern)
   over the seeded `.benchmarks/` store, replacing the session-scratch extract script. Backlog:
   research doc 20's ranked table; the `FrozenDictionary` and `System.Collections.Immutable`
   decisions ride the batch. Benchmark cadence per Q131 (`--job short` targeted per increment,
   exact allocation columns as evidence; `.benchmarks/` is the artifact home now).
2. **M5 breadth batches pulled ahead of M4**, grown against the current pin and filtered by the
   Q137/Q139 drift map — never select the upstream-removed families (question flow,
   `projectCopy.*`, `health.stop`, `project.directories`). The **M4 launcher** rides alongside as
   a small independent arc (1–2 page plan, approval before source).
3. **Prerelease packaging track**: decision-first amendment of the partial-operation packing wall
   in `Directory.Build.targets` (prerelease packs allowed, stable packs stay blocked; rides
   ADR-0006/#51), then the ADR-0006 pipeline — per-merge GitHub Packages CD, manual NuGet.org
   lane. CI legs validate once billing restores.

## Onboarding order

1. `AGENTS.md`, `CONTEXT.md`, `docs/ROADMAP.md` (the reordered queue), this handoff.
2. Research Q126–Q139; ADR-0018; ADR-0006 for the packaging track.
3. `docs/engineering/` canon (quality-gates carries the benchmark cadence) and
   `docs/architecture/`.
4. For the emitter batch: research doc 20 (ranked backlog), doc 19 (feature catalog with
   verified downlevel codegen), the generator emitters under
   `tools/OpenCode.Sdk.Tools/Generator/Emission/`.

## Completion gates

Unchanged from `docs/engineering/quality-gates.md` (slopwatch, Release build, format
whitespace/style, tests; generator changes add the tool smoke and `generate --verify`).
Repo policy: no AI trailers in commit messages or PR bodies. Direct green commits stay
authorized in the development loop; never push without an explicit ask; canonical-document
edits only with explicit maintainer approval.

## Ready-to-paste priming prompt

```text
Start a session for opencode-sdk-dotnet from
docs/agents/handover-prompts/HANDOFF-2026-08-25-16.md. Consume it against live Git, the
worktree, and GitHub before changing anything.

The repository is public. The runtime-pipeline arc is complete and pushed; CI is
billing-blocked (#50) and the spec refresh is blocked upstream (#56 /
anomalyco/opencode#44911) - both external, tracked, and not to be worked. Work the reordered
queue: (1) the emitter allocation batch on the current a6a712a3 base, building the file-based
C# benchmark-comparison app over the seeded .benchmarks/ store first; (2) M5 breadth batches
pulled ahead of M4, filtered by the Q137/Q139 drift map, with the M4 launcher as a small side
arc; (3) the prerelease packaging track, decision-first on the partial-operation pack wall.
Repo policy: no AI trailers in commits or PR bodies. Direct green commits stay authorized in
the development loop; never push without an explicit ask; canonical-document edits only with
explicit maintainer approval.
```
