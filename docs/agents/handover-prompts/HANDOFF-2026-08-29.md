# Session Handoff — both arcs closed and hosted-proven; next: the persistentPty batch, full coverage, release prep

Date: 2026-08-29 (replaces HANDOFF-2026-08-28.md in place)

Consume this against live Git, `src/OpenCode.Sdk/.generation-incomplete`, and `docs/ROADMAP.md`
before changing anything. Repository files and current canon beat this handoff.

## Start here

- **Master is at `4cea8b5`** with everything below landed and pushed; CI status for that push is
  the first thing to check (`gh run list --limit 3`). The accepted snapshot is still **`d2ee536c`**.
- **Profile: 112 selected / 22 pending** of 134 operations, plus the hand-written `PtySession`
  WebSocket door.
- **The full local gate is green and slopwatch reports ZERO findings** — the residual ledger that
  ran all through 2026-08-28 is empty. 3,530 tests pass on all TFMs; `generate --verify` current.
- **`.generation-incomplete` is now the map of what is left.** Every pending operation carries a
  `[bindable]` or `[refused: <verbatim wall message>]` mark, produced by the real binding pipeline
  (S1, `f0e1677`). Read it first — it is more current than any prose, including this file.

## What landed since the previous handoff

1. **C2 envelope completion closed** — final whole-branch review (0 Critical / 0 Important on both
   axes; 2 MUST FIX / 7 FIX SOON / 9 ACCEPT triage), its two MUST FIX items landed (`4db95d1`),
   canon paragraph in `protocol-and-generation.md` (`f625c28`), research log Q154.
2. **The two residual SW004s adjudicated** — one *redesigned away* by awaiting upstream's
   `session.execution.interrupted` event instead of polling (`d6f653d`; upstream publishes it only
   after the interrupted fiber's finalizers settle), one kept as a documented
   impossibility-argument suppression (`abdfb28`). Maintainer rule reaffirmed: **no suppression
   without an explicit maintainer approval AND a good, verifiable reason.**
3. **M4's three-OS hosted proof is green** (run `33221482103`). One fix round was needed first: the
   CI "Detect slop" step lacked the canonical `--exclude ".scratchpad/**,external/**"` args
   (`4bf0c07`) — see the gotcha below, this class of drift is invisible locally.
4. **The no-wall sweep is complete** — 14 operations across two batches (`4e430a5..4a41e44`,
   `b89e972`), profile 98/36 → 112/22, plus the bindability telltale (`f0e1677`).
5. **Benchmark coverage batch** — the CSV completeness fix (`98e682a`), three new rungs
   (`b672120`, `aff51d4`, `1d133c5`), and a **full 192-case two-leg default-job run** archived at
   `.benchmarks/post-sweep-default`.
6. **Benchmark comparison tooling repaired at source** (`df0234f..4cea8b5`, review clean): wire
   figures now travel as BDN *metrics* into the JSON and the comparison; a zero-median case no
   longer aborts the whole run; zero-overlap now fails loudly naming both runtime label sets; the
   canonical invocation is documented in `quality-gates.md`.

## Measurement findings the next session should not re-derive

Full report: <https://claude.ai/code/artifact/c523b445-3e6a-4dea-a46a-97f5dd3e5231>

- **Timing on this workstation is not comparable across runs.** A control run of *identical*
  compiled code four hours apart returned byte-identical allocation on 96 of 96 cases while 87 of
  those same cases moved more than 10% on time (0.12× to 1.36×). Never draw a timing conclusion
  from two separate runs here; allocation is the reliable axis.
- **Allocation, baseline → now:** .NET 10.0 **−1,847,984 B** (48 improved / 21 same / 7 worse by
  ≤64 B); net472 **+56,615 B** (47 regressed), of which `MessageList` is 50,242 B, mostly the
  `page-480` fixture at ~33 B per item. **Unattributed** — a bisect across the spec refresh and the
  envelope arc is the named follow-up. Wire bytes were identical on all 76 matched cases per leg,
  so the comparison is like-for-like.
- **Allocation amplification is scale-invariant at ~5.8×** (2 KB, 54 KB, and 1 MB fixtures all sit
  there), and the transport is exonerated: `ExecuteWithoutAdapterAsync` allocates a flat 2,128 B
  across a 490× payload range. The whole factor belongs to deserialization.
- **A second cost class is fixed per-call buffers**, invisible on large payloads and brutal on
  small: `PtySession.ReadFramesAsync` 932× (16,776 B for an 18-byte frame — a per-call 16 KiB
  receive buffer), the SSE reader ~26 KB per stream open, `SessionActive.GetActiveAsync` 53× at one
  entry. The PtySession `ArrayPool` rent is the cheapest targeted win and already has its own rung.

## The next session's goals (maintainer, 2026-08-29)

### 1. The persistentPty HTTP batch — "new PTY endpoints"

`persistentPty` is **ordinary target surface**, not an excluded family (ROADMAP §"ExperimentalDeferred
was dropped; persistentPty is ordinary target surface"). ADR-0021 governs *normal* PTY (`v2.pty.*`),
not this family. Its HTTP batch has been queued for a while; the telltale now says most of it is
free:

| Operation | Mark | What it needs |
|---|---|---|
| `persistentPty.create` / `.get` / `.handoff` / `.remove` / `.shutdown` / `.update` | **[bindable]** | curation rows only — a group row plus selection, the 7a/7c pattern |
| `persistentPty.list` | refused: payload name not derivable (group does not pluralize naively) | one `envelopePayloadNames` curation row |
| `persistentPty.snapshot` | refused: `EncodedStringNode` unsupported by the M1 emitter | emitter mechanism |
| `persistentPty.connectToken` | refused: header parameter `x-opencode-ticket` has no runtime channel | a declared-header runtime channel — **precedent exists**: the hand-written PTY token door already applies this header internally (ADR-0021, `PtyClient`) |
| `persistentPty.connect` | refused: WebSocket unsupported | hand-written door, the `PtySession` precedent |

So seven of the ten are reachable with curation alone or near-trivial work; three need real
mechanism. Sequence the free seven first — that is a fast, visible coverage jump.

### 2. Coverage to full — the other twelve

Grouped by the mechanism that unblocks them, not by family:

- **Curation-only:** `config.get` (C# name collides with `Config.Model.CostEncoded` → a
  `schemaNames` row), `fs.list` (payload name → an `envelopePayloadNames` row).
- **Query-parameter mechanisms (3 ops):** `fs.find` and `vcs.diff` both refuse with "query
  parameter must be optional"; `session.stats` refuses on an unsupported query schema shape. One
  design touch likely clears all three.
- **Error-style mechanism (3 ops):** `worktree.create` / `.refresh` / `.remove` all refuse with
  "selected errors must use the Effect `_tag` style in M1".
- **Envelope mechanism (1):** `shell.output` — "location envelope 'data' must reference a named
  component schema, or be an array of one". **Closely adjacent to what C2 already built** for
  `vcs.branches` (ref-to-array resolution); read that arm before designing.
- **Emitter node support (1):** `persistentPty.snapshot` (`EncodedStringNode`).
- **Structural-union wall (1):** `experimental.migration.v1.status` — the known union-overlap wall,
  a design-first mechanism batch (maintainer chose option (a) on 2026-08-28: the wall stands).
- **Path mechanism (1):** `fs.read` — "wildcard paths are not supported in M1".
- **By-design, not defects (2):** `pty.connect` and `persistentPty.connect` are WebSocket upgrades
  that ADR-0008 excludes from generation; they earn hand-written doors, and every exclusion is
  fingerprint-pinned.

### 3. Release preparation

Untouched and maintainer-gated; nothing here has been decided:
- Packing-wall amendment (prerelease packs allowed, stable blocked while
  `.generation-incomplete` exists).
- Pre-1.0 versioning: `VersionPrefix`, `RELEASE_NOTES.md` discipline.
- GitHub Packages CD plus a manual NuGet.org lane (ADR-0006).

Note the coupling: the packing wall keys on generation completeness, so goals 1–2 and goal 3 meet.
Decide the wall's wording before the coverage work closes, or it will block its own release.

## Also queued (recorded in ROADMAP, do not re-derive)

- **Tools hygiene touch** — the wrapper-shape predicate duplicated across `SchemaNameResolver` and
  `EnvelopeFacetBinder` is the one with a real silent-drift hazard (a one-sided widening falls back
  to a pointer-derived name with **no wall to catch it**); plus two XML-doc one-liners, two unit
  rows, and the `AssertMissingDataFailsAsync` rename.
- **vcs.status contract top-up** — empty-list, location-query, protocol-failure tests.
- **PtySession `ArrayPool` rent** (see measurement findings) and the net472 allocation bisect.
- **Benchmark metric-id pin** — the diagnoser's descriptor ids and `BenchmarkRunReader`'s constants
  are duplicated strings in *different assemblies*; an emit-side rename fails **soft** to blank wire
  columns with every test still green. A shared constant or contract test closes it.
- Two curation/doc minors: the `form` group reason says "no per-id operations, ever" where siblings
  state present-tense fact; the `MedianNanoseconds` CSV column breaks the abbreviation convention.

## Standing facts and gotchas

- **Slopwatch walks the filesystem and does NOT consult `.gitignore`.** `node_modules/` is
  gitignored and was still scanned on CI. The canonical invocation
  (`--exclude ".scratchpad/**,external/**"`, `quality-gates.md`) is what protects the gate — CI and
  local must use the same line, or drift appears only on the hosted runner.
- **`external/` is a submodule, deliberately checked out on CI** (`submodules: true`) so `bun
  install` can build the pinned server. It is not ignored, and the runner creates its
  `node_modules` itself.
- **Never edit the performance project while a benchmark run is live.** BDN regenerates and
  rebuilds a project referencing it per job/runtime *during* the run; a mid-edit tree either breaks
  a later build or silently changes what is measured. Run from a clean copy, or leave the tree
  alone; keep the machine otherwise quiet.
- **The canonical benchmark invocation is now in `quality-gates.md`.** `--runtimes net10.0 net472`
  is load-bearing: without it BDN labels every case `DefaultJob`, and the comparison joins on the
  runtime label, so a whole run becomes unjoinable against the archive. (This cost a 1h48m run; the
  tool now fails loudly instead of printing an all-one-sided comparison.)
- **Editor/LSP diagnostics in this repo are stale and noisy** — routinely flagging generated members
  that exist. The gate is the referee, never the squiggles.
- **No backward compatibility until release** (Q131): renames are free; curation reasons must state
  facts that are true *today*, and several have gone stale as later work landed — check before
  citing one.
- Naming traps: the location query member is `workspace` (the body elsewhere uses `workspaceID`);
  the emitted vcs method is `GetBranchesAsync`, not `ListBranchesAsync`.
- `session.idle` is deprecated upstream with no publisher while `SessionIdle` stays in the event
  union — an upstream-report candidate parked with the other reports.

## Completion gates

Unchanged (`docs/engineering/quality-gates.md`): slopwatch, Release build, format whitespace/style,
tests; generator changes add the tool smoke and `generate --verify`; snapshot changes add
`refresh-spec --verify`. **Slopwatch is now at zero — keep it there.** Repo policy: no AI trailers;
direct green commits stay authorized in the development loop; **never push without an explicit
ask**; canonical-document edits only with explicit maintainer approval.

## Ready-to-paste priming prompt

```text
Start a session for opencode-sdk-dotnet from
docs/agents/handover-prompts/HANDOFF-2026-08-29.md. Consume it against live Git,
src/OpenCode.Sdk/.generation-incomplete (the pending map), and docs/ROADMAP.md before changing
anything.

State: master 4cea8b5 (pushed), pin d2ee536c, profile 112/22, slopwatch at ZERO, 3530 tests green.
Both the envelope-completion and launcher arcs are closed and hosted-proven.

Goals this session, in order: (1) the persistentPty HTTP batch — six operations are already
[bindable] and one needs a single curation row, so land those seven first, then judge the three
that need mechanism; (2) drive coverage toward full using the refusal marks as the map, grouping
by mechanism rather than by family; (3) open the release-prep track — packing-wall wording first,
since it keys on generation completeness and would otherwise block its own release.

Work subagent-driven with per-task reviews, foreground commands only. Repo policy: no AI trailers;
never push without my explicit ask; canonical edits need my approval.
```
