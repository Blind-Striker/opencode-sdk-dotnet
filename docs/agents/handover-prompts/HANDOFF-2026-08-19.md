# Session Handoff - post-CI-gate checkpoint; make stream-union evidence honest

Date: 2026-08-19

Consume this against live Git and GitHub. Issue #47 and the bounded CI analyzer/format gate pass are
complete, pushed, and hosted green. The exact next source increment is issue #49 pre-Arc-3b work.
Do not start typed stream failure causes, event-subscribe naming, or Arc 3b breadth inside #49.

## Live Baseline

| Item | State |
|---|---|
| Source head | `005030f` (`ci: split formatting and style gates`) |
| Stream-contract source | `2f48d11` (`fix(tools): close stream generation contracts`) |
| Branch | `master`, synchronized with `origin/master` before this docs-only checkpoint commit |
| Worktree | Expected clean after this docs-only checkpoint commit |
| Tests | 1,255 local modern-TFM executions, 0 failed, 0 skipped |
| Generation profile | 14 selected, 106 pending |
| Hosted CI | Run `32188680204` green on Linux, Windows, and macOS at `005030f` |
| Completed issue | #47 closed as completed |
| Carried issue | #27 remains open for first-occurrence and M5 walls |
| Exact next issue | #49, open and `ready-for-agent` |
| ADRs | 0001-0014 |
| Research | Through Session 31 / Q110 |

Direct green commits to `master` remain authorized for the development loop. Work one green source
increment at a time: red evidence, smallest implementation, complete diff/PublicApi/manifest review,
focused tests, one fresh-context read-only review, all applicable gates, commit, push, hosted
Linux/Windows/macOS, and owning-issue update before the next increment.

The main agent exclusively owns edits, generation, tests, measurements, commits, pushes, and GitHub
updates. Subagents are read-only reviewers/researchers. Do not use stacked PRs, implementation
worktrees, or parallel writers. `docs/superpowers/` is transient reference material; do not update
it or use it as operational authority.

## Reading Order

1. `AGENTS.md` and `docs/architecture/protocol-and-generation.md`.
2. ADR-0009, ADR-0011, ADR-0013, and ADR-0014.
3. `docs/engineering/coding-style.md`, `docs/engineering/testing-style.md`, and
   `docs/engineering/quality-gates.md`.
4. Research log Sessions 28-31, then `docs/ROADMAP.md`.
5. This handoff.
6. Issue #49 and its maintainer comment. The comment seals representative schema-valid runtime
   fixtures plus mechanical completeness; it rejects both a speculative minimal-JSON generator and
   40/87 hand-authored unchecked payloads.

Repository files and current canon beat this handoff. The pinned `spec/openapi.json` remains the
sole protocol-semantic input. Upstream source is diagnostic evidence only and cannot enrich fixture
or generator semantics (ADR-0013).

## Completed Checkpoint - #47 (`2f48d11`)

- Stream-frame `id`/`event`, SSE encoding, failure-event separation, and body refusal fail closed.
- Stream docs state API errors always throw and expose no per-call options.
- Generic bind -> emit -> source-generate -> compile evidence pins the stream method, route,
  cancellation, payload/failure metadata, stream-only namespace emission, and every status/tag.
- Selected `session.log` contract tests pin GET and typed 400/401/404 mappings.
- PublicApi and manifest are unchanged; only generated stream documentation changed.
- Local gates passed with 1,255 tests; hosted run `32178776110` passed all three OSes; #47 is closed.

## Completed Checkpoint - CI Gate Split (`005030f`)

- Build owns compiler, SDK CA, third-party analyzer, source-generator, and build-enforceable IDE
  diagnostics.
- Linux CI separately runs physical whitespace and unfiltered warning-level Roslyn style gates.
- The style gate has no diagnostic allow-list, preserving future warning/error rules, import
  organization, and current `EnforceOnBuild.Never` style errors.
- Repository generation remains a separate mutating path and keeps project-scoped full format over
  only generated paths with `TargetFramework=net10.0`.
- Controlled probes, official/current source evidence, analyzer inventory, local timings, and the
  accepted decision are recorded in research Q110 and the related Memorizer references.
- Hosted run `32188680204` passed all three OSes. Linux whitespace/style took 0:17/1:34 (1:51 total)
  versus the previous 3:00 full solution lint pass. Build remains the matrix bottleneck.

## Exact Next Increment - #49

Title: **M3 decision: make stream-union fixtures and coverage claims honest**.

Issue #49 is pre-Arc-3b evidence work. Its sealed policy is **representative schema-valid runtime
fixtures plus mechanical completeness**.

### Required behavior

- Fix central known-message fixture identifiers from invalid `message-*` values to the pinned
  `msg_` form. Update their exact assertions with them.
- Keep runtime fixtures representative: currently two of 40 durable event branches are observed in
  tests, plus the `log.synced` watermark and unknown-carrier behavior. State that count honestly.
- Mechanically prove every bound union converter tag maps to its declared variant type.
- Mechanically prove every converter-required payload/unknown type is present in the single
  source-generated JSON registry.
- Carry the proof through bind -> emit -> source generation -> compile. Runtime fixtures exercise a
  small schema-valid corpus; they do not impersonate exhaustive branch evidence.
- Preserve ADR-0009 unknown-variant behavior, reflection-disabled source generation, and current
  stream framing/runtime controls.

### Current invalid fixture targets

- `tests/OpenCode.Sdk.Tests/Fixtures/Serialization/known-session-message.json` (`message-1`)
- `tests/OpenCode.Sdk.Tests/Fixtures/Serialization/unknown-session-message.json` (`message-2`)
- `tests/OpenCode.Sdk.Tests/Fixtures/Serialization/null-optional-collection.json` (`message-3`)
- `tests/OpenCode.Sdk.Tests/Fixtures/Serialization/known-compaction-message.json` (`message-4`)
- Assertions in `tests/OpenCode.Sdk.Tests/Serialization/OpenCodeJsonContextTests.cs` and
  `tests/OpenCode.Sdk.Tests/OpenCodeClientContractTests.cs`

Search all fixtures and assertions again before editing; the list above is a current census, not a
license to stop after four files.

### Mechanical-evidence audit targets

- `tools/OpenCode.Sdk.Tools/Generator/Binding/SchemaPlanBinder.cs`
- `tools/OpenCode.Sdk.Tools/Generator/Emission/UnionEmitter.cs`
- `tools/OpenCode.Sdk.Tools/Generator/Emission/RegistryEmitter.cs`
- `tools/OpenCode.Sdk.Tools/Generator/Emission/SourceEmitter.cs`
- `tests/OpenCode.Sdk.Tools.Tests/Generator/Binding/SpecBinderTests.cs`
- `tests/OpenCode.Sdk.Tools.Tests/Generator/Emission/UnionEmitterTests.cs`
- `tests/OpenCode.Sdk.Tools.Tests/Generator/Emission/RegistryEmitterTests.cs`
- `tests/OpenCode.Sdk.Tools.Tests/Generator/Emission/SourceEmitterTests.cs`
- `tests/OpenCode.Sdk.Tests/Serialization/OpenCodeJsonContextTests.cs`
- `tests/OpenCode.Sdk.Tests/Sessions/SessionLogContractTests.cs`

### Red evidence

- Central known-message fixtures and assertions use valid `msg_` identifiers.
- For each selected/bound union, converter tags and variant type names are complete and exact.
- Every converter-dispatched and unknown-carrier type appears in `RegistryPlan` and emitted
  `OpenCodeJsonContext` metadata.
- Fresh emitted source compiles with reflection disabled.
- Roadmap/runtime test comments never claim 40/87 runtime payload coverage from two observed durable
  branch fixtures.

### Deferred by the sealed decision

- Do not generate minimal JSON instances from every schema.
- Do not hand-author 40 durable or 87 live-event payloads.
- Do not claim runtime observation from plan/source inspection.
- Do not complete plural durable/live interface materialization yet. Its bind -> emit -> compile ->
  deserialize proof lands with Arc 3b when one concrete selected leaf belongs to both generated
  sibling interfaces.
- Do not select `v2.event.subscribe`, add #44 operation-name curation, or pull #53 cause-schema work
  into this increment.

### Expected review surface

- Four central JSON fixture identifier corrections and their exact assertions.
- Generic/pinned mechanical converter and registry completeness evidence.
- Compile/source-generation coverage needed by that evidence.
- Roadmap or test-comment wording only where a claim is inaccurate.
- No broad PublicApi signature change, generation-profile expansion, runtime validator, reflection
  path, or generated model rewrite. Any such diff is a stop condition requiring explanation.

## Queue After #49

Do not start these inside #49:

1. Typed stream failure cause (#53 M3 subset): admit only the schema-applicator mechanisms the pinned
   `x-effect-stream.causeSchema` needs, then replace text-only failure carrying with the declared
   typed cause.
2. Arc 3a deliverable closure: Generic Host `SessionClient.GetLogAsync` example, end-to-end
   pipeline/reader/adapter benchmark, and a precise repeat live demonstration.
3. Arc 3b: #44 operation-name curation plus generated `Events.SubscribeAsync`, live union breadth
   over proven mechanisms, plural-interface leaf evidence, and the live global-bus demo.

Arc 3b does not open until #49 pre-work, typed failure causes, and Arc 3a deliverable closure are
green and hosted.

## Completion Gates

Use the current canonical gate:

```bash
dotnet tool run slopwatch analyze --exclude ".scratchpad/**,external/**" --fail-on warning
dotnet build --configuration Release
dotnet format whitespace --verify-no-changes --no-restore
dotnet format style --verify-no-changes --no-restore --severity warn
dotnet test --configuration Release --no-build
```

Protocol/generator/generated-output changes also run:

```bash
dotnet run --file tools/opencode-tool.cs -- --help
./tools/opencode-tool.cs --help
dotnet run --file tools/opencode-tool.cs -- generate --verify
```

Before committing, inspect status, every changed/new/deleted file, the complete diff, PublicApi,
manifest, and recent log. Stage only #49. Push directly to `master`, wait for Linux/Windows/macOS,
update #49 with its completed pre-work evidence, leave it open for the deferred Arc 3b
plural-interface closure, and checkpoint before the next item.

## Ready-To-Paste Priming Prompt

```text
Continue work on /home/deniz/src/opencode-sdk-dotnet from
docs/agents/handover-prompts/HANDOFF-2026-08-19.md.

Consume it against live Git and GitHub before touching files. Expected source head is 005030f; hosted
run 32188680204 is green on Linux, Windows, and macOS. #47 and the CI analyzer/format gate split are
complete. The exact next source increment is #49 pre-Arc-3b evidence under its sealed maintainer
decision: representative schema-valid runtime fixtures plus mechanical converter/registry
completeness.

Work red-test-first. Fix invalid central message-* fixture IDs to the pinned msg_ form, prove every
bound converter tag and required source-generated registry type mechanically through bind -> emit ->
source generation -> compile, and keep runtime coverage claims honest. Do not generate or hand-write
40/87 payload corpora. Defer plural durable/live interface deserialization until Arc 3b supplies a
selected shared leaf. Do not start #53, #44, event.subscribe, or Arc 3b breadth inside #49.
```

Delete this handoff when #49 ships or when a newer handoff supersedes it.
