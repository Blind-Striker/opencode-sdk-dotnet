# Handover Prompts

Date: 2026-08-18

Session handover documents live here, named `HANDOFF-<date>[-<n>].md`. Lifecycle is canonical in
`../../engineering/documentation.md`: consume them against live git, then delete them when the
follow-up ships or a newer handoff supersedes them. An empty directory is the healthy state.
