# Handover: codegen spike done → grill session next

Date: 2026-08-08

Consume against live git; delete when the grill session ships its outputs.

## What happened this session

Queue item 1 (codegen spike) completed and documented; two follow-up decisions sealed by the
maintainer along the way. Canonical records: `docs/research/08-codegen-spike.md` (spike:
mechanism/emission/packaging + evidence), `docs/research/09-upstream-v1v2.md` (product-vs-API
versioning, 2.0 outlook), research log Q19–Q24, updated `AGENTS.md` Locked Decisions
(hybrid/Roslyn emission, generator packaging, generation principles, both-surfaces API
decision, `.scratchpad/` rule) and `docs/ROADMAP.md` (queue advanced, versioning question
narrowed to independent-semver).

## Next session: grill (`grilling` / `grill-with-docs` against `AGENTS.md` + docs)

Spike evidence is in hand — docs 08 and 09 are the primary exhibits. ADR candidates and their
ready-made reversal triggers are listed in ROADMAP queue item 1 and doc 08 § Decisions.
Grill-worthy tension points an interrogator should push on:

- **Both-surfaces decision vs upstream churn:** legacy dies in 2.0 and the transitional
  `v2.*` block renames under us (only 15/61 names survive) — is "generate both, evolve at our
  major" robust, and is best-effort legacy testing honest given the MCP server will lean on
  legacy ops for capability?
- **Roslyn emission** was chosen against the slice's cost evidence (maintainer: semantic
  construction wins at full-generator scale) — the ADR must record both sides.
- **`.g.cs` + generated-code exemption vs on-merit style conformance** (doc 08 quantifies the
  186-diagnostic gap) — which posture does the repo adopt?
- **Unknown-discriminator forward compatibility** for SSE unions (STJ throws by default) —
  parked for API design; the grill should confirm it stays parked.
- Downlevel (ns2.0/net472) compile of the generated model layer is UNTESTED — ROADMAP net472
  spike item.

## Local-only context (not required, this machine only)

The gitignored `.scratchpad/` on the authoring machine holds the raw spike artifacts (three
benchmark-tool runs with FINDINGS + logs, the two own-generator slice emitters with strict
harnesses and smoke test, tool survey + packaging-axis notes). Everything decision-relevant
was folded into docs 08/09; the scratchpad is corroborating raw material only.
