// Generated by OpenCode.Sdk.Tools from spec/openapi.json.
// Do not edit by hand — change tools/curation.json or the emitters, then regenerate.
using System.Diagnostics.CodeAnalysis;
using System.Text;
using OpenCode.Sdk.Models;

namespace OpenCode.Sdk;
/// <summary>
/// Represents the response of the &apos;GET /api/model/default&apos; operation.
/// </summary>
public sealed record ModelDefaultResponse : OpenCodeResponse
{
    private readonly ModelInfo? _default;
    private readonly LocationInfo? _location;
    /// <summary>
    /// Initializes a success instance of the &apos;ModelDefaultResponse&apos; envelope.
    /// </summary>
    public ModelDefaultResponse()
    {
    }

    /// <summary>
    /// Initializes an error-path instance; the payload stays unset behind its guard.
    /// </summary>
    [SetsRequiredMembers]
    internal ModelDefaultResponse(int status, IOpenCodeError? error, string? rawBody)
    {
        Status = status;
        IsError = true;
        Error = error;
        RawBody = rawBody;
        Default = null;
        Location = null!;
    }

    /// <summary>
    /// Gets the Default payload; guarded on the error path.
    /// </summary>
    public required ModelInfo? Default { get => !IsError ? _default : throw new InvalidOperationException("The response is an error; check IsError before accessing Default."); init => _default = value; }
    /// <summary>
    /// Gets the location the server resolved for the request; guarded on the error path.
    /// </summary>
    public required LocationInfo Location { get => _location ?? throw new InvalidOperationException("The response is an error; check IsError before accessing Location."); init => _location = value; }

    /// <summary>
    /// Prints the shared metadata and appends the payload only when it is present.
    /// </summary>
    protected override bool PrintMembers(StringBuilder builder)
    {
        ArgumentNullException.ThrowIfNull(builder);
        var printed = base.PrintMembers(builder);
        if (IsError)
        {
            return printed;
        }

        if (printed)
        {
            _ = builder.Append(", ");
        }

        _ = builder.Append("Default = ").Append(_default);
        return true;
    }
}
