// Generated by OpenCode.Sdk.Tools from spec/openapi.json.
// Do not edit by hand — change tools/curation.json or the emitters, then regenerate.
using OpenCode.Sdk.Internal.Serialization;

namespace OpenCode.Sdk.Internal.ResponseAdapters;
/// <summary>
/// Adapts the &apos;GET /api/shell&apos; responses onto &apos;ShellListResponse&apos;.
/// </summary>
internal sealed class ShellListResponseAdapter : ResponseAdapter<ShellListResponse>
{
    private static readonly string[] Status400Tags = ["InvalidRequestError"];
    private static readonly string[] Status401Tags = ["UnauthorizedError"];
    private ShellListResponseAdapter()
    {
    }

    /// <summary>
    /// Gets the shared adapter instance.
    /// </summary>
    public static ShellListResponseAdapter Instance { get; } = new ShellListResponseAdapter();
    /// <summary>
    /// Gets the declared success status.
    /// </summary>
    public override int SuccessStatusCode => 200;

    /// <summary>
    /// Maps the declared UTF-8 success body onto the typed envelope.
    /// </summary>
    public override ShellListResponse AdaptSuccess(int status, ReadOnlySpan<byte> utf8Body) => CreateSuccess(status, ReadBarePayload(utf8Body, OpenCodeJsonContext.Default.ShellListResponseEnvelope));
    /// <summary>
    /// Maps one buffered response onto the typed envelope.
    /// </summary>
    public override ShellListResponse Adapt(int status, string rawBody)
    {
        ArgumentNullException.ThrowIfNull(rawBody);
        return status switch
        {
            200 => CreateSuccess(status, ReadBarePayload(rawBody, OpenCodeJsonContext.Default.ShellListResponseEnvelope)),
            >= 200 and < 300 => throw UndeclaredSuccessFailure(status),
            400 => new ShellListResponse(status, ReadTolerantError(rawBody, Status400Tags), rawBody),
            401 => new ShellListResponse(status, ReadTolerantError(rawBody, Status401Tags), rawBody),
            _ => new ShellListResponse(status, ReadTolerantError(rawBody, null), rawBody)
        };
    }

    private static ShellListResponse CreateSuccess(int status, ShellListResponseEnvelope envelope) => new()
    {
        Status = status,
        Shells = envelope.Data,
        Location = envelope.Location
    };
}
