// Generated by OpenCode.Sdk.Tools from spec/openapi.json.
// Do not edit by hand — change tools/curation.json or the emitters, then regenerate.
using System.Text.Json;
using System.Text.Json.Serialization;
using OpenCode.Sdk.Models;

namespace OpenCode.Sdk.Internal.Serialization;

internal sealed class SessionStatusJsonConverter : JsonConverter<ISessionStatus>
{
    private static readonly Dictionary<string, Type> TypesByTag = new(StringComparer.Ordinal)
    {
        ["busy"] = typeof(SessionStatusBusy),
        ["idle"] = typeof(SessionStatusIdle),
        ["retry"] = typeof(SessionStatusRetry)
    };
    private static readonly UnionDiscriminatorReader DiscriminatorReader = new();
    public override ISessionStatus Read(ref Utf8JsonReader reader, Type typeToConvert, JsonSerializerOptions options)
    {
        ArgumentNullException.ThrowIfNull(typeToConvert);
        ArgumentNullException.ThrowIfNull(options);
        var marker = DiscriminatorReader.ReadString(ref reader, "type", "SessionStatus");
        if (TypesByTag.TryGetValue(marker, out var targetType))
        {
            var typeInfo = OpenCodeJsonContext.Default.GetTypeInfo(targetType) ?? throw new JsonException("The generated context has no metadata for SessionStatus.");
            return JsonSerializer.Deserialize(ref reader, typeInfo) as ISessionStatus ?? throw new JsonException("The SessionStatus payload deserialized to null.");
        }

        using var document = JsonDocument.ParseValue(ref reader);
        var payload = document.RootElement;
        return new UnknownSessionStatus(marker, payload);
    }

    public override void Write(Utf8JsonWriter writer, ISessionStatus value, JsonSerializerOptions options)
    {
        ArgumentNullException.ThrowIfNull(writer);
        ArgumentNullException.ThrowIfNull(value);
        ArgumentNullException.ThrowIfNull(options);
        if (value is UnknownSessionStatus unknown)
        {
            unknown.Payload.WriteTo(writer);
            return;
        }

        var typeInfo = OpenCodeJsonContext.Default.GetTypeInfo(value.GetType()) ?? throw new JsonException("The generated context has no metadata for SessionStatus.");
        JsonSerializer.Serialize(writer, value, typeInfo);
    }
}
