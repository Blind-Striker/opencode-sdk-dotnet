// Generated by OpenCode.Sdk.Tools from spec/openapi.json.
// Do not edit by hand — change tools/curation.json or the emitters, then regenerate.
using System.Text.Json;
using System.Text.Json.Serialization;
using OpenCode.Sdk.Models;

namespace OpenCode.Sdk.Internal.Serialization;

internal sealed class SessionInboxItemJsonConverter : JsonConverter<ISessionInboxItem>
{
    private static readonly Dictionary<string, Type> TypesByTag = new(StringComparer.Ordinal)
    {
        ["compaction"] = typeof(SessionInboxItemCompaction),
        ["move"] = typeof(SessionInboxItemMove),
        ["synthetic"] = typeof(SessionInboxItemSynthetic),
        ["user"] = typeof(SessionInboxItemUser)
    };
    public override ISessionInboxItem Read(ref Utf8JsonReader reader, Type typeToConvert, JsonSerializerOptions options)
    {
        ArgumentNullException.ThrowIfNull(typeToConvert);
        ArgumentNullException.ThrowIfNull(options);
        using var document = JsonDocument.ParseValue(ref reader);
        var payload = document.RootElement;
        if (payload.ValueKind != JsonValueKind.Object)
        {
            throw new JsonException("The SessionInboxItem payload must be a JSON object.");
        }

        if (!payload.TryGetProperty("type", out var markerElement))
        {
            throw new JsonException("The SessionInboxItem payload must contain 'type'.");
        }

        if (markerElement.ValueKind != JsonValueKind.String)
        {
            throw new JsonException("The 'type' marker must be a string.");
        }

        var marker = markerElement.GetString();
        if (marker is null || string.IsNullOrWhiteSpace(marker))
        {
            throw new JsonException("The 'type' marker must be a non-empty string.");
        }

        if (TypesByTag.TryGetValue(marker, out var targetType))
        {
            var typeInfo = OpenCodeJsonContext.Default.GetTypeInfo(targetType) ?? throw new JsonException("The generated context has no metadata for SessionInboxItem.");
            return JsonSerializer.Deserialize(payload, typeInfo) as ISessionInboxItem ?? throw new JsonException("The SessionInboxItem payload deserialized to null.");
        }

        return new UnknownSessionInboxItem(marker, payload);
    }

    public override void Write(Utf8JsonWriter writer, ISessionInboxItem value, JsonSerializerOptions options)
    {
        ArgumentNullException.ThrowIfNull(writer);
        ArgumentNullException.ThrowIfNull(value);
        ArgumentNullException.ThrowIfNull(options);
        if (value is UnknownSessionInboxItem unknown)
        {
            unknown.Payload.WriteTo(writer);
            return;
        }

        var typeInfo = OpenCodeJsonContext.Default.GetTypeInfo(value.GetType()) ?? throw new JsonException("The generated context has no metadata for SessionInboxItem.");
        JsonSerializer.Serialize(writer, value, typeInfo);
    }
}
