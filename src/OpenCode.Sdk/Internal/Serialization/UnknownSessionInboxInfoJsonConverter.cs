// Generated by OpenCode.Sdk.Tools from spec/openapi.json.
// Do not edit by hand — change tools/curation.json or the emitters, then regenerate.
using System.Text.Json;
using System.Text.Json.Serialization;
using OpenCode.Sdk.Models;

namespace OpenCode.Sdk.Internal.Serialization;

internal sealed class UnknownSessionInboxInfoJsonConverter : JsonConverter<UnknownSessionInboxInfo>
{
    public override UnknownSessionInboxInfo Read(ref Utf8JsonReader reader, Type typeToConvert, JsonSerializerOptions options)
    {
        ArgumentNullException.ThrowIfNull(typeToConvert);
        ArgumentNullException.ThrowIfNull(options);
        using var document = JsonDocument.ParseValue(ref reader);
        var payload = document.RootElement;
        if (payload.ValueKind != JsonValueKind.Object)
        {
            throw new JsonException("The SessionInboxInfo payload must be a JSON object.");
        }

        if (!payload.TryGetProperty("type", out var markerElement))
        {
            throw new JsonException("The SessionInboxInfo payload must contain 'type'.");
        }

        if (markerElement.ValueKind != JsonValueKind.String)
        {
            throw new JsonException("The 'type' marker must be a string.");
        }

        var marker = markerElement.GetString();
        if (marker is null || string.IsNullOrWhiteSpace(marker))
        {
            throw new JsonException("The 'type' marker must be a non-empty string.");
        }

        return new UnknownSessionInboxInfo(marker, payload);
    }

    public override void Write(Utf8JsonWriter writer, UnknownSessionInboxInfo value, JsonSerializerOptions options)
    {
        ArgumentNullException.ThrowIfNull(writer);
        ArgumentNullException.ThrowIfNull(value);
        ArgumentNullException.ThrowIfNull(options);
        value.Payload.WriteTo(writer);
    }
}
