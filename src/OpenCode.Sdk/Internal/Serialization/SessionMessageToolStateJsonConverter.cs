// Generated by OpenCode.Sdk.Tools from spec/openapi.json.
// Do not edit by hand — change tools/curation.json or the emitters, then regenerate.
using System.Text.Json;
using System.Text.Json.Serialization;
using OpenCode.Sdk.Models;

namespace OpenCode.Sdk.Internal.Serialization;

internal sealed class SessionMessageToolStateJsonConverter : JsonConverter<SessionMessageToolState>
{
    private static readonly Dictionary<string, Type> TypesByTag = new(StringComparer.Ordinal)
    {
        ["completed"] = typeof(SessionMessageToolStateCompleted),
        ["error"] = typeof(SessionMessageToolStateError),
        ["running"] = typeof(SessionMessageToolStateRunning),
        ["streaming"] = typeof(SessionMessageToolStateStreaming)
    };
    public override bool HandleNull => true;

    public override SessionMessageToolState Read(ref Utf8JsonReader reader, Type typeToConvert, JsonSerializerOptions options)
    {
        ArgumentNullException.ThrowIfNull(typeToConvert);
        ArgumentNullException.ThrowIfNull(options);
        if (reader.TokenType == JsonTokenType.Null)
        {
            throw new JsonException("The SessionMessageToolState payload cannot be null.");
        }

        using var document = JsonDocument.ParseValue(ref reader);
        var payload = document.RootElement;
        if (payload.ValueKind != JsonValueKind.Object)
        {
            throw new JsonException("The SessionMessageToolState payload must be a JSON object.");
        }

        if (!payload.TryGetProperty("status", out var markerElement))
        {
            throw new JsonException("The SessionMessageToolState payload must contain 'status'.");
        }

        if (markerElement.ValueKind != JsonValueKind.String)
        {
            throw new JsonException("The 'status' marker must be a string.");
        }

        var marker = markerElement.GetString() ?? throw new JsonException("The 'status' marker cannot be null.");
        if (TypesByTag.TryGetValue(marker, out var targetType))
        {
            var typeInfo = OpenCodeJsonContext.Default.GetTypeInfo(targetType) ?? throw new JsonException("The generated context has no metadata for SessionMessageToolState.");
            return JsonSerializer.Deserialize(payload, typeInfo) as SessionMessageToolState ?? throw new JsonException("The SessionMessageToolState payload deserialized to null.");
        }

        return new UnknownSessionMessageToolState(marker, payload);
    }

    public override void Write(Utf8JsonWriter writer, SessionMessageToolState value, JsonSerializerOptions options)
    {
        ArgumentNullException.ThrowIfNull(writer);
        ArgumentNullException.ThrowIfNull(value);
        ArgumentNullException.ThrowIfNull(options);
        if (value is UnknownSessionMessageToolState unknown)
        {
            unknown.Payload.WriteTo(writer);
            return;
        }

        var typeInfo = OpenCodeJsonContext.Default.GetTypeInfo(value.GetType()) ?? throw new JsonException("The generated context has no metadata for SessionMessageToolState.");
        JsonSerializer.Serialize(writer, value, typeInfo);
    }
}
