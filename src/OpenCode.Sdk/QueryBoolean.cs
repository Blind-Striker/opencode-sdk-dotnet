namespace OpenCode.Sdk;

/// <summary>Defines a boolean query whose declared wire tokens are the strings true and false.</summary>
public enum QueryBoolean
{
    /// <summary>Writes the string token true.</summary>
    True = 0,

    /// <summary>Writes the string token false.</summary>
    False = 1,
}
