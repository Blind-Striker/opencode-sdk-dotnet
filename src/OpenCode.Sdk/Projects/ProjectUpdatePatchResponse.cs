// Generated by OpenCode.Sdk.Tools from spec/openapi.json.
// Do not edit by hand — change tools/curation.json or the emitters, then regenerate.
using System.Diagnostics.CodeAnalysis;
using System.Text;
using OpenCode.Sdk.Models;

namespace OpenCode.Sdk;
/// <summary>
/// Represents the response of the &apos;PATCH /api/project/{projectID}&apos; operation.
/// </summary>
public sealed record ProjectUpdatePatchResponse : OpenCodeResponse
{
    private readonly Project? _update;
    /// <summary>
    /// Initializes a success instance of the &apos;ProjectUpdatePatchResponse&apos; envelope.
    /// </summary>
    public ProjectUpdatePatchResponse()
    {
    }

    /// <summary>
    /// Initializes an error-path instance; the payload stays unset behind its guard.
    /// </summary>
    [SetsRequiredMembers]
    internal ProjectUpdatePatchResponse(int status, IOpenCodeError? error, string? rawBody)
    {
        Status = status;
        IsError = true;
        Error = error;
        RawBody = rawBody;
        Update = null!;
    }

    /// <summary>
    /// Gets the Update payload; guarded on the error path.
    /// </summary>
    public required Project Update { get => _update ?? throw new InvalidOperationException("The response is an error; check IsError before accessing Update."); init => _update = value; }

    /// <summary>
    /// Prints the shared metadata and appends the payload only when it is present.
    /// </summary>
    protected override bool PrintMembers(StringBuilder builder)
    {
        ArgumentNullException.ThrowIfNull(builder);
        var printed = base.PrintMembers(builder);
        if (_update is null)
        {
            return printed;
        }

        if (printed)
        {
            _ = builder.Append(", ");
        }

        _ = builder.Append("Update = ").Append(_update);
        return true;
    }
}
