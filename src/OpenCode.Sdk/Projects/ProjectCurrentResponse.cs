// Generated by OpenCode.Sdk.Tools from spec/openapi.json.
// Do not edit by hand — change tools/curation.json or the emitters, then regenerate.
using System.Diagnostics.CodeAnalysis;
using System.Text;
using OpenCode.Sdk.Models;

namespace OpenCode.Sdk;
/// <summary>
/// Represents the response of the &apos;GET /api/project/current&apos; operation.
/// </summary>
public sealed record ProjectCurrentResponse : OpenCodeResponse
{
    private readonly ProjectCurrent? _current;
    /// <summary>
    /// Initializes a success instance of the &apos;ProjectCurrentResponse&apos; envelope.
    /// </summary>
    public ProjectCurrentResponse()
    {
    }

    /// <summary>
    /// Initializes an error-path instance; the payload stays unset behind its guard.
    /// </summary>
    [SetsRequiredMembers]
    internal ProjectCurrentResponse(int status, IOpenCodeError? error, string? rawBody)
    {
        Status = status;
        IsError = true;
        Error = error;
        RawBody = rawBody;
        Current = null!;
    }

    /// <summary>
    /// Gets the Current payload; guarded on the error path.
    /// </summary>
    public required ProjectCurrent Current { get => _current ?? throw new InvalidOperationException("The response is an error; check IsError before accessing Current."); init => _current = value; }

    /// <summary>
    /// Prints the shared metadata and appends the payload only when it is present.
    /// </summary>
    protected override bool PrintMembers(StringBuilder builder)
    {
        ArgumentNullException.ThrowIfNull(builder);
        var printed = base.PrintMembers(builder);
        if (_current is null)
        {
            return printed;
        }

        if (printed)
        {
            _ = builder.Append(", ");
        }

        _ = builder.Append("Current = ").Append(_current);
        return true;
    }
}
