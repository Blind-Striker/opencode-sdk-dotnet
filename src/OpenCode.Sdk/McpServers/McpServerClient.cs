// Generated by OpenCode.Sdk.Tools from spec/openapi.json.
// Do not edit by hand — change tools/curation.json or the emitters, then regenerate.
using OpenCode.Sdk.Internal;
using OpenCode.Sdk.Internal.ResponseAdapters;

namespace OpenCode.Sdk;
/// <summary>
/// A bound &apos;McpServerClient&apos; handle; it holds an immutable identifier and the shared pipeline.
/// </summary>
public class McpServerClient
{
    private readonly Pipeline? _pipeline;
    private readonly string? _server;
    internal McpServerClient(Pipeline pipeline, string server)
    {
        ArgumentNullException.ThrowIfNull(pipeline);
        ArgumentException.ThrowIfNullOrWhiteSpace(server);
        if (server is "." or "..")
        {
            throw new ArgumentException("Route values must not be dot segments.", nameof(server));
        }

        _pipeline = pipeline;
        _server = server;
    }

    /// <summary>
    /// Initializes a mocking instance; members invoked without an override throw an instructive failure.
    /// </summary>
    protected McpServerClient()
    {
    }

    private Pipeline Pipeline => _pipeline ?? throw MockSeam.CreateError("McpServerClient", "Pipeline");
    private string Server => _server ?? throw MockSeam.CreateError("McpServerClient", "Server");

    /// <summary>
    /// Remove MCP server. Stop an MCP server and remove it from the runtime set until restart.
    /// </summary>
    /// <param name = "request">The request shaping the query.</param>
    /// <param name = "requestOptions">The per-call options.</param>
    /// <param name = "cancellationToken">The cancellation token.</param>
    /// <returns>The &apos;McpRemoveResponse&apos; envelope.</returns>
    /// <exception cref = "OpenCodeApiException">The API returned an error status (declared: 400, 401, 404) and NoThrow was not selected.</exception>
    /// <exception cref = "OpenCodeTransportException">The server could not be reached or returned a malformed success body.</exception>
    public virtual Task<McpRemoveResponse> RemoveMcpServerAsync(McpRemoveRequest? request = null, OpenCodeRequestOptions? requestOptions = null, CancellationToken cancellationToken = default)
    {
        return Pipeline.ExecuteAsync(HttpMethod.Delete, OpenCodeRoutes.McpServers.RemoveMcpServer(Server, request), McpRemoveResponseAdapter.Instance, requestOptions, cancellationToken);
    }
}
