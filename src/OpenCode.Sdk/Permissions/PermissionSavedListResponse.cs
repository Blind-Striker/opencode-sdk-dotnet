// Generated by OpenCode.Sdk.Tools from spec/openapi.json.
// Do not edit by hand — change tools/curation.json or the emitters, then regenerate.
using System.Diagnostics.CodeAnalysis;
using System.Text;
using OpenCode.Sdk.Models;

namespace OpenCode.Sdk;
/// <summary>
/// Represents the response of the &apos;GET /api/permission/saved&apos; operation.
/// </summary>
public sealed record PermissionSavedListResponse : OpenCodeResponse
{
    private readonly IReadOnlyList<PermissionSavedInfo>? _saved;
    /// <summary>
    /// Initializes a success instance of the &apos;PermissionSavedListResponse&apos; envelope.
    /// </summary>
    public PermissionSavedListResponse()
    {
    }

    /// <summary>
    /// Initializes an error-path instance; the payload stays unset behind its guard.
    /// </summary>
    [SetsRequiredMembers]
    internal PermissionSavedListResponse(int status, IOpenCodeError? error, string? rawBody)
    {
        Status = status;
        IsError = true;
        Error = error;
        RawBody = rawBody;
        Saved = null!;
    }

    /// <summary>
    /// Gets the Saved payload; guarded on the error path.
    /// </summary>
    public required IReadOnlyList<PermissionSavedInfo> Saved { get => _saved ?? throw new InvalidOperationException("The response is an error; check IsError before accessing Saved."); init => _saved = value; }

    /// <summary>
    /// Prints the shared metadata and appends the payload only when it is present.
    /// </summary>
    protected override bool PrintMembers(StringBuilder builder)
    {
        ArgumentNullException.ThrowIfNull(builder);
        var printed = base.PrintMembers(builder);
        if (_saved is null)
        {
            return printed;
        }

        if (printed)
        {
            _ = builder.Append(", ");
        }

        _ = builder.Append("Saved = ").Append(_saved);
        return true;
    }
}
