// Generated by OpenCode.Sdk.Tools from spec/openapi.json.
// Do not edit by hand — change tools/curation.json or the emitters, then regenerate.
using System.Diagnostics.CodeAnalysis;
using System.Text;
using OpenCode.Sdk.Models;

namespace OpenCode.Sdk;
/// <summary>
/// Represents the response of the &apos;GET /api/config/shell&apos; operation.
/// </summary>
public sealed record ConfigShellsResponse : OpenCodeResponse
{
    private readonly IReadOnlyList<ConfigShellOption>? _shells;
    /// <summary>
    /// Initializes a success instance of the &apos;ConfigShellsResponse&apos; envelope.
    /// </summary>
    public ConfigShellsResponse()
    {
    }

    /// <summary>
    /// Initializes an error-path instance; the payload stays unset behind its guard.
    /// </summary>
    [SetsRequiredMembers]
    internal ConfigShellsResponse(int status, IOpenCodeError? error, string? rawBody)
    {
        Status = status;
        IsError = true;
        Error = error;
        RawBody = rawBody;
        Shells = null!;
    }

    /// <summary>
    /// Gets the Shells payload; guarded on the error path.
    /// </summary>
    public required IReadOnlyList<ConfigShellOption> Shells { get => _shells ?? throw new InvalidOperationException("The response is an error; check IsError before accessing Shells."); init => _shells = value; }

    /// <summary>
    /// Prints the shared metadata and appends the payload only when it is present.
    /// </summary>
    protected override bool PrintMembers(StringBuilder builder)
    {
        ArgumentNullException.ThrowIfNull(builder);
        var printed = base.PrintMembers(builder);
        if (_shells is null)
        {
            return printed;
        }

        if (printed)
        {
            _ = builder.Append(", ");
        }

        _ = builder.Append("Shells = ").Append(_shells);
        return true;
    }
}
