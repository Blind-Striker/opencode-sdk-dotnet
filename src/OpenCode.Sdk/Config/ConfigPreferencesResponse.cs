// Generated by OpenCode.Sdk.Tools from spec/openapi.json.
// Do not edit by hand — change tools/curation.json or the emitters, then regenerate.
using System.Diagnostics.CodeAnalysis;
using System.Text;
using OpenCode.Sdk.Models;

namespace OpenCode.Sdk;
/// <summary>
/// Represents the response of the &apos;GET /api/config/preferences&apos; operation.
/// </summary>
public sealed record ConfigPreferencesResponse : OpenCodeResponse
{
    private readonly ConfigPreferences? _preferences;
    /// <summary>
    /// Initializes a success instance of the &apos;ConfigPreferencesResponse&apos; envelope.
    /// </summary>
    public ConfigPreferencesResponse()
    {
    }

    /// <summary>
    /// Initializes an error-path instance; the payload stays unset behind its guard.
    /// </summary>
    [SetsRequiredMembers]
    internal ConfigPreferencesResponse(int status, IOpenCodeError? error, string? rawBody)
    {
        Status = status;
        IsError = true;
        Error = error;
        RawBody = rawBody;
        Preferences = null!;
    }

    /// <summary>
    /// Gets the Preferences payload; guarded on the error path.
    /// </summary>
    public required ConfigPreferences Preferences { get => _preferences ?? throw new InvalidOperationException("The response is an error; check IsError before accessing Preferences."); init => _preferences = value; }

    /// <summary>
    /// Prints the shared metadata and appends the payload only when it is present.
    /// </summary>
    protected override bool PrintMembers(StringBuilder builder)
    {
        ArgumentNullException.ThrowIfNull(builder);
        var printed = base.PrintMembers(builder);
        if (_preferences is null)
        {
            return printed;
        }

        if (printed)
        {
            _ = builder.Append(", ");
        }

        _ = builder.Append("Preferences = ").Append(_preferences);
        return true;
    }
}
