// Generated by OpenCode.Sdk.Tools from spec/openapi.json.
// Do not edit by hand — change tools/curation.json or the emitters, then regenerate.
using System.Diagnostics.CodeAnalysis;
using System.Text;
using OpenCode.Sdk.Models;

namespace OpenCode.Sdk;
/// <summary>
/// Represents the response of the &apos;GET /api/experimental/session/stats&apos; operation.
/// </summary>
public sealed record ExperimentalSessionStatsResponse : OpenCodeResponse
{
    private readonly SessionStatsInfo? _sessionStats;
    /// <summary>
    /// Initializes a success instance of the &apos;ExperimentalSessionStatsResponse&apos; envelope.
    /// </summary>
    public ExperimentalSessionStatsResponse()
    {
    }

    /// <summary>
    /// Initializes an error-path instance; the payload stays unset behind its guard.
    /// </summary>
    [SetsRequiredMembers]
    internal ExperimentalSessionStatsResponse(int status, IOpenCodeError? error, string? rawBody)
    {
        Status = status;
        IsError = true;
        Error = error;
        RawBody = rawBody;
        SessionStats = null!;
    }

    /// <summary>
    /// Gets the SessionStats payload; guarded on the error path.
    /// </summary>
    public required SessionStatsInfo SessionStats { get => _sessionStats ?? throw new InvalidOperationException("The response is an error; check IsError before accessing SessionStats."); init => _sessionStats = value; }

    /// <summary>
    /// Prints the shared metadata and appends the payload only when it is present.
    /// </summary>
    protected override bool PrintMembers(StringBuilder builder)
    {
        ArgumentNullException.ThrowIfNull(builder);
        var printed = base.PrintMembers(builder);
        if (_sessionStats is null)
        {
            return printed;
        }

        if (printed)
        {
            _ = builder.Append(", ");
        }

        _ = builder.Append("SessionStats = ").Append(_sessionStats);
        return true;
    }
}
