namespace OpenCode.Sdk;

/// <summary>The page order of a list operation's first page.</summary>
public enum ListOrder
{
    /// <summary>Oldest entries first.</summary>
    Ascending = 0,

    /// <summary>Newest entries first.</summary>
    Descending = 1,
}
