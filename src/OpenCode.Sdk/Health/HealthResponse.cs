// Generated by OpenCode.Sdk.Tools from spec/openapi.json.
// Do not edit by hand — change tools/curation.json or the emitters, then regenerate.
using System.Diagnostics.CodeAnalysis;
using System.Text;
using OpenCode.Sdk.Models;

namespace OpenCode.Sdk;
/// <summary>
/// Represents the response of the &apos;GET /api/health&apos; operation.
/// </summary>
public sealed record HealthResponse : OpenCodeResponse
{
    private readonly ServiceHealth? _health;
    /// <summary>
    /// Initializes a success instance of the &apos;HealthResponse&apos; envelope.
    /// </summary>
    public HealthResponse()
    {
    }

    /// <summary>
    /// Initializes an error-path instance; the payload stays unset behind its guard.
    /// </summary>
    [SetsRequiredMembers]
    internal HealthResponse(int status, OpenCodeError? error, string? rawBody)
    {
        Status = status;
        IsError = true;
        Error = error;
        RawBody = rawBody;
        Health = null!;
    }

    /// <summary>
    /// Gets the Health payload; guarded on the error path.
    /// </summary>
    public required ServiceHealth Health { get => _health ?? throw new InvalidOperationException("The response is an error; check IsError before accessing Health."); init => _health = value; }

    /// <summary>
    /// Prints the shared metadata and appends the payload only when it is present.
    /// </summary>
    protected override bool PrintMembers(StringBuilder builder)
    {
        ArgumentNullException.ThrowIfNull(builder);
        var printed = base.PrintMembers(builder);
        if (_health is null)
        {
            return printed;
        }

        if (printed)
        {
            _ = builder.Append(", ");
        }

        _ = builder.Append("Health = ").Append(_health);
        return true;
    }
}
