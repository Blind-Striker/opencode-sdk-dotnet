// Generated by OpenCode.Sdk.Tools from spec/openapi.json.
// Do not edit by hand — change tools/curation.json or the emitters, then regenerate.
using System.Diagnostics.CodeAnalysis;
using System.Text;
using OpenCode.Sdk.Models;

namespace OpenCode.Sdk;
/// <summary>
/// Represents the response of the &apos;GET /api/location&apos; operation.
/// </summary>
public sealed record LocationResponse : OpenCodeResponse
{
    private readonly LocationInfo? _resolvedLocation;
    /// <summary>
    /// Initializes a success instance of the &apos;LocationResponse&apos; envelope.
    /// </summary>
    public LocationResponse()
    {
    }

    /// <summary>
    /// Initializes an error-path instance; the payload stays unset behind its guard.
    /// </summary>
    [SetsRequiredMembers]
    internal LocationResponse(int status, IOpenCodeError? error, string? rawBody)
    {
        Status = status;
        IsError = true;
        Error = error;
        RawBody = rawBody;
        ResolvedLocation = null!;
    }

    /// <summary>
    /// Gets the ResolvedLocation payload; guarded on the error path.
    /// </summary>
    public required LocationInfo ResolvedLocation { get => _resolvedLocation ?? throw new InvalidOperationException("The response is an error; check IsError before accessing ResolvedLocation."); init => _resolvedLocation = value; }

    /// <summary>
    /// Prints the shared metadata and appends the payload only when it is present.
    /// </summary>
    protected override bool PrintMembers(StringBuilder builder)
    {
        ArgumentNullException.ThrowIfNull(builder);
        var printed = base.PrintMembers(builder);
        if (_resolvedLocation is null)
        {
            return printed;
        }

        if (printed)
        {
            _ = builder.Append(", ");
        }

        _ = builder.Append("ResolvedLocation = ").Append(_resolvedLocation);
        return true;
    }
}
